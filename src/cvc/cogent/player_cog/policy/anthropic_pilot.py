from __future__ import annotations

from typing import Any

from mettagrid_sdk.sdk import MacroDirective, MettagridState

from cvc.cogent.player_cog.policy import helpers as _h
from cvc.cogent.player_cog.policy.helpers.types import KnownEntity
from cvc.cogent.player_cog.policy.semantic_cog import (
    MettagridSemanticPolicy,
    SemanticCogAgentPolicy,
    SharedWorldModel,
)
from cvc.cogent.player_cog.policy.pilot_base import PilotAgentPolicy, PilotCyborgPolicy
from cvc.cogent.player_cog.runtime.anthropic_pilot import AnthropicPilotSession
from mettagrid.policy.policy import AgentPolicy
from mettagrid.policy.policy_env_interface import PolicyEnvInterface
from mettagrid.simulator import Action

__all__ = [
    "AnthropicCyborgPolicy",
    "AnthropicPilotAgentPolicy",
    "AnthropicPilotSession",
]

_ELEMENTS = ("carbon", "oxygen", "germanium", "silicon")
# Max distance from hub for miners (stay safe, reduce deaths)
_MINER_MAX_HUB_DISTANCE = 15


def _shared_resources(state: MettagridState) -> dict[str, int]:
    if state.team_summary is None:
        return {r: 0 for r in _ELEMENTS}
    return {r: int(state.team_summary.shared_inventory.get(r, 0)) for r in _ELEMENTS}


def _least_resource(resources: dict[str, int]) -> str:
    return min(_ELEMENTS, key=lambda r: resources[r])


class AlphaV65TrueReplicaAgentPolicy(SemanticCogAgentPolicy):
    """True v65 replica: uses original hub_penalty targeting, expansion 5/30, retreat_margin=15."""

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 15  # v65 used 15, not 20
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 5
        margin += min(_h.resource_total(state), 12) // 2
        if not _h.has_role_gear(state, role):
            margin += 10
        if (state.step or 0) >= 2_500:
            margin += 10 if role in {"aligner", "scrambler"} else 5
        return hp <= safe_steps + margin

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]
        return min(
            candidates,
            key=lambda entity: (
                _h.v65_aligner_target_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                ),
                entity.position,
            ),
        )

    def _preferred_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        candidate = self._nearest_alignable_neutral_junction(state)
        sticky = self._sticky_align_target(state)
        if sticky is None:
            return candidate
        if candidate is None:
            return sticky
        from cvc.cogent.player_cog.policy.semantic_cog import _TARGET_SWITCH_THRESHOLD
        current_pos = _h.absolute_position(state)
        team_id = _h.team_id(state)
        neutral_junctions = self._world_model.entities(
            entity_type="junction", predicate=lambda junction: junction.owner in {None, "neutral"},
        )
        enemy_junctions = self._world_model.entities(
            entity_type="junction", predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        sticky_score = _h.v65_aligner_target_score(
            current_position=current_pos,
            candidate=sticky,
            unreachable=[entity for entity in neutral_junctions if entity.position != sticky.position],
            enemy_junctions=enemy_junctions,
            claimed_by_other=False,
            hub_position=hub_pos,
        )[0]
        candidate_score = _h.v65_aligner_target_score(
            current_position=current_pos,
            candidate=candidate,
            unreachable=[entity for entity in neutral_junctions if entity.position != candidate.position],
            enemy_junctions=enemy_junctions,
            claimed_by_other=_h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            ),
            hub_position=hub_pos,
        )[0]
        if candidate.position != sticky.position and candidate_score + _TARGET_SWITCH_THRESHOLD < sticky_score:
            return candidate
        return sticky


class AlphaV65ReplicaAgentPolicy(SemanticCogAgentPolicy):
    """Replica of v65 behavior: base budgets, no network/hotspot penalties, retreat_margin=15."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._network_weight = 0.0
        self._hotspot_weight = 0.0
        self._retreat_margin = 15  # v65 era used 15, not 20

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        return 0

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = self._retreat_margin
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 5
        margin += min(_h.resource_total(state), 12) // 2
        if not _h.has_role_gear(state, role):
            margin += 10
        if (state.step or 0) >= 2_500:
            margin += 10 if role in {"aligner", "scrambler"} else 5
        return hp <= safe_steps + margin


class AlphaV65PlusBiasAgentPolicy(AlphaV65ReplicaAgentPolicy):
    """V65-style targeting + resource bias."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaBiasOnlyAgentPolicy(SemanticCogAgentPolicy):
    """Base policy + resource bias only. No budget/retreat changes."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaDoubleScrambleAgentPolicy(SemanticCogAgentPolicy):
    """Base policy + 2 scramblers from step 100 for more opponent disruption."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)

        if objective == "resource_coverage":
            return 0, 0

        if step < 30:
            pressure_budget = 2
        elif step < 3000:
            pressure_budget = 6  # 4 aligners + 2 scramblers, 2 miners
            if min_res < 1 and not can_hearts:
                pressure_budget = 2
            elif min_res < 3:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 1 and not can_hearts:
                pressure_budget = 3

        scrambler_budget = 0
        if step >= 100:
            scrambler_budget = 2
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaSuperAggroAgentPolicy(SemanticCogAgentPolicy):
    """Maximum aggression: 6 pressure from step 30, resource bias."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)

        if objective == "resource_coverage":
            return 0, 0

        if step < 20:
            pressure_budget = 3
        else:
            pressure_budget = 6  # 4a + 2s = 2 miners
            if min_res < 1 and not can_hearts:
                pressure_budget = 3

        scrambler_budget = 0
        if step >= 50:
            scrambler_budget = 2
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaScrambleHeavyAgentPolicy(SemanticCogAgentPolicy):
    """3 scramblers + 3 aligners + 2 miners. Heavy disruption focus."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)

        if objective == "resource_coverage":
            return 0, 0

        if step < 30:
            return 2, 0

        pressure_budget = 6  # 3a + 3s, 2 miners
        if min_res < 1 and not can_hearts:
            pressure_budget = 3

        scrambler_budget = min(3, pressure_budget // 2) if step >= 100 else 0
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaV65ScrambleHeavyAgentPolicy(AlphaV65ReplicaAgentPolicy):
    """V65 targeting + 3 scramblers."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)

        if objective == "resource_coverage":
            return 0, 0

        if step < 30:
            return 2, 0

        pressure_budget = 6
        if min_res < 1 and not can_hearts:
            pressure_budget = 3

        scrambler_budget = min(3, pressure_budget // 2) if step >= 100 else 0
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTeamAwareAgentPolicy(AlphaV65ReplicaAgentPolicy):
    """V65 targeting + team-size-aware budgets for tournament variable team sizes."""

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        """Lower deposit threshold (12) for faster economy turnover."""
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        threshold = 12 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        safe_target = self._nearest_friendly_depot(state)
        if safe_target is None:
            return cargo >= 4
        safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
        if cargo >= 12 and safe_distance > 18:
            return True
        if cargo >= 8 and self._should_retreat(state, "miner", safe_target):
            return True
        if cargo >= 8 and self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            return True
        return False

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2 agents: 1 aligner + 1 miner, no scrambler
        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0  # Both mine until economy is established
            if objective == "economy_bootstrap":
                return 1, 0
            return 1, 0

        # 3-4 agents: 2 aligners + rest miners, scrambler at step 200+
        if num_agents <= 4:
            if step < 30:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: base aggressive budgets
        if step < 30:
            pressure_budget = 2
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(2, num_agents // 4)
            elif min_res < 3:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaV65RealignAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 targeting + re-alignment boost + team-aware budgets + lower deposit.

    Best known targeting (hub_penalty) + prioritize re-aligning scrambled
    junctions + adapt to variable team sizes.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """V65 targeting with hotspot re-alignment boost."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]
        return min(
            candidates,
            key=lambda entity: (
                _h.v65_aligner_target_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=8.0,
                ),
                entity.position,
            ),
        )

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        """Negative count = re-alignment bonus for recently scrambled junctions."""
        if hub is None:
            return 0
        rel = (entity.global_x - hub.global_x, entity.global_y - hub.global_y)
        count = self._shared_hotspots.get(rel, 0)
        return -min(count, 3)

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        """Lower deposit threshold (12)."""
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        threshold = 12 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        safe_target = self._nearest_friendly_depot(state)
        if safe_target is None:
            return cargo >= 4
        safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
        if cargo >= 12 and safe_distance > 18:
            return True
        if cargo >= 8 and self._should_retreat(state, "miner", safe_target):
            return True
        if cargo >= 8 and self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            return True
        return False

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 20:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: v65-style budgets
        if step < 30:
            pressure_budget = 2
        elif step < 3000:
            pressure_budget = 5
            if min_res < 1 and not can_hearts:
                pressure_budget = 2
            elif min_res < 3:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 1 and not can_hearts:
                pressure_budget = 3

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = 2
        elif step >= 100:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaV65TeamAwareAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 true targeting (hub_penalty) + team-size-aware budgets."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return 1, 0

        if num_agents <= 4:
            if step < 30:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: aggressive with floor of 3 to prevent gear oscillation
        if step < 30:
            pressure_budget = 2
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)  # Floor: keep roles stable
            elif min_res < 3:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaCleanTeamAwareAgentPolicy(SemanticCogAgentPolicy):
    """Base targeting (network weight) + team-size-aware budgets + resource bias."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return 1, 0

        if num_agents <= 4:
            if step < 30:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: base aggressive budgets with oscillation floor
        if step < 30:
            pressure_budget = 2
        elif step < 3000:
            pressure_budget = 5
            if min_res < 1 and not can_hearts:
                pressure_budget = 3  # Floor prevents gear churn
            elif min_res < 3:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 1 and not can_hearts:
                pressure_budget = 3

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = 2
        elif step >= 100:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaRealignBoostAgentPolicy(AlphaV65ReplicaAgentPolicy):
    """Re-alignment boost: prioritize re-aligning recently scrambled junctions.

    Key insight: hotspot penalty DISCOURAGES re-aligning junctions we lost.
    But re-aligning is cheap (junction is already in our network) and high-value
    (restores score immediately). Flip the penalty to a bonus.

    Also: faster ramp, team-aware budgets, lower deposit threshold.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # CRITICAL: V65Replica sets hotspot_weight=0.0, which nullifies our boost.
        # Re-enable it so negative hotspot counts create bonuses.
        self._hotspot_weight = 8.0

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        """Return negative hotspot count = BONUS for re-alignment targets.

        With hotspot_weight=8.0, a count of -3 gives a bonus of -24 points,
        strongly prioritizing recently scrambled junctions for re-alignment.
        """
        if hub is None:
            return 0
        rel = (entity.global_x - hub.global_x, entity.global_y - hub.global_y)
        count = self._shared_hotspots.get(rel, 0)
        # Flip: recently scrambled junctions get a BONUS (negative count)
        # Cap at -3 to avoid over-prioritizing highly contested junctions
        return -min(count, 3)

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        """Lower deposit threshold (12) for faster economy turnover."""
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        threshold = 12 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        safe_target = self._nearest_friendly_depot(state)
        if safe_target is None:
            return cargo >= 4
        safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
        if cargo >= 12 and safe_distance > 18:
            return True
        if cargo >= 8 and self._should_retreat(state, "miner", safe_target):
            return True
        if cargo >= 8 and self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            return True
        return False

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2 agents: 1 aligner + 1 miner, no scrambler
        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        # 3-4 agents: 2 aligners + rest miners, NO scrambler (economy too tight)
        if num_agents <= 4:
            if step < 20:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, 0  # No scramblers for small teams

        # 5+ agents: aggressive ramp, 5 aligners at peak
        if step < 10:
            pressure_budget = 2
        elif step < 50:
            pressure_budget = 3  # Fast ramp: 3 aligners from step 10
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)  # 5 pressure, 1 scrambler
            if min_res < 1 and not can_hearts:
                pressure_budget = max(2, num_agents // 4)
            elif min_res < 3:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaMaxAlignAgentPolicy(AlphaV65ReplicaAgentPolicy):
    """Maximum alignment: 6 aligners, 0 scramblers for 8 agents.
    Tests whether pure alignment focus beats mixed strategy."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = 8.0

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        """Re-alignment boost (same as RealignBoost)."""
        if hub is None:
            return 0
        rel = (entity.global_x - hub.global_x, entity.global_y - hub.global_y)
        count = self._shared_hotspots.get(rel, 0)
        return -min(count, 3)

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        threshold = 12 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        safe_target = self._nearest_friendly_depot(state)
        if safe_target is None:
            return cargo >= 4
        safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
        if cargo >= 12 and safe_distance > 18:
            return True
        if cargo >= 8 and self._should_retreat(state, "miner", safe_target):
            return True
        return False

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 20:
                return 1, 0
            aligner_budget = min(num_agents - 1, 3)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, 0  # No scramblers

        # 5+ agents: ALL pressure goes to alignment
        if step < 10:
            pressure_budget = 2
        elif step < 50:
            pressure_budget = 3
        else:
            pressure_budget = min(6, num_agents - 2)  # 6 aligners, 2 miners
            if min_res < 1 and not can_hearts:
                pressure_budget = max(2, num_agents // 4)
            elif min_res < 3:
                pressure_budget = min(4, num_agents - 2)

        if objective == "economy_bootstrap":
            return min(pressure_budget, 2), 0
        return pressure_budget, 0  # NO scramblers


class AlphaStableBoostAgentPolicy(AlphaRealignBoostAgentPolicy):
    """RealignBoost with more stable budgets to prevent role oscillation.

    Key change: tighter economy thresholds and higher floors prevent
    agents from oscillating between aligner and miner roles, which wastes
    gear and kills performance on marginal-economy seeds.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 20:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: stable budgets with higher floors
        if step < 10:
            pressure_budget = 2
        elif step < 50:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)  # Floor 3, not 2
            elif min_res < 2:  # Tighter threshold: only drop at <2, not <3
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaZoneBoostAgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with zone-based aligner targeting.

    Each aligner prefers junctions in their assigned sector (N/E/S/W),
    reducing travel time, target conflicts, and enabling faster re-alignment
    of recently scrambled junctions in their patrol area.
    """

    # Zone assignment by agent_id (angles in degrees, 0=North, clockwise)
    _ZONE_ANGLES = {
        0: 0,    # N
        1: 90,   # E
        2: 180,  # S
        4: 270,  # W
        5: 45,   # NE (5th aligner)
    }
    _ZONE_BONUS = 6.0  # Score bonus for junctions in agent's zone

    def _zone_score_bonus(self, entity: KnownEntity, hub: KnownEntity | None) -> float:
        """Return negative bonus (lower score = better) for junctions in agent's zone."""
        if hub is None or self._agent_id not in self._ZONE_ANGLES:
            return 0.0
        import math
        dx = entity.global_x - hub.global_x
        dy = entity.global_y - hub.global_y
        if dx == 0 and dy == 0:
            return 0.0
        # Angle from hub to junction (0=North, clockwise)
        angle = math.degrees(math.atan2(dx, -dy)) % 360
        zone_angle = self._ZONE_ANGLES[self._agent_id]
        # Angular distance (0-180)
        diff = abs(angle - zone_angle)
        if diff > 180:
            diff = 360 - diff
        # Bonus scales with proximity to zone center: full bonus at 0°, none at 90°+
        if diff >= 90:
            return 0.0
        return -self._ZONE_BONUS * (1.0 - diff / 90.0)

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Standard targeting + zone preference bonus."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]
        return min(
            candidates,
            key=lambda entity: (
                _h.aligner_target_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    friendly_junctions=friendly_junctions,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    network_weight=self._network_weight,
                    hotspot_weight=self._hotspot_weight,
                    expansion_weight=self._expansion_weight,
                    expansion_cap=self._expansion_cap,
                )[0] + self._zone_score_bonus(entity, hub),
                entity.position,
            ),
        )


class AlphaAggroBoostAgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with more aggressive heart batching and faster ramp.

    Changes from StableBoost:
    - Heart batch target 4 from step 200 (not 500), 5 from step 500 (not 2000)
    - Even faster ramp: 3 aligners from step 5 (not 10)
    - 2 scramblers from step 200 for 5+ agents (not just step 3000)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 20 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 15:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 150 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: faster ramp, earlier scramblers
        if step < 5:
            pressure_budget = 2
        elif step < 30:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)
            elif min_res < 2:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)

        # Earlier scrambler: 1 from step 50, 2 from step 200
        scrambler_budget = 0
        if step >= 200:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 50:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaV65NoScrambleBoostAgentPolicy(AlphaV65RealignAgentPolicy):
    """V65Realign but with NO scramblers — all hearts go to alignment.

    Inherits V65 targeting + re-alignment boost + lower deposit threshold.
    Overrides budgets to remove scramblers entirely.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Team-aware budgets with NO scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 20:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, 0

        # 5+ agents: all pressure to alignment, keep 3 miners
        if step < 30:
            return 2, 0
        aligner_budget = min(5, num_agents - 3)
        if min_res < 1 and not can_hearts:
            aligner_budget = max(2, num_agents // 4)
        elif min_res < 3:
            aligner_budget = min(4, num_agents - 3)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, 0


class AlphaNoScrambleAgentPolicy(SemanticCogAgentPolicy):
    """All aligners, no scramblers. 5 aligners + 3 miners."""

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)

        if objective == "resource_coverage":
            return 0, 0

        if step < 30:
            return 2, 0

        aligner_budget = 5
        if min_res < 1 and not can_hearts:
            aligner_budget = 2

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, 0


class AlphaCogAgentPolicy(SemanticCogAgentPolicy):
    """Optimized agent policy: re-alignment boost + stable budgets + scrambler defense."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._last_budget_change_step = 0
        self._last_bias_resource: str | None = None
        # Init budget adapts to team size, capped to leave 1 miner
        num_agents = self.policy_env_info.num_agents
        self._current_aligner_budget = min(4, max(num_agents - 1, 1))
        # Match StableBoost proven config
        self._hotspot_weight = 8.0   # Re-alignment boost (flip hotspot to bonus)
        self._network_weight = 0.0   # No network proximity penalty (allows expansion)

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        """Flip hotspot count to BONUS — prioritize re-aligning scrambled junctions."""
        if hub is None:
            return 0
        rel = (entity.global_x - hub.global_x, entity.global_y - hub.global_y)
        count = self._shared_hotspots.get(rel, 0)
        return -min(count, 3)

    def _preferred_miner_extractor(self, state: MettagridState) -> KnownEntity | None:
        """Clear sticky target when resource priority changes significantly.

        Also: if the bottleneck resource has no known extractors and is critically
        low, return None to force exploration (discover missing extractor types).
        """
        resources = _shared_resources(state)
        least = _least_resource(resources)
        least_amount = resources[least]

        if (
            self._sticky_target_kind is not None
            and self._sticky_target_kind.endswith("_extractor")
        ):
            current_resource = self._sticky_target_kind.removesuffix("_extractor")
            if current_resource != least:
                if least_amount < 7:
                    self._clear_sticky_target()
                elif least_amount > 0:
                    max_amount = max(resources.values())
                    if resources[current_resource] > max_amount * 0.8 and least_amount < max_amount * 0.5:
                        self._clear_sticky_target()

        if self._last_bias_resource is not None and self._last_bias_resource != self._resource_bias:
            if least_amount < 14:
                self._clear_sticky_target()
        self._last_bias_resource = self._resource_bias

        # If bottleneck resource is critically low (< 7, can't make hearts)
        # while other resources are abundant (10x imbalance), and we have no
        # known extractors for the bottleneck, explore to discover them.
        # This prevents miners from ignoring undiscovered extractor types.
        max_amount = max(resources.values())
        if least_amount < 7 and max_amount >= least_amount * 10 + 20:
            step = state.step or self._step_index
            least_extractors = self._world_model.entities(
                entity_type=f"{least}_extractor",
                predicate=lambda entity: _h.is_usable_recent_extractor(entity, step=step),
            )
            if not least_extractors:
                self._clear_sticky_target()
                return None

        return super()._preferred_miner_extractor(state)

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Less conservative retreat (margin=15) + miner hub-distance check."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 15  # Less conservative than default 20
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 5
        margin += min(_h.resource_total(state), 12) // 2
        if not _h.has_role_gear(state, role):
            margin += 10
        if (state.step or 0) >= 2_500:
            margin += 10 if role in {"aligner", "scrambler"} else 5
        if hp <= safe_steps + margin:
            return True
        # Miners: retreat if too far from hub
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 20:
                return True
        return False

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        """Lower deposit threshold (12) for faster economy turnover."""
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        threshold = 12 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        safe_target = self._nearest_friendly_depot(state)
        if safe_target is None:
            return cargo >= 4
        safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
        if cargo >= 8 and self._should_retreat(state, "miner", safe_target):
            return True
        return cargo >= 4 and safe_distance <= 3

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Extended aligner: when no frontier, walk toward nearest unreachable junction."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier junctions — try to expand toward known unreachable junctions
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            # Filter to junctions we can safely reach: distance < hp - safety margin
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 30
                and _h.manhattan(hub_pos, j.position) < 40  # Don't go too far from hub
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            # Only expand if target is reasonably close
            if dist < hp - 30 and _h.manhattan(hub_pos, nearest.position) < 40:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # No expansion target — help economy by mining (idle-mine >> idle-explore in practice)
        return self._miner_action(state, summary_prefix="idle_align_")

    def evaluate_state(self, state: MettagridState) -> Action:
        action = super().evaluate_state(state)
        step = state.step or self._step_index
        role = self._infos.get("role", "?")
        subtask = self._infos.get("subtask", "?")
        # Log every 100 steps for agent 0 (detailed), every 500 for all
        if step % 500 == 0 or step == 1 or (step % 100 == 0 and self._agent_id == 0):
            pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hearts = int(state.self_state.inventory.get("heart", 0))
            cargo = _h.resource_total(state)
            aligner_b = self._infos.get("aligner_budget", "?")
            scrambler_b = self._infos.get("scrambler_budget", "?")
            frontier = self._infos.get("frontier_neutral_junctions", "?")
            team_id = _h.team_id(state)
            friendly = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner == team_id))
            enemy = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner not in {None, "neutral", team_id}))
            shared = _shared_resources(state)
            hub = self._nearest_hub(state)
            hub_pos = (hub.global_x, hub.global_y) if hub else None
            print(
                f"[COG] step={step} agent={self._agent_id} pos={pos} hub={hub_pos} role={role} "
                f"subtask={subtask} hp={hp} hearts={hearts} cargo={cargo} "
                f"aligners={aligner_b} scramblers={scrambler_b} "
                f"friendly_j={friendly} enemy_j={enemy} frontier={frontier} "
                f"hub_res={shared}",
                flush=True,
            )
        return action

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Stable role allocation with resource-responsive scaling."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # 2 agents: mine first 200 steps, then 1 aligner if economy supports it
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0  # Both mine to build economy
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0  # Economy-first: 1 aligner, rest mine
            # Only ramp up when economy can support it
            if min_res < 7:
                return 1, 0  # Keep mining until we have hearts capacity
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: economy-responsive with aggressive peak
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3  # Ramp up slowly
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)

        # Scramblers: delay until economy can support it
        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaAggressiveAgentPolicy(AlphaCogAgentPolicy):
    """Aggressive variant: faster early game, idle aligners scramble, more pressure.

    Key changes from AlphaCog:
    1. Lower early heart batch target (1 for steps 0-200) → faster first alignments
    2. Idle aligners become scramblers instead of miners → deny opponent score
    3. More aligners + earlier scramblers → economy surplus is wasted on mining
    4. Economy-aware: only 1 miner needed once hub has 100+ of each resource
    5. Less conservative retreat: lower margins → more productive time
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Tighter retreat margins — agents spend more time being productive."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 10  # Tighter than parent's 15
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 8
        margin += int(state.self_state.inventory.get("heart", 0)) * 3  # 3 per heart vs 5
        margin += min(_h.resource_total(state), 12) // 3  # Less cargo penalty
        if not _h.has_role_gear(state, role):
            margin += 8
        if (state.step or 0) >= 2_500:
            margin += 5 if role in {"aligner", "scrambler"} else 3
        if hp <= safe_steps + margin:
            return True
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 15:
                return True
        return False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner with idle-scramble instead of idle-mine."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Early game: don't batch, go align immediately with whatever hearts we have
        if step < 200:
            pass  # Skip batching check entirely
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier junctions — expand toward known unreachable junctions
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos
        # Check both neutral AND enemy junctions as expansion targets
        # (enemy junctions near our network can be scrambled then re-aligned)
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20  # Less conservative
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle aligners: scramble if economy healthy, mine if economy tight, explore if both fail
        min_res = _h.team_min_resource(state)
        if int(state.self_state.inventory.get("heart", 0)) > 0 and min_res >= 14:
            # Economy healthy: scramble to deny opponent score
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 14:
            # Economy needs help: mine to rebuild
            return self._miner_action(state, summary_prefix="idle_align_")

        # Explore to discover new junction clusters
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """More aggressive: more aligners, earlier scramblers, fewer miners."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0  # Economy-first: 1 aligner, rest mine
            if min_res < 7 and not can_hearts:
                return 1, 0
            # 4 agents: max alignment pressure. Scrambler only with massive surplus.
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                # Economy healthy: 3 aligners + 1 miner
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 200 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents: match AlphaCog budgets + economy surplus boost
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            # Surplus: shift to max pressure, only 1-2 miners needed
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Scramblers: start at step 200 like AlphaCog
        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaAggressivePolicy(MettagridSemanticPolicy):
    """Aggressive scoring: fast early game, idle-scramble, economy-aware budgets."""
    short_names = ["alpha-aggressive"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAggressiveAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# Keep these for backwards compatibility with tournament uploads
class AnthropicPilotAgentPolicy(PilotAgentPolicy):
    _LLM_ANALYSIS_INTERVAL = 500  # Run LLM analysis every N steps

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._last_budget_change_step = 0
        num_agents = self.policy_env_info.num_agents
        self._current_aligner_budget = min(4, max(num_agents - 1, 1))

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Use LLM directive when available, fall back to heuristic resource bias."""
        # Let the pilot session provide the directive (incorporates runtime reviews)
        try:
            llm_directive = self._pilot_session.directive_for_state(state, memory=self._memory)
            # Always override resource_bias with heuristic (LLM can't see economy)
            resources = _shared_resources(state)
            least = _least_resource(resources)
            llm_directive = MacroDirective(
                resource_bias=least,
                objective=llm_directive.objective,
                note=llm_directive.note,
            )
            step = state.step or self._step_index
            if step > 0 and step % self._LLM_ANALYSIS_INTERVAL == 0:
                print(
                    f"[LLM] step={step} agent={self._agent_id} "
                    f"objective={llm_directive.objective} "
                    f"note={llm_directive.note} "
                    f"bias={least}",
                    flush=True,
                )
            return llm_directive
        except Exception as e:
            resources = _shared_resources(state)
            least = _least_resource(resources)
            return MacroDirective(resource_bias=least)

    def _run_llm_analysis(self, state: MettagridState, current_directive: MacroDirective) -> None:
        """Ask the LLM to analyze game state and log insights."""
        try:
            llm_directive = self._pilot_session.directive_for_state(state, memory=self._memory)
            print(
                f"[LLM] step={state.step} agent={self._agent_id} "
                f"llm_objective={llm_directive.objective} "
                f"llm_bias={llm_directive.resource_bias} "
                f"llm_note={llm_directive.note} "
                f"heuristic_bias={current_directive.resource_bias}",
                flush=True,
            )
        except Exception as e:
            print(f"[LLM] step={state.step} agent={self._agent_id} error={e}", flush=True)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy-responsive with hysteresis — synced with AlphaCogAgentPolicy."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if objective == "economy_bootstrap":
                return 1, 0
            return 1, 0

        if num_agents <= 4:
            if step < 10:
                return 1, 0
            aligner_budget = 2
            scrambler_budget = 1 if step >= 200 and num_agents >= 4 else 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        if step < 10:
            return min(2, num_agents - 1), 0
        if step < 100:
            aligner_budget = min(3, num_agents - 2)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, 0

        aligner_budget = min(4, num_agents - 2)
        scrambler_budget = 1 if step >= 200 else 0

        if min_res < 1 and not _h.team_can_refill_hearts(state):
            aligner_budget = max(aligner_budget - 1, 1)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Miners: retreat if too far from hub."""
        if super()._should_retreat(state, role, safe_target):
            return True
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            hp = int(state.self_state.inventory.get("hp", 0))
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 20:
                return True
        return False


class AnthropicCyborgPolicy(PilotCyborgPolicy):
    short_names = ["anthropic-cyborg", "claude-cyborg", "cyborg-anthropic"]
    _session_class = AnthropicPilotSession
    _agent_policy_class = AnthropicPilotAgentPolicy
    _background_reviews_default = True

    def _provider_session_kwargs(self, kwargs: dict[str, Any]) -> dict[str, Any]:
        return {
            "api_key": kwargs.get("api_key"),
            "api_key_file": kwargs.get("api_key_file"),
            "anthropic_api_key": kwargs.get("anthropic_api_key"),
            "anthropic_api_key_file": kwargs.get("anthropic_api_key_file"),
        }


class AlphaV65TrueReplicaPolicy(MettagridSemanticPolicy):
    """True v65: uses original hub_penalty targeting with tiered distance costs."""
    short_names = ["alpha-v65-true"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65TrueReplicaAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65ReplicaPolicy(MettagridSemanticPolicy):
    """Replica of v65: base budgets, no network/hotspot penalties."""
    short_names = ["alpha-v65-replica"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65ReplicaAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65PlusBiasPolicy(MettagridSemanticPolicy):
    """V65 targeting + resource bias."""
    short_names = ["alpha-v65-bias"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65PlusBiasAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaBiasOnlyPolicy(MettagridSemanticPolicy):
    """Base policy + resource bias. Uses base budgets (aggressive like v65)."""
    short_names = ["alpha-bias-only"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaBiasOnlyAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaDoubleScramblePolicy(MettagridSemanticPolicy):
    """Base + resource bias + 2 scramblers from step 100."""
    short_names = ["alpha-double-scramble"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaDoubleScrambleAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSuperAggroPolicy(MettagridSemanticPolicy):
    """Maximum aggression: 6 pressure from step 30."""
    short_names = ["alpha-super-aggro"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSuperAggroAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaScrambleHeavyPolicy(MettagridSemanticPolicy):
    """3 scramblers + 3 aligners. Heavy disruption."""
    short_names = ["alpha-scramble-heavy"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScrambleHeavyAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65ScrambleHeavyPolicy(MettagridSemanticPolicy):
    """V65 targeting + 3 scramblers. Best targeting + best disruption."""
    short_names = ["alpha-v65-scramble"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65ScrambleHeavyAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTeamAwarePolicy(MettagridSemanticPolicy):
    """V65 targeting + team-size-aware budgets for 2/4/6/8 agents."""
    short_names = ["alpha-team-aware"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTeamAwareAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65RealignPolicy(MettagridSemanticPolicy):
    """V65 targeting + re-alignment boost + team-aware budgets."""
    short_names = ["alpha-v65-realign"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65RealignAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65TeamAwarePolicy(MettagridSemanticPolicy):
    """V65 true targeting (hub_penalty) + team-size-aware budgets."""
    short_names = ["alpha-v65-team-aware"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65TeamAwareAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaRealignBoostPolicy(MettagridSemanticPolicy):
    """Re-alignment boost + team-aware budgets + faster ramp."""
    short_names = ["alpha-realign-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaRealignBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaMaxAlignPolicy(MettagridSemanticPolicy):
    """Maximum alignment: 6 aligners, 0 scramblers."""
    short_names = ["alpha-max-align"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMaxAlignAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCleanTeamAwarePolicy(MettagridSemanticPolicy):
    """Base targeting (network weights) + team-size-aware budgets + resource bias."""
    short_names = ["alpha-clean-team-aware"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCleanTeamAwareAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaStableBoostPolicy(MettagridSemanticPolicy):
    """RealignBoost with stable budgets to prevent role oscillation."""
    short_names = ["alpha-stable-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaStableBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaZoneBoostPolicy(MettagridSemanticPolicy):
    """StableBoost with zone-based aligner targeting."""
    short_names = ["alpha-zone-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaZoneBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaGentleZoneAgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with gentler zone preference (bonus=3 instead of 6)."""

    _ZONE_ANGLES = {
        0: 0,    # N
        1: 90,   # E
        2: 180,  # S
        4: 270,  # W
        5: 45,   # NE
    }
    _ZONE_BONUS = 3.0  # Half of ZoneBoost's 6.0

    def _zone_score_bonus(self, entity: KnownEntity, hub: KnownEntity | None) -> float:
        if hub is None or self._agent_id not in self._ZONE_ANGLES:
            return 0.0
        import math
        dx = entity.global_x - hub.global_x
        dy = entity.global_y - hub.global_y
        if dx == 0 and dy == 0:
            return 0.0
        angle = math.degrees(math.atan2(dx, -dy)) % 360
        zone_angle = self._ZONE_ANGLES[self._agent_id]
        diff = abs(angle - zone_angle)
        if diff > 180:
            diff = 360 - diff
        if diff >= 90:
            return 0.0
        return -self._ZONE_BONUS * (1.0 - diff / 90.0)

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]
        return min(
            candidates,
            key=lambda entity: (
                _h.aligner_target_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    friendly_junctions=friendly_junctions,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    network_weight=self._network_weight,
                    hotspot_weight=self._hotspot_weight,
                    expansion_weight=self._expansion_weight,
                    expansion_cap=self._expansion_cap,
                )[0] + self._zone_score_bonus(entity, hub),
                entity.position,
            ),
        )


class AlphaGentleZonePolicy(MettagridSemanticPolicy):
    """StableBoost with gentler zone preference."""
    short_names = ["alpha-gentle-zone"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaGentleZoneAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAggroBoostPolicy(MettagridSemanticPolicy):
    """StableBoost with aggressive heart batching and faster ramp."""
    short_names = ["alpha-aggro-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAggroBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65NoScrambleBoostPolicy(MettagridSemanticPolicy):
    """V65 targeting + re-alignment boost + no scramblers."""
    short_names = ["alpha-v65-noscramble-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65NoScrambleBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaNoScramblePolicy(MettagridSemanticPolicy):
    """All aligners, no scramblers."""
    short_names = ["alpha-no-scramble"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaNoScrambleAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaClusterBoostAgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with cluster-near-hub targeting.

    Key changes:
    - expansion_weight=0: don't prioritize frontier expansion
    - Agents target closest junctions, staying near hub
    - Faster re-alignment when junctions are lost (shorter travel)
    - Stronger re-align bonus (hotspot_weight=12 instead of 8)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 0.0
        self._expansion_cap = 0.0
        self._hotspot_weight = 12.0  # Stronger re-align bonus


class AlphaClusterBoostPolicy(MettagridSemanticPolicy):
    """StableBoost with cluster-near-hub targeting."""
    short_names = ["alpha-cluster-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaClusterBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaMidExpansionAgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with reduced expansion and stronger re-alignment.

    Balance between cluster (expansion=0) and full expansion (expansion=10).
    Reduced expansion keeps agents closer to hub while still allowing
    some frontier pushing. Stronger re-alignment ensures lost junctions
    are quickly recaptured.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 3.0  # Reduced from 10.0
        self._expansion_cap = 20.0    # Reduced from 60.0
        self._hotspot_weight = 12.0   # Stronger re-align bonus


class AlphaMidExpansionPolicy(MettagridSemanticPolicy):
    """StableBoost with reduced expansion and stronger re-alignment."""
    short_names = ["alpha-mid-expansion"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMidExpansionAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaDoubleScrambleMidAgentPolicy(AlphaMidExpansionAgentPolicy):
    """MidExpansion with 2 scramblers from step 100 (instead of 1).

    Hypothesis: more scrambling reduces junction losses, improving net score.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner_budget, scrambler_budget = super()._pressure_budgets(state, objective=objective)
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents
        if num_agents >= 5 and step >= 100 and aligner_budget >= 2:
            # Use 2 scramblers instead of 1, taking from aligners
            scrambler_budget = 2
            aligner_budget = max(aligner_budget - 1, 2)
        return aligner_budget, scrambler_budget


class AlphaDoubleScrambleMidPolicy(MettagridSemanticPolicy):
    """MidExpansion with 2 scramblers."""
    short_names = ["alpha-double-scramble-mid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaDoubleScrambleMidAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaMidZoneAgentPolicy(AlphaZoneBoostAgentPolicy):
    """ZoneBoost with reduced expansion and stronger re-alignment."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 3.0
        self._expansion_cap = 20.0
        self._hotspot_weight = 12.0


class AlphaMidZonePolicy(MettagridSemanticPolicy):
    """ZoneBoost with reduced expansion and stronger re-alignment."""
    short_names = ["alpha-mid-zone"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMidZoneAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaExp5AgentPolicy(AlphaStableBoostAgentPolicy):
    """StableBoost with expansion_weight=5 and hotspot=12."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 5.0
        self._expansion_cap = 30.0
        self._hotspot_weight = 12.0


class AlphaExp5Policy(MettagridSemanticPolicy):
    """StableBoost with expansion_weight=5."""
    short_names = ["alpha-exp5"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaExp5AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaTurboMidAgentPolicy(AlphaMidExpansionAgentPolicy):
    """MidExpansion with faster early ramp and late-game double scrambler.

    Early game (steps 0-3000): faster ramp to max aligners
    Late game (steps 3000+): add second scrambler to protect gains
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 15:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 150 and num_agents >= 4 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: faster early ramp
        if step < 5:
            pressure_budget = 2
        elif step < 30:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(3, num_agents // 3)
            elif min_res < 2:
                pressure_budget = min(4, num_agents - 2)
        else:
            # Late game: 6 pressure with 2 scramblers
            pressure_budget = min(6, num_agents - 1)
            if min_res < 1 and not can_hearts:
                pressure_budget = max(4, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTurboMidPolicy(MettagridSemanticPolicy):
    """MidExpansion with faster ramp and late-game double scrambler."""
    short_names = ["alpha-turbo-mid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTurboMidAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaHybridAgentPolicy(AlphaCogAgentPolicy):
    """Hybrid: v65 hub-penalty targeting + AlphaCog economy/budgets + re-alignment bonus.

    Key insight: v65's hub_penalty keeps expansion tight (reduces death risk,
    travel time) while AlphaCog's economy management prevents starvation.
    Adding hotspot bonus helps reclaim scrambled territory faster.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = 8.0  # Keep re-alignment bonus
        self._network_weight = 0.0  # Not used — hub_penalty replaces it

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Use v65's hub_penalty targeting with hotspot bonus."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]
        return min(
            candidates,
            key=lambda entity: (
                _h.v65_aligner_target_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                ),
                entity.position,
            ),
        )

    def _preferred_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Sticky target with v65 scoring + hotspot bonus."""
        candidate = self._nearest_alignable_neutral_junction(state)
        sticky = self._sticky_align_target(state)
        if sticky is None:
            return candidate
        if candidate is None:
            return sticky
        from cvc.cogent.player_cog.policy.semantic_cog import _TARGET_SWITCH_THRESHOLD
        current_pos = _h.absolute_position(state)
        team_id = _h.team_id(state)
        neutral_junctions = self._world_model.entities(
            entity_type="junction", predicate=lambda junction: junction.owner in {None, "neutral"},
        )
        enemy_junctions = self._world_model.entities(
            entity_type="junction", predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        sticky_score = _h.v65_aligner_target_score(
            current_position=current_pos,
            candidate=sticky,
            unreachable=[entity for entity in neutral_junctions if entity.position != sticky.position],
            enemy_junctions=enemy_junctions,
            claimed_by_other=False,
            hub_position=hub_pos,
            hotspot_count=self._junction_hotspot_count(sticky, hub),
            hotspot_weight=self._hotspot_weight,
        )[0]
        candidate_score = _h.v65_aligner_target_score(
            current_position=current_pos,
            candidate=candidate,
            unreachable=[entity for entity in neutral_junctions if entity.position != candidate.position],
            enemy_junctions=enemy_junctions,
            claimed_by_other=_h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            ),
            hub_position=hub_pos,
            hotspot_count=self._junction_hotspot_count(candidate, hub),
            hotspot_weight=self._hotspot_weight,
        )[0]
        if candidate.position != sticky.position and candidate_score + _TARGET_SWITCH_THRESHOLD < sticky_score:
            return candidate
        return sticky

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Tuned for tournament: 75% of games are 2-4 agents. More conservative scrambling."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0  # Fast ramp: 1 aligner from step 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            # No scramblers in small teams — all resources to alignment
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 500 and min_res >= 14:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaHybridPolicy(MettagridSemanticPolicy):
    """Hybrid policy: v65 targeting + AlphaCog economy."""
    short_names = ["alpha-hybrid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaHybridAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaNoScrambleCogAgentPolicy(AlphaCogAgentPolicy):
    """AlphaCog but zero scramblers — all pressure budget goes to alignment.

    Hypothesis: in tournament, hearts spent on scrambling could be better
    used for alignment. More aligners = more territory = higher score.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(num_agents - 1, 3)  # Max aligners, keep 1 miner
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0  # No scramblers ever

        # 5+ agents
        if step < 30:
            return 2, 0
        elif step < 100:
            return 3, 0
        else:
            aligner_budget = min(num_agents - 2, 6)
            if min_res < 3 and not can_hearts:
                aligner_budget = max(2, num_agents // 3)
            elif min_res < 7:
                aligner_budget = min(4, num_agents - 2)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, 0  # No scramblers ever


class AlphaNoScramblePolicy(MettagridSemanticPolicy):
    """AlphaCog with zero scramblers."""
    short_names = ["alpha-noscramble"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaNoScrambleCogAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSoftHubAgentPolicy(AlphaCogAgentPolicy):
    """AlphaCog + mild hub proximity preference (network_weight=0.5).

    v65's hub_penalty was too harsh (kills self-play scores).
    network_weight=0.0 (AlphaCog) has no hub preference at all.
    This uses network_weight=0.5 as a soft middle ground.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._network_weight = 0.5  # Mild hub/network preference


class AlphaSoftHubPolicy(MettagridSemanticPolicy):
    """AlphaCog with mild hub proximity preference."""
    short_names = ["alpha-softhub"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSoftHubAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCyborgPolicy(MettagridSemanticPolicy):
    """Lightweight policy without LLM dependencies."""
    short_names = ["alpha-cyborg"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCogAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCyborgGlobalRolesPolicy(MettagridSemanticPolicy):
    """All AlphaCog improvements + global role assignment (like v65)."""
    short_names = ["alpha-cyborg-global"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # Do NOT pass shared_team_ids — use global role priorities like v65
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCogAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                # No shared_team_ids → global role priorities like v65
            )
        return self._agent_policies[agent_id]


class AlphaV65BuggyStationAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 replica with original station targeting bug (pre-fix behavior)."""

    def _acquire_role_gear(self, state: MettagridState, role: str) -> tuple[Action, str]:
        """Replicate pre-fix station targeting: use raw spawn-relative coords."""
        station_type = f"{role}_station"
        current_pos = _h.absolute_position(state)
        station = self._world_model.nearest(position=current_pos, entity_type=station_type)
        if station is not None:
            return self._move_to_known(state, station, summary=f"get_{role}_gear", vibe="change_vibe_gear")
        # OLD BUG: use spawn-relative coords as absolute (sends agents to wrong position)
        target = _h.spawn_relative_station_target(self._agent_id, role)
        if target is None:
            hub = self._nearest_hub(state)
            if hub is None:
                return self._explore_action(state, role=role, summary=f"find_{role}_station")
            from cvc.cogent.player_cog.policy.semantic_cog import _STATION_OFFSETS
            dx, dy = _STATION_OFFSETS[role]
            target = (hub.global_x + dx, hub.global_y + dy)
        return self._move_to_position(state, target, summary=f"search_{role}_station", vibe="change_vibe_gear")


class AlphaV65BuggyStationPolicy(MettagridSemanticPolicy):
    """V65 replica with station targeting bug + global roles."""
    short_names = ["alpha-v65-buggy-station"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65BuggyStationAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaV65OriginalPolicy(MettagridSemanticPolicy):
    """True v65 replica WITHOUT team-relative role assignment.

    v65 was uploaded before the shared_team_ids fix, so it uses global agent IDs
    for role assignment. This replicates that exact behavior.
    """
    short_names = ["alpha-v65-original"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # Do NOT pass shared_team_ids — v65 used global role assignment
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65TrueReplicaAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaV65ExactAgentPolicy(SemanticCogAgentPolicy):
    """Exact v65 agent: original constants, budgets, priorities.

    Matches v65's original behavior:
    - pressure_budget=4 immediate, 5 when resources>=20
    - Scrambler at step 1500 (not 100)
    - No early-game survival logic overrides
    - No hotspot tracking (weight=0)
    - No network weight
    """

    # v65 original priorities
    _V65_ALIGNER_PRIORITY = (4, 5, 6, 7, 3)
    _V65_SCRAMBLER_PRIORITY = (7, 6)

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = 0.0   # v65 had no hotspot tracking
        self._network_weight = 0.0   # v65 had no network penalty

    def _desired_role(self, state: MettagridState, *, objective: str | None = None) -> str:
        """v65 role assignment: agents 4,5,6,7 are aligners, 7,6 are scramblers."""
        aligner_budget, scrambler_budget = self._pressure_budgets(state, objective=objective)
        scrambler_ids = set(self._V65_SCRAMBLER_PRIORITY[:scrambler_budget])
        aligner_ids = []
        for agent_id in self._V65_ALIGNER_PRIORITY:
            if agent_id in scrambler_ids:
                continue
            if len(aligner_ids) == aligner_budget:
                break
            aligner_ids.append(agent_id)
        if self._agent_id in scrambler_ids:
            return "scrambler"
        if self._agent_id in aligner_ids:
            return "aligner"
        return "miner"

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """v65 original: simple 4-aligner budget, scrambler at step 1500."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)

        if objective == "resource_coverage":
            return 0, 0

        pressure_budget = 4
        if step >= 40 and min_res >= 20:
            pressure_budget = 5

        scrambler_budget = 0
        if step >= 1_500:
            scrambler_budget = 1
        aligner_budget = pressure_budget - scrambler_budget

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaV65ExactPolicy(MettagridSemanticPolicy):
    """Exact v65 replica: original constants, global role assignment, simple budgets."""
    short_names = ["alpha-v65-exact"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # No shared_team_ids — v65 used global role priorities
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65ExactAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """Tournament-optimized: v65 targeting + idle-mine + team-aware budgets.

    Combines proven tournament elements:
    - v65 hub_penalty targeting (conservative, stays near hub — safer in PvP)
    - Idle aligners mine instead of exploring (biggest local improvement: +89%)
    - Team-size-aware budgets (handles 2v6, 4v4, 6v2 tournament formats)
    - Default retreat margin (20) for better survivability in PvP
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Default retreat margin (20) — more conservative than v65's 15 for PvP safety."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 20  # More conservative for tournament PvP
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 5
        margin += min(_h.resource_total(state), 12) // 2
        if not _h.has_role_gear(state, role):
            margin += 10
        if (state.step or 0) >= 2_500:
            margin += 10 if role in {"aligner", "scrambler"} else 5
        return hp <= safe_steps + margin

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting with idle-mine: when no frontier junctions, mine to boost economy."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier — explore to find new junctions (avoid _miner_action to prevent gear churn)
        return self._explore_action(state, role="aligner", summary="idle_explore")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy-first budgets for tournament — build economy before aggression."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0  # Economy-first: 1 aligner, rest mine
            if min_res < 7:
                return 1, 0  # Keep mining until hearts capacity
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: economy-responsive
        if step < 30:
            pressure_budget = 2
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = 5
            if min_res < 3 and not can_hearts:
                pressure_budget = 2
            elif min_res < 7:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 3 and not can_hearts:
                pressure_budget = 2

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = 2
        elif step >= 100:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaTournamentPolicy(MettagridSemanticPolicy):
    """Tournament-optimized: v65 targeting + idle-mine + team-aware."""
    short_names = ["alpha-tournament"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65IdleMinePolicy(MettagridSemanticPolicy):
    """V65 targeting + global roles (like v65) + idle-mine improvement.

    Combines the proven v65 tournament formula (global role assignment,
    hub_penalty targeting) with the biggest local improvement (idle-mine).
    Does NOT use shared_team_ids to match v65's behavior.
    """
    short_names = ["alpha-v65-idle-mine"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # Do NOT pass shared_team_ids — match v65's global role assignment
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                # No shared_team_ids → global role priorities like v65
            )
        return self._agent_policies[agent_id]


class AlphaV65HybridAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 budgets + team-relative roles + idle-mine.

    The key insight: v65's tournament success comes from its aggressive
    budget (4 aligners from step 0). Previous "improvements" delayed
    aligners with economy-first budgets, which loses territory in PvP.

    This hybrid keeps v65's proven budgets and targeting, adds:
    - Team-relative role assignment (works for any team size)
    - Idle-mine for aligners when no frontier (biggest local gain)
    - Logging for tournament debugging
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # Disable network/hotspot weights like v65
        self._network_weight = 0.0
        self._hotspot_weight = 0.0

    def _choose_action(self, state: MettagridState, role: str) -> tuple[Action, str]:
        """Override to skip early-game survival code (hub_camp_heal, wipeout_hub_hold).

        V65 doesn't have these — agents play immediately. The survival code
        wastes early steps and causes permanent stalls at hp=0.
        """
        if role not in {"aligner", "miner"}:
            self._clear_target_claim()
            self._clear_sticky_target()
        elif role == "aligner" and self._sticky_target_kind not in {None, "junction"}:
            self._clear_sticky_target()
        elif role == "miner" and (
            self._sticky_target_kind is not None and not self._sticky_target_kind.endswith("_extractor")
        ):
            self._clear_sticky_target()

        safe_target = self._nearest_hub(state)

        # Skip early-game survival code — go straight to retreat like v65
        if self._should_retreat(state, role, safe_target):
            self._clear_target_claim()
            self._clear_sticky_target()
            if safe_target is not None:
                safe_distance = _h.manhattan(_h.absolute_position(state), safe_target.position)
                if safe_distance > 2:
                    return self._move_to_known(state, safe_target, summary="retreat_to_hub")
            if _h.has_role_gear(state, role):
                return self._hold(summary="retreat_hold", vibe="change_vibe_default")

        if self._oscillation_steps >= 4:
            return self._unstick_action(state, role)

        if self._stalled_steps >= 12:
            return self._unstick_action(state, role)

        if role != "miner" and _h.needs_emergency_mining(state):
            return self._miner_action(state, summary_prefix="emergency_")

        if self._should_deposit_resources(state):
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_resources")

        if not _h.has_role_gear(state, role):
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_afford_gear(state, role):
                return self._miner_action(state, summary_prefix=f"fund_{role}_gear_")
            return self._acquire_role_gear(state, role)

        if role == "aligner":
            return self._aligner_action(state)
        elif role == "scrambler":
            return self._scrambler_action(state)
        else:
            return self._miner_action(state)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting with idle-mine fallback."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier — mine to help economy instead of wandering
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """V65-style aggressive budgets, adapted for variable team sizes.

        V65 original: pressure=4 from step 0, ramp to 5 when rich, scrambler at 1500+.
        This adapts that for smaller teams while keeping the same philosophy:
        get aligners out ASAP, don't delay for economy.
        """
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents
        min_res = _h.team_min_resource(state)

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # 2 agents: mine first to build economy, then 1 aligner
            if step < 200 or (min_res < 7 and not _h.team_can_refill_hearts(state)):
                return 0, 0  # Both mine
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            # 4 agents: 2 aligners + 2 miners (v65 ratio: half align, half mine)
            pressure = min(2, num_agents - 1)
            if step >= 40 and min_res >= 14:
                pressure = min(3, num_agents - 1)
            scrambler = 1 if step >= 1500 else 0
            aligner = max(pressure - scrambler, 1)
            if objective == "economy_bootstrap":
                return min(aligner, 2), 0
            return aligner, scrambler

        # 5+ agents: v65 original budgets
        pressure = 4
        if step >= 40 and min_res >= 20:
            pressure = 5

        scrambler = 0
        if step >= 1500:
            scrambler = 1
        if step >= 5000 and _h.team_can_refill_hearts(state):
            scrambler = 2
        aligner = pressure - scrambler
        if objective == "economy_bootstrap":
            return min(aligner, 2), 0
        return aligner, scrambler

    def evaluate_state(self, state: MettagridState) -> Action:
        action = super().evaluate_state(state)
        step = state.step or self._step_index
        if step % 500 == 0 or step == 1:
            pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hearts = int(state.self_state.inventory.get("heart", 0))
            cargo = _h.resource_total(state)
            role = self._infos.get("role", "?")
            subtask = self._infos.get("subtask", "?")
            aligner_b = self._infos.get("aligner_budget", "?")
            scrambler_b = self._infos.get("scrambler_budget", "?")
            team_id = _h.team_id(state)
            friendly = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner == team_id))
            print(
                f"[COG] step={step} agent={self._agent_id} pos={pos} role={role} "
                f"subtask={subtask} hp={hp} hearts={hearts} cargo={cargo} "
                f"aligners={aligner_b} scramblers={scrambler_b} friendly_j={friendly}",
                flush=True,
            )
        return action

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaV65HybridPolicy(MettagridSemanticPolicy):
    """V65 hybrid: v65 budgets + team-aware roles + idle-mine."""
    short_names = ["alpha-v65-hybrid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65HybridAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaV65HybridGlobalPolicy(MettagridSemanticPolicy):
    """V65 hybrid with global roles (no team-relative) — closer to original v65."""
    short_names = ["alpha-v65-hybrid-global"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65HybridAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                # No shared_team_ids → global role priorities like v65
            )
        return self._agent_policies[agent_id]


class AlphaWaveAgentPolicy(AlphaV65HybridAgentPolicy):
    """Wave strategy: economy-first, then mass alignment.

    Phase 1 (step 0-199): ALL mine — build massive resource stockpile
    Phase 2 (step 200-499): 6 aligners + 2 miners — rapid territory grab
    Phase 3 (step 500+): 5 aligners + 1 scrambler + 2 miners — hold territory
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 100:
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 150:
                return 0, 0  # All mine
            if step < 400:
                return min(3, num_agents - 1), 0  # Mass align
            return min(2, num_agents - 1), 1  # Hold + scramble

        # 5+ agents
        if step < 150:
            return 0, 0  # Phase 1: ALL MINE
        if step < 500:
            return min(6, num_agents - 2), 0  # Phase 2: MASS ALIGN
        # Phase 3: Hold + scramble
        scrambler = min(2, num_agents // 4)
        aligner = min(5, num_agents - 2 - scrambler)
        if objective == "economy_bootstrap":
            return min(aligner, 2), 0
        return aligner, scrambler


class AlphaWavePolicy(MettagridSemanticPolicy):
    """Wave strategy: economy-first, then mass alignment."""
    short_names = ["alpha-wave"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaWaveAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaMaxPressureAgentPolicy(AlphaV65HybridAgentPolicy):
    """Maximum pressure from step 0: 6 aligners + 2 miners.

    Skip economy buildup entirely. The starting resources (24 each)
    give 3 hearts. Use them immediately and mine to sustain.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            return 1, 0

        if num_agents <= 4:
            aligner = num_agents - 1  # All but 1 align
            scrambler = 1 if step >= 1000 else 0
            aligner = max(aligner - scrambler, 1)
            return aligner, scrambler

        # 5+ agents: maximum alignment pressure
        aligner = max(num_agents - 2, 4)  # 6 aligners for 8 agents
        scrambler = 0
        if step >= 2000:
            scrambler = 1
            aligner -= 1
        if objective == "economy_bootstrap":
            return min(aligner, 3), 0
        return aligner, scrambler


class AlphaMaxPressurePolicy(MettagridSemanticPolicy):
    """Maximum pressure: 6 aligners from step 0."""
    short_names = ["alpha-max-pressure"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMaxPressureAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaFlashRushAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """Flash Rush: all-mine first 50 steps, then mass-align with 6 aligners + 1 scrambler.

    Theory: front-load economy to sustain maximum territorial pressure.
    Instead of gradual scaling, we burst with maximum force after building reserves.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 50 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 50:
                return 0, 0  # All mine
            if min_res < 3 and not can_hearts:
                return 1, 0
            return min(3, num_agents - 1), 0

        # 5+ agents: ALL mine for 50 steps, then flash rush
        if step < 50:
            return 0, 0  # ALL mine — build economy reserves

        # After step 50: maximum pressure
        if min_res < 3 and not can_hearts:
            # Economy emergency — pull back
            return 2, 0

        # Normal: 6 aligners + 1 scrambler (1 miner)
        if step < 200:
            # Pure alignment rush — no scramblers yet
            aligner_budget = min(num_agents - 1, 7)  # Leave just 1 miner
            if objective == "economy_bootstrap":
                return min(aligner_budget, 3), 0
            return aligner_budget, 0

        # After step 200: add scrambler
        scrambler_budget = 1
        aligner_budget = min(num_agents - 2, 6)  # 6 aligners + 1 scrambler + 1 miner
        if step >= 3000:
            scrambler_budget = 2
            aligner_budget = min(num_agents - 3, 5)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 3), 0
        return aligner_budget, scrambler_budget

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting with idle-explore (avoid gear churn)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier — explore to find new junctions (avoid gear churn)
        return self._explore_action(state, role="aligner", summary="idle_explore")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaFlashRushPolicy(MettagridSemanticPolicy):
    """Flash Rush: all-mine 50 steps, then mass-align."""
    short_names = ["alpha-flash-rush"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaFlashRushAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaEconDominanceAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """Economic Dominance: keep 5 miners throughout, but use 2 very efficient aligners.

    Theory: with massive economy, aligners always have hearts and can work non-stop.
    Fewer aligners but they never idle waiting for hearts.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 100:
                return 0, 0  # Both mine
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 0, 0  # All mine first
            return 1, 1 if step >= 500 else 0  # Just 1 aligner + maybe 1 scrambler

        # 5+ agents: economy-heavy — only 2 aligners, 1 scrambler, 5 miners
        if step < 100:
            return 1, 0  # 1 aligner to start exploring while miners build economy
        if step < 500:
            return 2, 0  # 2 aligners, 6 miners

        # After 500: 2 aligners + 1 scrambler + 5 miners
        scrambler_budget = 1
        if min_res < 3 and not can_hearts:
            return 1, 0  # Economy crisis
        if objective == "economy_bootstrap":
            return 1, 0
        return 2, scrambler_budget

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Idle-explore for aligners."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        return self._explore_action(state, role="aligner", summary="idle_explore")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaEconDominancePolicy(MettagridSemanticPolicy):
    """Economic Dominance: heavy mining, few but efficient aligners."""
    short_names = ["alpha-econ-dominance"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconDominanceAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaScrambleDominanceAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """Scramble Dominance: 4-5 scramblers + 2 aligners + 1-2 miners.

    Theory: overwhelm enemy with scramblers to keep them at 0 junctions.
    Even 3-4 friendly junctions = huge score advantage if enemy holds 0.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            return 0, 1 if step >= 200 else (1, 0)

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            return 1, min(2, num_agents - 2)

        # 5+ agents: scramble-heavy
        if step < 50:
            return 2, 0  # Quick territory grab
        if step < 200:
            return 2, 2  # 2 aligners + 2 scramblers + 4 miners

        if min_res < 3 and not can_hearts:
            return 1, 1

        # 2 aligners + 4 scramblers + 2 miners
        scrambler_budget = min(4, num_agents - 3)
        aligner_budget = 2
        if objective == "economy_bootstrap":
            return 1, 1
        return aligner_budget, scrambler_budget

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Idle-explore for aligners."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        return self._explore_action(state, role="aligner", summary="idle_explore")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaScrambleDominancePolicy(MettagridSemanticPolicy):
    """Scramble Dominance: overwhelm with scramblers, hold few junctions."""
    short_names = ["alpha-scramble-dominance"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScrambleDominanceAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaHybridAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """Hybrid: v65 conservative targeting + re-alignment boost + idle-mine + AT budgets.

    Combines:
    - v65 hub_penalty targeting (conservative, proven in tournament)
    - Re-alignment boost from AlphaCyborg (hotspot flip)
    - Idle-mine for economy boost when no junctions to align
    - AlphaTournament's economy-first budgets
    - Retreat margin 20 (conservative for PvP)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = 8.0  # Re-alignment boost (flip hotspot to bonus)

    def _junction_hotspot_count(self, entity: KnownEntity, hub: KnownEntity | None) -> int:
        """Flip hotspot count to BONUS — prioritize re-aligning scrambled junctions."""
        if hub is None:
            return 0
        rel = (entity.global_x - hub.global_x, entity.global_y - hub.global_y)
        count = self._shared_hotspots.get(rel, 0)
        return -min(count, 3)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting + idle-mine (when no frontier junctions, mine for economy)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # No frontier — mine for economy (idle-mine >> idle-explore)
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """AlphaTournament economy-first budgets."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 30 or (min_res < 1 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 30:
            pressure_budget = 2
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = 5
            if min_res < 3 and not can_hearts:
                pressure_budget = 2
            elif min_res < 7:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 3 and not can_hearts:
                pressure_budget = 2

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = 2
        elif step >= 100:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)


class AlphaHybridPolicy(MettagridSemanticPolicy):
    """Hybrid: v65 targeting + re-alignment boost + idle-mine + AT budgets."""
    short_names = ["alpha-hybrid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaHybridAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# Re-export the ORIGINAL pre-rewrite semantic_cog for pure v65 testing
from cvc.cogent.player_cog.policy.semantic_cog_v65 import (
    MettagridSemanticPolicy as _V65BasePolicy,
)


class AlphaV65PurePolicy(_V65BasePolicy):
    """Pure v65: uses the ORIGINAL semantic_cog.py from before any modifications.

    This bypasses ALL changes made in the rewrite (b882dbf) and later commits.
    If this matches v65's 3.59, the rewrite itself is the problem.
    """
    short_names = ["alpha-v65-pure"]


class AlphaSmallTeamAgentPolicy(AlphaCogAgentPolicy):
    """Optimized for tournament's 4-agent games with 1-3 agent team sizes.

    Key changes from AlphaCog:
    - 1-2 agents: Rush alignment immediately (hub has 5 free hearts)
    - No mining phase for tiny teams — hearts are free, align first
    - After hearts spent, mine to replenish then align again
    - Better economy: mine least resource specifically
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Small-team-optimized budgets: align first, mine later.

        KEY INSIGHT: Hub starts with 5 hearts regardless of team size.
        Small teams should rush alignment with free hearts before mining.
        state.team_summary may be None early (hub not yet observed), so
        we assume starting resources are available.
        """
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents

        # Check actual hub state, but assume positive for early steps
        has_team_summary = state.team_summary is not None
        min_res = _h.team_min_resource(state) if has_team_summary else (num_agents * 3)
        can_hearts = _h.team_can_refill_hearts(state) if has_team_summary else True

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 1:
            # Solo agent: ALWAYS be aligner (mine as idle activity when no hearts)
            # Hub has 5 free hearts at start — use them immediately
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0  # Always try to align

        if num_agents <= 2:
            # 2 agents: 1 aligner from step 0 (free hearts!)
            if objective == "economy_bootstrap" and not can_hearts:
                return 0, 0
            return 1, 0  # One aligns, one mines

        if num_agents <= 4:
            # 3-4 agents: aggressive alignment from start
            if step < 50:
                return min(2, num_agents - 1), 0  # Align with free hearts
            if min_res < 3 and not can_hearts:
                return 1, 0  # Keep 1 aligner, rest mine
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: use parent's logic
        return super()._pressure_budgets(state, objective=objective)


class AlphaSmallTeamPolicy(MettagridSemanticPolicy):
    """Tournament-optimized policy for small team games (1-4 agents)."""
    short_names = ["alpha-small-team"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSmallTeamAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaExpanderAgentPolicy(AlphaCogAgentPolicy):
    """Better network expansion when frontier stalls (frontier=0).

    Key change: more aggressive expansion toward unreachable junctions.
    When no frontier exists, walk toward unreachable junctions to bring them
    into alignment range, rather than idle-mining.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Extended aligner with aggressive expansion when frontier stalls."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # AGGRESSIVE EXPANSION: walk toward unreachable junctions to bring them into range
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub_pos = hub.position if hub is not None else current_pos
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable and hp > 20:
            # Find the closest unreachable junction that's between us and the frontier edge
            # More aggressive: allow expansion up to 50 tiles from hub (was 40)
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20  # Less safety margin (was 30)
                and _h.manhattan(hub_pos, j.position) < 50  # Wider range (was 40)
            ]
            if safe_unreachable:
                # Prefer junctions that are closest to an existing friendly junction
                # (most likely to become alignable when we walk near them)
                team_id = _h.team_id(state)
                friendly = self._known_junctions(state, predicate=lambda j: j.owner == team_id)
                if friendly:
                    nearest = min(safe_unreachable, key=lambda j: min(
                        _h.manhattan(j.position, f.position) for f in friendly
                    ))
                else:
                    nearest = min(safe_unreachable, key=lambda j: _h.manhattan(current_pos, j.position))
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

            # If no safe targets, try the nearest unreachable anyway if HP allows
            nearest = min(unreachable, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_risky", vibe="change_vibe_aligner")

        # Fallback: mine while waiting
        return self._miner_action(state, summary_prefix="idle_align_")


class AlphaExpanderPolicy(MettagridSemanticPolicy):
    """AlphaCyborg + aggressive network expansion."""
    short_names = ["alpha-expander"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaExpanderAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptivePolicy(MettagridSemanticPolicy):
    """Adaptive policy: AlphaCyborg for small teams, Expander for large teams.

    Tournament is 75% 4-agent (where AlphaCyborg excels) and 25% 8-agent
    (where Expander excels). This policy adapts to team size for best overall.
    """
    short_names = ["alpha-adaptive"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        num_agents = self.policy_env_info.num_agents
        if agent_id not in self._agent_policies:
            # Use Expander for 5+ agents, AlphaCog for small teams
            policy_cls = AlphaExpanderAgentPolicy if num_agents >= 5 else AlphaCogAgentPolicy
            self._agent_policies[agent_id] = policy_cls(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaEconFixAgentPolicy(AlphaCogAgentPolicy):
    """Fix miner resource-switching: clear sticky target when resource priority shifts.

    Key insight: miners get locked to wrong resources via sticky targets even when
    a critical resource (like carbon) hits 0. This wastes mining capacity.

    Also uses v65-style budgets (4 aligners early) with earlier scramblers.
    Disables hotspot weight (hurts in tournament per prior testing).
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._last_bias_resource: str | None = None
        # Disable hotspot weight — confirmed to hurt tournament performance
        self._hotspot_weight = 0.0

    def _preferred_miner_extractor(self, state: MettagridState) -> KnownEntity | None:
        """Override: clear sticky target when resource priority changes significantly."""
        resources = _shared_resources(state)
        least = _least_resource(resources)
        least_amount = resources[least]

        if (
            self._sticky_target_kind is not None
            and self._sticky_target_kind.endswith("_extractor")
        ):
            current_resource = self._sticky_target_kind.removesuffix("_extractor")
            if current_resource != least:
                # Critical shortage: switch immediately
                if least_amount < 7:
                    self._clear_sticky_target()
                # Resource imbalance: mining the most abundant while least is < 50% of max
                elif least_amount > 0:
                    max_amount = max(resources.values())
                    if resources[current_resource] > max_amount * 0.8 and least_amount < max_amount * 0.5:
                        self._clear_sticky_target()

        # Also clear if bias changed (different resource became most needed)
        if self._last_bias_resource is not None and self._last_bias_resource != self._resource_bias:
            if least_amount < 14:
                self._clear_sticky_target()
        self._last_bias_resource = self._resource_bias

        return super()._preferred_miner_extractor(state)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """V65-style aggressive budgets: 4 aligners from the start.

        The key insight from tournament data: v65's aggressive alignment wins territory
        early, which compounds over the game. Economy-first approaches lose territory
        that's hard to recapture.
        """
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            # 4-agent: 2 aligners + 2 miners (balanced)
            if step < 30:
                return 1, 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 300 and min_res >= 7 else 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: v65-aggressive — territory wins compound
        if step < 30:
            pressure_budget = 2
        else:
            pressure_budget = min(4, num_agents - 2)  # 4 aligners like v65
            if min_res < 1 and not can_hearts:
                pressure_budget = 2
            elif min_res < 3:
                pressure_budget = 3

        # Scramblers: earlier than v65 (step 100 not 1500) for disruption
        scrambler_budget = 0
        if step >= 100 and min_res >= 7:
            scrambler_budget = 1
        if step >= 2000 and min_res >= 14:
            scrambler_budget = 2
        aligner_budget = max(pressure_budget - scrambler_budget, 0)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaLateGameAgentPolicy(AlphaEconFixAgentPolicy):
    """EconFix + late-game sustainability.

    Key changes for 10k step games:
    - After step 5000, shift to more scramblers (disrupt clips)
    - Resource-gated budget: fewer aligners when economy is weak
    - More conservative with hearts in late game
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 30:
                return 1, 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 300 and min_res >= 7 else 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: phase-based strategy
        if step < 30:
            pressure_budget = 2
        elif step < 5000:
            # Early/mid: aggressive alignment
            pressure_budget = min(4, num_agents - 2)
            if min_res < 1 and not can_hearts:
                pressure_budget = 2
            elif min_res < 3:
                pressure_budget = 3
        else:
            # Late game (5000+): shift to defense/economy
            pressure_budget = min(4, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = 2  # Emergency: mass mine
            elif min_res < 7:
                pressure_budget = 3  # Reduced pressure, more mining

        # Scramblers: ramp up in late game to disrupt clips
        scrambler_budget = 0
        if step >= 5000 and min_res >= 7:
            scrambler_budget = min(2, pressure_budget // 2)  # 2 scramblers in late game
        elif step >= 100 and min_res >= 7:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaLateGamePolicy(MettagridSemanticPolicy):
    """EconFix + late-game sustainability + team-relative roles."""
    short_names = ["alpha-late-game"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaLateGameAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaEconFixPolicy(MettagridSemanticPolicy):
    """Economy fix: resource-aware miners + v65 aggressive budgets + team-relative roles."""
    short_names = ["alpha-econ-fix"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconFixAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaEconFixGlobalPolicy(MettagridSemanticPolicy):
    """Economy fix + global role assignment (like v65)."""
    short_names = ["alpha-econ-fix-global"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # No shared_team_ids — match v65's global role assignment
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconFixAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


class AlphaV65PlusFixAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 exact behavior + resource fix + idle-mine.

    Minimal changes to v65 that are known to help:
    1. Resource-aware miner switching (fix carbon bottleneck)
    2. Idle-mine for aligners when no frontier (biggest local improvement)
    3. V65 budgets and targeting (proven in tournament)
    4. No hotspot/network weights (v65 had 0)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._last_bias_resource: str | None = None
        # Match v65: no hotspot/network weights
        self._hotspot_weight = 0.0
        self._network_weight = 0.0

    def _preferred_miner_extractor(self, state: MettagridState) -> KnownEntity | None:
        """Clear sticky target when resource priority changes significantly."""
        resources = _shared_resources(state)
        least = _least_resource(resources)
        least_amount = resources[least]

        if (
            self._sticky_target_kind is not None
            and self._sticky_target_kind.endswith("_extractor")
        ):
            current_resource = self._sticky_target_kind.removesuffix("_extractor")
            if current_resource != least:
                if least_amount < 7:
                    self._clear_sticky_target()
                elif least_amount > 0:
                    max_amount = max(resources.values())
                    if resources[current_resource] > max_amount * 0.8 and least_amount < max_amount * 0.5:
                        self._clear_sticky_target()

        if self._last_bias_resource is not None and self._last_bias_resource != self._resource_bias:
            if least_amount < 14:
                self._clear_sticky_target()
        self._last_bias_resource = self._resource_bias

        return super()._preferred_miner_extractor(state)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting with idle-mine: mine when no frontier junctions available."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Idle-mine instead of explore (proven +89% local improvement)
        return self._miner_action(state, summary_prefix="idle_mine_")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """V65 original budgets: 4 aligners from step 0, scrambler at 1500."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)

        if objective == "resource_coverage":
            return 0, 0

        pressure_budget = 4
        if step >= 40 and min_res >= 20:
            pressure_budget = 5

        scrambler_budget = 0
        if step >= 1_500:
            scrambler_budget = 1
        aligner_budget = pressure_budget - scrambler_budget

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def evaluate_state(self, state: MettagridState) -> Action:
        action = super().evaluate_state(state)
        step = state.step or self._step_index
        role = self._infos.get("role", "?")
        subtask = self._infos.get("subtask", "?")
        if step % 500 == 0 or step == 1 or (step % 100 == 0 and self._agent_id == 0):
            pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hearts = int(state.self_state.inventory.get("heart", 0))
            cargo = _h.resource_total(state)
            aligner_b = self._infos.get("aligner_budget", "?")
            scrambler_b = self._infos.get("scrambler_budget", "?")
            frontier = self._infos.get("frontier_neutral_junctions", "?")
            team_id = _h.team_id(state)
            friendly = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner == team_id))
            enemy = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner not in {None, "neutral", team_id}))
            shared = _shared_resources(state)
            hub = self._nearest_hub(state)
            hub_pos = (hub.global_x, hub.global_y) if hub else None
            print(
                f"[COG] step={step} agent={self._agent_id} pos={pos} hub={hub_pos} role={role} "
                f"subtask={subtask} hp={hp} hearts={hearts} cargo={cargo} "
                f"aligners={aligner_b} scramblers={scrambler_b} "
                f"friendly_j={friendly} enemy_j={enemy} frontier={frontier} "
                f"hub_res={shared}",
                flush=True,
            )
        return action


class AlphaV65PlusFixPolicy(MettagridSemanticPolicy):
    """V65 + resource fix + idle-mine. Global role assignment."""
    short_names = ["alpha-v65-plus-fix"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        # No shared_team_ids — match v65's global role assignment
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaV65PlusFixAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaAlignMax: maximize aligner count, minimize scramblers.
# Hypothesis: extra aligner > extra scrambler since alignment rate is the
# bottleneck (each alignment = 0.03/tick, each scramble = 0.01/tick).
# ---------------------------------------------------------------------------

class AlphaAlignMaxAgentPolicy(AlphaCogAgentPolicy):
    """More aligners, fewer scramblers. Aggressive re-alignment."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            # 4 agents: 3 aligners, 0 scramblers, 1 miner
            aligner_budget = min(3, num_agents - 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: maximize aligners
        if step < 30:
            return 2, 0
        elif step < 100:
            return 3, 0
        elif step < 500:
            # Ramp up: 4 aligners, 0 scramblers, rest mine
            pressure_budget = min(4, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            return pressure_budget, 0
        elif step < 5000:
            # Peak: 5 aligners, 1 scrambler for clips defense
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(5, num_agents - 2)
            scrambler_budget = 1 if min_res >= 7 else 0
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget
        else:
            # Late game: 4 aligners, 2 scramblers
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            scrambler_budget = min(2, pressure_budget // 3)
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget


# ---------------------------------------------------------------------------
# AlphaOptimal: V65 hub-centric targeting + team-aware budgets + idle-mine +
# resource bias + re-alignment bonus. Best of all worlds.
# ---------------------------------------------------------------------------

class AlphaOptimalAgentPolicy(AlphaV65TrueReplicaAgentPolicy):
    """V65 targeting (hub_penalty) + AlphaCog improvements (idle-mine, resource bias, team budgets)."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._last_budget_change_step = 0
        num_agents = self.policy_env_info.num_agents
        self._current_aligner_budget = min(4, max(num_agents - 1, 1))

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V65 targeting with idle-mine (proven 89% better than idle-explore)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")
        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Idle-mine: proven better than idle-explore (8.05 vs 3.54 in A/B tests)
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Team-size-aware budgets (critical for 2v6/6v2 tournament matches)."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: v65-like budgets with team awareness
        if step < 30:
            return 2, 0
        elif step < 3000:
            pressure_budget = 5
            if min_res < 1 and not can_hearts:
                pressure_budget = 2
            elif min_res < 3:
                pressure_budget = 4
        else:
            pressure_budget = 6
            if min_res < 1 and not can_hearts:
                pressure_budget = 3

        scrambler_budget = 0
        if step >= 3000:
            scrambler_budget = 2
        elif step >= 100:
            scrambler_budget = 1
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        least = _least_resource(resources)
        return MacroDirective(resource_bias=least)

    def evaluate_state(self, state: MettagridState) -> Action:
        action = super().evaluate_state(state)
        step = state.step or self._step_index
        role = self._infos.get("role", "?")
        subtask = self._infos.get("subtask", "?")
        if step % 500 == 0 or step == 1 or (step % 100 == 0 and self._agent_id == 0):
            pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hearts = int(state.self_state.inventory.get("heart", 0))
            cargo = _h.resource_total(state)
            aligner_b = self._infos.get("aligner_budget", "?")
            scrambler_b = self._infos.get("scrambler_budget", "?")
            frontier = self._infos.get("frontier_neutral_junctions", "?")
            team_id = _h.team_id(state)
            friendly = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner == team_id))
            enemy = len(self._world_model.entities(
                entity_type="junction", predicate=lambda e: e.owner not in {None, "neutral", team_id}))
            shared = _shared_resources(state)
            hub = self._nearest_hub(state)
            hub_pos = (hub.global_x, hub.global_y) if hub else None
            print(
                f"[COG] step={step} agent={self._agent_id} pos={pos} hub={hub_pos} role={role} "
                f"subtask={subtask} hp={hp} hearts={hearts} cargo={cargo} "
                f"aligners={aligner_b} scramblers={scrambler_b} "
                f"friendly_j={friendly} enemy_j={enemy} frontier={frontier} "
                f"hub_res={shared}",
                flush=True,
            )
        return action


class AlphaOptimalPolicy(MettagridSemanticPolicy):
    """V65 targeting + idle-mine + team-aware budgets + resource bias."""
    short_names = ["alpha-optimal"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaOptimalAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAlignMaxPolicy(MettagridSemanticPolicy):
    """Maximum aligners, minimal scramblers."""
    short_names = ["alpha-align-max"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAlignMaxAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaDefenseWall: Heavy scramblers to suppress clips early, then shift to alignment.
# Counter-hypothesis: scramblers reduce clips pressure, freeing aligners.
# ---------------------------------------------------------------------------

class AlphaDefenseWallAgentPolicy(AlphaCogAgentPolicy):
    """Early scramblers to fight clips, then ramp up aligners."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if num_agents >= 4 and step >= 200 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: early scramblers
        if step < 30:
            return 2, 0
        elif step < 200:
            return 3, 0  # Economy first
        elif step < 500:
            # 3 aligners + 2 scramblers: aggressively fight clips
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            scrambler_budget = min(2, pressure_budget // 2)
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget
        else:
            # Settled: 4 aligners, 2 scramblers
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            scrambler_budget = min(2, pressure_budget // 3)
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget


class AlphaDefenseWallPolicy(MettagridSemanticPolicy):
    """Heavy early scramblers to suppress clips."""
    short_names = ["alpha-defense-wall"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaDefenseWallAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaBlitz: All-in on alignment in early game (all agents align with free hearts),
# then transition to normal economy.
# ---------------------------------------------------------------------------

class AlphaBlitzAgentPolicy(AlphaCogAgentPolicy):
    """Early blitz: all agents try to align using the 5 free hearts, then economy."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        # Early blitz: ALL agents try to align with free hearts
        # Hub starts with 5 hearts. Use them before mining.
        if step < 50:
            # All aligners to use free hearts (hub starts with 5 hearts)
            return min(num_agents, 5), 0  # Up to 5 aligners (1 heart each)

        if num_agents <= 4:
            if step < 200:
                return min(2, num_agents - 1), 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if num_agents >= 4 and step >= 500 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: standard after blitz
        if step < 100:
            return 3, 0
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
            scrambler_budget = 1 if step >= 200 and min_res >= 7 else 0
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            scrambler_budget = min(2, pressure_budget // 3)
            aligner_budget = max(pressure_budget - scrambler_budget, 0)
            if objective == "economy_bootstrap":
                return min(aligner_budget, 2), 0
            return aligner_budget, scrambler_budget


class AlphaBlitzPolicy(MettagridSemanticPolicy):
    """Early blitz alignment with free hearts."""
    short_names = ["alpha-blitz"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaBlitzAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSustainAgentPolicy(AlphaCogAgentPolicy):
    """Sustain variant: maximize score per heart by conserving silicon.

    Key changes from AlphaCog:
    1. Silicon-first mining: always bias toward silicon (scarcest extractor, 45 vs 50-58).
    2. No scrambling at all: save every heart for alignment.
    3. Late-game conservation: when silicon < 14, reduce aligners to prevent heart waste.
    4. No early heart skip: go align immediately in first 200 steps.
    5. Smarter expansion: prefer junction clusters (more junctions unlocked per heart).
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Always bias toward silicon — the bottleneck resource."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        others_min = min(resources[r] for r in _ELEMENTS if r != "silicon")
        if silicon > others_min * 2 and others_min < 7:
            least = _least_resource(resources)
            return MacroDirective(resource_bias=least)
        return MacroDirective(resource_bias="silicon")

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner that never scrambles — all hearts go to alignment."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Early game: go align immediately (no batching for first 200 steps)
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions — prefer clusters
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable

            def cluster_score(j):
                nearby = sum(
                    1 for other in unreachable
                    if other is not j and _h.manhattan(j.position, other.position) <= 15
                )
                dist = _h.manhattan(current_pos, j.position)
                return dist - nearby * 5
            best = min(targets, key=cluster_score)
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_cluster_junction", vibe="change_vibe_aligner")

        # No expansion target — help economy by mining
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """No scramblers ever. Late-game conservation when silicon depletes."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)

        if objective == "resource_coverage":
            return 0, 0

        # Late-game conservation: when silicon critically low, reduce pressure
        if silicon < 14 and step > 2000:
            if num_agents <= 2:
                return 0, 0
            if num_agents <= 4:
                return 1, 0
            return 2, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0  # Never scramble

        # 5+ agents
        if step < 30:
            return 2, 0
        if step < 100:
            return 3, 0

        if min_res < 3 and not can_hearts:
            pressure = max(2, num_agents // 3)
        elif min_res < 7:
            pressure = min(4, num_agents - 2)
        else:
            pressure = min(5, num_agents - 2)

        if objective == "economy_bootstrap":
            return min(2, pressure), 0
        return pressure, 0  # Never scramble


class AlphaAdaptiveV2AgentPolicy(AlphaCogAgentPolicy):
    """Adaptive v2: aggressive early, sustain late. Best of both worlds.

    Phase 1 (step 0-2500): AlphaAggressive behavior — fast alignment, idle scramble
    Phase 2 (step 2500+ or silicon < 30): Conservation — no scramble, silicon mining, fewer aligners
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-first bias when silicon is the bottleneck."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        step = state.step or self._step_index
        # After step 1500 or when silicon is low, bias toward silicon
        if step > 1500 or silicon < 30:
            others_min = min(resources[r] for r in _ELEMENTS if r != "silicon")
            if silicon > others_min * 2 and others_min < 7:
                return MacroDirective(resource_bias=_least_resource(resources))
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=_least_resource(resources))

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: scramble in early game, pure alignment in late game."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Early game: go align immediately
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junction clusters
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable

            def cluster_score(j):
                nearby = sum(
                    1 for other in unreachable
                    if other is not j and _h.manhattan(j.position, other.position) <= 15
                )
                dist = _h.manhattan(current_pos, j.position)
                return dist - nearby * 5
            best = min(targets, key=cluster_score)
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_cluster_junction", vibe="change_vibe_aligner")

        # Early game: idle-scramble when economy healthy
        in_conservation = step > 2500 or silicon < 30
        min_res = _h.team_min_resource(state)
        if not in_conservation and hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 14:
            return self._miner_action(state, summary_prefix="idle_align_")

        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive early, conservative late. No dedicated scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)

        if objective == "resource_coverage":
            return 0, 0

        in_conservation = step > 2500 or silicon < 30

        # Conservation mode: fewer aligners, no scramblers
        if in_conservation and silicon < 14:
            if num_agents <= 2:
                return 0, 0
            if num_agents <= 4:
                return 1, 0
            return 2, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            # Early game: allow 1 scrambler with surplus
            scrambler_budget = 0
            if not in_conservation and min_res >= 200 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 30:
            return 2, 0
        if step < 100:
            return 3, 0

        economy_surplus = min_res >= 100

        if economy_surplus and not in_conservation:
            pressure = min(num_agents - 1, 7)
        elif min_res < 3 and not can_hearts:
            pressure = max(2, num_agents // 3)
        elif min_res < 7:
            pressure = min(4, num_agents - 2)
        else:
            pressure = min(5, num_agents - 2)

        # Scramblers only in early/aggressive phase
        scrambler_budget = 0
        if not in_conservation:
            if step >= 200 and min_res >= 7:
                scrambler_budget = min(1, pressure // 3)

        aligner_budget = max(pressure - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaAdaptiveV2Policy(MettagridSemanticPolicy):
    """Adaptive v2: aggressive early, sustain late. Silicon-first bias."""
    short_names = ["alpha-adaptive-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSustainPolicy(MettagridSemanticPolicy):
    """Sustain scoring: silicon-first mining, no scrambling, late-game conservation."""
    short_names = ["alpha-sustain"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSustainAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# Wider explore offsets for full map coverage (88x88 map)
_WIDE_ALIGNER_EXPLORE_OFFSETS = (
    (0, -35),
    (25, -25),
    (35, 0),
    (25, 25),
    (0, 35),
    (-25, 25),
    (-35, 0),
    (-25, -25),
    (0, -15),
    (15, 0),
    (0, 15),
    (-15, 0),
)


class AlphaExplorerAgentPolicy(AlphaAggressiveAgentPolicy):
    """Explorer variant: better junction discovery through wider exploration.

    Key changes from AlphaAggressive:
    1. Wider explore offsets (35 vs 22) to cover full 88x88 map.
    2. No hub distance limit on expansion (was 40, now unlimited).
    3. Idle aligners explore before mining — discover junctions is priority.
    4. Bridge expansion: prefer junctions that extend network toward unexplored areas.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner with aggressive exploration and no hub-distance limits."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Early game: go align immediately (no batching for first 200 steps)
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions — NO hub distance limit
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            # Prefer junctions that bridge to more unreachable junctions
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15  # Less conservative
            ]
            targets = safe_unreachable if safe_unreachable else unreachable

            def bridge_score(j):
                # How many other unreachable junctions would become reachable
                # if we aligned this one (within alignment distance 15)?
                bridged = sum(
                    1 for other in unreachable
                    if other is not j and _h.manhattan(j.position, other.position) <= 15
                )
                dist = _h.manhattan(current_pos, j.position)
                return dist - bridged * 8  # Strong bonus for bridging
            best = min(targets, key=bridge_score)
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 15:
                return self._move_to_known(state, best, summary="expand_bridge_junction", vibe="change_vibe_aligner")

        # Idle aligners: scramble if economy healthy
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Explore with wider offsets to discover distant junctions
        return self._wide_explore_action(state, role="aligner", summary="wide_explore_junctions")

    def _wide_explore_action(self, state: MettagridState, *, role: str, summary: str) -> tuple[Action, str]:
        """Explore with wider offsets to cover the full map."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _WIDE_ALIGNER_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary=summary, vibe=_h.role_vibe(role))


class AlphaExplorerPolicy(MettagridSemanticPolicy):
    """Explorer: wider exploration, no hub-distance limits, bridge expansion."""
    short_names = ["alpha-explorer"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaExplorerAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaBalancedAgentPolicy(AlphaAggressiveAgentPolicy):
    """Economy-balanced variant: sustains aligners through the whole game.

    Key insight: AlphaAggressive collapses after step 2000 because 6 aligners
    consume hearts faster than 1-2 miners can produce silicon. This policy:
    1. Keeps 3-4 miners always (never fewer than 3 for 8-agent games)
    2. Limits aligners to 3 early, 4 mid-game, based on silicon reserves
    3. No scrambling until economy stable (min_res >= 50)
    4. Silicon-priority mining when silicon is scarce
    5. Earlier economy pullback (min_res < 30 triggers miner shift)
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner that only idle-scrambles when economy is very healthy."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Early game: don't batch, go align immediately
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: ONLY scramble if economy very healthy, otherwise mine to sustain economy
        min_res = _h.team_min_resource(state)
        if int(state.self_state.inventory.get("heart", 0)) > 0 and min_res >= 50:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Always mine when idle — economy sustain is critical
        return self._miner_action(state, summary_prefix="idle_align_")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining when silicon is scarce."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        # If silicon is within 20 of the lowest resource, prioritize silicon
        least_amount = resources[least]
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Conservative budgets: sustain economy through the whole game."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # 4 agents: max 2 aligners, keep 2 miners always
            aligner_budget = 2
            scrambler_budget = 0
            if min_res < 30:
                aligner_budget = 1  # Economy struggling: pull back to 1 aligner
            return aligner_budget, scrambler_budget

        # 5+ agents: always keep at least 3-4 miners for sustainable economy
        min_miners = max(3, num_agents // 2 - 1)  # 3 miners for 8 agents
        max_pressure = num_agents - min_miners

        if step < 30:
            return 2, 0

        # Economy-gated aligner count
        if min_res < 10 and not can_hearts:
            # Crisis: almost all mine
            return 1, 0
        elif min_res < 30:
            # Struggling: limit to 2 aligners
            return min(2, max_pressure), 0
        elif min_res < 50:
            # Moderate: 3 aligners
            return min(3, max_pressure), 0
        else:
            # Healthy: full pressure but capped
            aligner_budget = min(4, max_pressure)
            # Only scramble when very healthy
            scrambler_budget = 0
            if step >= 500 and min_res >= 80:
                scrambler_budget = 1
                aligner_budget = min(3, max_pressure - 1)
            return aligner_budget, scrambler_budget


class AlphaBalancedPolicy(MettagridSemanticPolicy):
    """Economy-balanced: sustains aligners through whole game, silicon-priority mining."""
    short_names = ["alpha-balanced"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaBalancedAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveV3AgentPolicy(AlphaBalancedAgentPolicy):
    """Adaptive economy v3: ramps aligners when resources permit.

    Fixes AlphaBalanced's over-hoarding (1000+ resources with only 3 aligners).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Dynamic budgets: scale aligners proportional to resource health."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        # 5+ agents: dynamic scaling, always keep >= 2 miners
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 500 else 0
            return 4, scrambler
        else:
            # Rich: max pressure, keep 2 miners
            scrambler = 1 if step >= 500 else 0
            aligner = min(5, num_agents - 2 - scrambler)
            return aligner, scrambler


class AlphaAdaptiveV3Policy(MettagridSemanticPolicy):
    """Adaptive economy v3: dynamic aligner scaling based on resource health."""
    short_names = ["alpha-adaptive-v3"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveV3AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaChainExpandAgentPolicy(AlphaAdaptiveV3AgentPolicy):
    """AdaptiveV3 + aggressive chain expansion + better frontier exploration.

    Improvements:
    1. Higher expansion_weight (20 vs 10) to strongly prefer chain-building junctions
    2. Higher expansion_cap (120 vs 60) to distinguish junctions that unlock many others
    3. When idle: alternate between exploring new areas and mining (50/50)
       instead of only idle-scrambling, to discover more junctions
    4. Wider explore offsets for idle aligners to find distant junction clusters
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 20.0  # 2x: strongly prefer chain-building junctions
        self._expansion_cap = 120.0    # 2x: distinguish 6-unlock from 12-unlock junctions

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner with exploration-heavy idle behavior to discover more junctions."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions (walk there to extend network)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: alternate between exploring (odd agent_id) and other actions (even)
        # This ensures some agents always explore to discover new junction clusters
        min_res = _h.team_min_resource(state)
        if self._agent_id % 2 == 0:
            # Even agents: explore to discover new junctions
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        else:
            # Odd agents: scramble if economy healthy, mine if not
            if hearts > 0 and min_res >= 50:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")
            return self._miner_action(state, summary_prefix="idle_align_")


class AlphaChainExpandPolicy(MettagridSemanticPolicy):
    """Chain expansion: high expansion weight + exploration-heavy idle behavior."""
    short_names = ["alpha-chain-expand"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaChainExpandAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaChainDefenseAgentPolicy(AlphaChainExpandAgentPolicy):
    """ChainExpand + late-game defense: more scramblers when losing.

    When enemy junctions exceed ours, shift to scrambling to deny their score.
    Score = avg aligned junctions per tick (both teams same), so denying
    enemy junctions is as valuable as gaining our own.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Add late-game scramble defense when enemy is winning."""
        aligner_budget, scrambler_budget = super()._pressure_budgets(state, objective=objective)
        if objective in {"resource_coverage", "economy_bootstrap"}:
            return aligner_budget, scrambler_budget

        step = state.step or self._step_index
        if step < 1000:
            return aligner_budget, scrambler_budget

        num_agents = self.policy_env_info.num_agents
        # Small teams: no defense shift (scramblers too expensive)
        if num_agents <= 4:
            return aligner_budget, scrambler_budget

        # Count friendly vs enemy junctions
        team_id = _h.team_id(state)
        friendly = len(self._world_model.entities(
            entity_type="junction", predicate=lambda e: e.owner == team_id))
        enemy = len(self._world_model.entities(
            entity_type="junction", predicate=lambda e: e.owner not in {None, "neutral", team_id}))

        min_res = _h.team_min_resource(state)

        # When losing: shift aligners to scramblers
        if enemy > friendly + 3 and min_res >= 14:
            extra_scramblers = min(2, max(0, aligner_budget - 1))
            scrambler_budget += extra_scramblers
            aligner_budget -= extra_scramblers
        elif enemy > friendly and min_res >= 14:
            if aligner_budget > 1:
                scrambler_budget += 1
                aligner_budget -= 1

        return aligner_budget, scrambler_budget


class AlphaChainDefensePolicy(MettagridSemanticPolicy):
    """Chain expansion + late-game defense scrambling."""
    short_names = ["alpha-chain-defense"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaChainDefenseAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaMaxChainAgentPolicy(AlphaChainDefenseAgentPolicy):
    """Maximum chain expansion: higher weights + all-explore idle.

    Push expansion_weight to 30 and cap to 180 for maximum chain-building.
    ALL idle aligners explore to maximize junction discovery.
    Defense scrambling still active via budget shifts when losing.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 30.0
        self._expansion_cap = 180.0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """ALL idle aligners explore — maximum junction discovery."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe = [j for j in unreachable if _h.manhattan(current_pos, j.position) < hp - 20]
            targets = safe if safe else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            if _h.manhattan(current_pos, nearest.position) < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # ALL idle aligners explore — maximum junction discovery
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaMaxChainPolicy(MettagridSemanticPolicy):
    """Maximum chain expansion + defense scrambling."""
    short_names = ["alpha-max-chain"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMaxChainAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAggroChainAgentPolicy(AlphaAggressiveAgentPolicy):
    """Aggressive + silicon-priority mining.

    Takes the tournament-proven Aggressive base and adds silicon-priority mining
    to delay the economy collapse that typically starts around step 2000.
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining when silicon is scarce."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        least_amount = resources[least]
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaAggroChainPolicy(MettagridSemanticPolicy):
    """Aggressive base + chain expansion weights + silicon mining."""
    short_names = ["alpha-aggro-chain"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAggroChainAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaPureAlign — zero scrambling, chain expansion, fast start, pure alignment
# ---------------------------------------------------------------------------

class AlphaPureAlignAgentPolicy(AlphaChainExpandAgentPolicy):
    """Pure alignment: zero scrambling, chain expansion, aggressive early game.

    Combines:
    - ChainExpand's expansion_weight=20 and expansion_cap=120
    - Aggressive's fast early game (no heart batching before step 200)
    - Aggressive's tighter retreat margins
    - Zero scrambling (all hearts for alignment)
    - Idle aligners: always explore to find more junctions
    - Silicon-priority mining to delay resource bottleneck
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Tighter retreat margins (from Aggressive) — more productive time."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 10
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 8
        margin += int(state.self_state.inventory.get("heart", 0)) * 3
        margin += min(_h.resource_total(state), 12) // 3
        if not _h.has_role_gear(state, role):
            margin += 8
        if (state.step or 0) >= 2_500:
            margin += 5 if role == "aligner" else 3
        if hp <= safe_steps + margin:
            return True
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 15:
                return True
        return False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Pure aligner: align, expand, explore. Never scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Fast early game: no batching before step 200 (from Aggressive)
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15  # Even less conservative
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 15:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: always explore to find more junctions (never scramble)
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Zero scrambling. All pressure goes to alignment."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            # 4-agent: aggressive alignment ramp
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # With even modest economy, go 2 aligners immediately
            if min_res < 20:
                return min(2, num_agents - 1), 0
            # Strong economy: 3 aligners, 1 miner
            return min(3, num_agents - 1), 0

        # 5+ agents: aggressive alignment, zero scramblers
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            # Max pressure: only 1 miner needed
            return min(num_agents - 1, 7), 0
        elif step < 100:
            return 3, 0
        elif economy_crisis:
            return max(2, num_agents // 3), 0
        elif min_res < 7:
            return min(4, num_agents - 2), 0
        elif min_res < 30:
            return min(5, num_agents - 2), 0
        else:
            return min(6, num_agents - 1), 0

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining to delay depletion bottleneck."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        least_amount = resources[least]
        # Prioritize silicon when it's within 20 of bottleneck
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaPureAlignPolicy(MettagridSemanticPolicy):
    """Pure alignment: zero scrambling, chain expansion, fast start."""
    short_names = ["alpha-pure-align"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaPureAlignAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaFortress — sustained scoring via defense + economy management
# ---------------------------------------------------------------------------

class AlphaFortressAgentPolicy(AlphaChainExpandAgentPolicy):
    """Fortress: sustained scoring via early defense, economy conservation, chain expansion.

    Key insight: 10000-step games mean sustained scoring beats peak scoring.
    Maintaining 10 junctions for 10000 steps beats 13 for 3000 then 5 for 7000.

    Strategy:
    1. Heavy early scrambling to prevent Clips from building a lead
    2. More miners mid-game to build resource reserves for late game
    3. Chain expansion for efficient network building
    4. Silicon-priority mining to delay depletion
    5. Economy-responsive: ramp down pressure when resources low
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Tighter retreat for scramblers (they need to be aggressive), conservative for miners."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 12 if role == "scrambler" else 15
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 8
        margin += int(state.self_state.inventory.get("heart", 0)) * 4
        margin += min(_h.resource_total(state), 12) // 3
        if not _h.has_role_gear(state, role):
            margin += 8
        if (state.step or 0) >= 2_500:
            margin += 5 if role in {"aligner", "scrambler"} else 3
        if hp <= safe_steps + margin:
            return True
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 15:
                return True
        return False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner with idle-scramble when economy is healthy."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # No batching before step 200
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble if we have hearts and economy is healthy
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Economy needs help or nothing to scramble: explore
        if min_res < 14:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Sustained pressure: more scramblers early, more miners for economy."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            # 4-agent: balanced alignment + defense
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # 2 aligners + 1 scrambler when economy supports it
            if min_res >= 50 and step >= 300:
                return 2, 1
            if min_res >= 20:
                return 2, 0
            return 1, 0

        # 5+ agents: strong defense early, sustained economy
        if step < 30:
            return 2, 0

        economy_crisis = min_res < 3 and not can_hearts

        if economy_crisis:
            # Economy collapsed: minimal pressure, rebuild
            return max(1, num_agents // 4), 0

        if step < 200:
            # Early: ramp up with 1 scrambler from step 100
            if step >= 100 and min_res >= 7:
                return 3, 1
            return 2, 0

        # Mid-to-late game: sustained pressure with heavy defense
        if min_res >= 100:
            # Rich: max pressure, 2 scramblers
            return min(num_agents - 3, 5), 2
        elif min_res >= 30:
            # Healthy: balanced
            return min(num_agents - 3, 4), 1
        elif min_res >= 7:
            # Tight: fewer aligners, keep scrambler
            return min(num_agents - 3, 3), 1
        else:
            # Very tight: minimal pressure
            return 2, 0

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        least_amount = resources[least]
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaFortressPolicy(MettagridSemanticPolicy):
    """Fortress: sustained scoring via defense + economy management."""
    short_names = ["alpha-fortress"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaFortressAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaEconSurge — max miners early, then surge aligners when economy peaks
# ---------------------------------------------------------------------------

class AlphaEconSurgeAgentPolicy(AlphaFortressAgentPolicy):
    """Econ Surge: heavy mining first 1000 steps, then surge to max alignment.

    Insight: the Aggressive policy runs out of resources mid-game.
    By mining heavily early, we build reserves that sustain alignment for the
    full 10000 steps. Clips take junctions early but we catch up and sustain.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy-first: mine heavily early, surge alignment mid-game."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 300 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 200:
                return 1, 0  # Economy-first
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res >= 100:
                return min(3, num_agents - 1), 0
            if min_res >= 30:
                return 2, 0
            return 1, 0

        # 5+ agents: heavy mining first 500 steps
        economy_crisis = min_res < 3 and not can_hearts

        if economy_crisis:
            return max(1, num_agents // 4), 0

        if step < 100:
            return 1, 0  # Almost all mine
        if step < 500:
            # Building reserves: 2 aligners, 1 scrambler, rest mine
            if min_res >= 7:
                return 2, 1
            return 1, 0

        # Post-500: surge based on accumulated resources
        if min_res >= 200:
            # Massive reserves: go all-out
            return min(num_agents - 2, 6), 2
        elif min_res >= 100:
            return min(num_agents - 2, 5), 2
        elif min_res >= 50:
            return min(num_agents - 3, 4), 1
        elif min_res >= 20:
            return 3, 1
        elif min_res >= 7:
            return 2, 1
        else:
            return 1, 0


class AlphaEconSurgePolicy(MettagridSemanticPolicy):
    """Econ Surge: mine heavily early, then surge alignment with reserves."""
    short_names = ["alpha-econ-surge"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconSurgeAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaUltra — optimized for tournament (weak opponents): max align, zero scramble,
# chain expansion, aggressive economy scaling, exploration-heavy idle
# ---------------------------------------------------------------------------

class AlphaUltraAgentPolicy(AlphaAggressiveAgentPolicy):
    """Ultra: tournament-optimized for weak opponents.

    Combines best elements:
    - Aggressive: fast start, tight retreat margins, no early batching
    - ChainExpand: expansion_weight=20 for efficient network building
    - Zero scramblers: opponents are weak, hearts better spent on alignment
    - All idle aligners explore: discover more junction clusters
    - Economy-responsive: scale aligners with resources, never starve
    - Re-alignment hotspot bonus from AlphaCog
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 20.0
        self._expansion_cap = 120.0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > explore. Never scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # No batching first 200 steps — go align immediately
        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions — braver than parent (hp - 15)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 15:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: always explore to discover more junctions (never scramble)
        min_res = _h.team_min_resource(state)
        if min_res < 10:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Tournament-optimized: zero scramblers, max aligners, economy-responsive."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # 4 agents: scale aggressively, no scramblers
            if min_res >= 100:
                return min(3, num_agents - 1), 0
            if min_res >= 30:
                return 2, 0
            return 1, 0

        # 5+ agents: max alignment, zero scramblers
        if step < 20:
            return 2, 0

        economy_crisis = min_res < 3 and not can_hearts

        if economy_crisis:
            return max(1, num_agents // 4), 0

        # Aggressive scaling based on economy
        if min_res >= 200:
            return min(num_agents - 1, 7), 0  # Nearly all align
        elif min_res >= 100:
            return min(num_agents - 1, 6), 0
        elif min_res >= 50:
            return min(num_agents - 2, 5), 0
        elif min_res >= 20:
            return min(num_agents - 2, 4), 0
        elif min_res >= 7:
            return 3, 0
        else:
            return 2, 0

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        least_amount = resources[least]
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaUltraPolicy(MettagridSemanticPolicy):
    """Ultra: tournament-optimized for weak opponents. Max align, zero scramble, chain expand."""
    short_names = ["alpha-ultra"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaUltraAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaUltraV2 — Ultra chain expansion + light defense (1 scrambler)
# Insight: Ultra peaks at 20j but collapses to 0. Adding 1 scrambler
# should slow Clips' scrambling enough to sustain high junction counts.
# ---------------------------------------------------------------------------

class AlphaUltraV2AgentPolicy(AlphaUltraAgentPolicy):
    """Ultra V2: chain expansion + light defense.

    Ultra peaks at 20 junctions but collapses because Clips scrambles
    everything. V2 adds 1 scrambler to slow Clips' advance while keeping
    near-max alignment pressure and chain expansion.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > idle-scramble-if-rich > explore."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 15:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble if economy healthy (hearts + resources), else explore
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 20:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")
        if min_res < 10:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Chain expansion + light defense: 1 scrambler after step 200."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # 4 agents: scale with economy, add scrambler when rich
            if min_res >= 100:
                return 2, 1  # 2 align + 1 scramble + 1 mine
            if min_res >= 30:
                return 2, 0
            return 1, 0

        # 5+ agents: max alignment + 1 scrambler
        if step < 20:
            return 2, 0

        economy_crisis = min_res < 3 and not can_hearts
        if economy_crisis:
            return max(1, num_agents // 4), 0

        # 1 scrambler once economy supports it (step 200+)
        scrambler = 1 if step >= 200 and min_res >= 7 else 0

        if min_res >= 200:
            return min(num_agents - 1 - scrambler, 6), scrambler
        elif min_res >= 100:
            return min(num_agents - 2 - scrambler, 5), scrambler
        elif min_res >= 50:
            return min(num_agents - 2 - scrambler, 4), scrambler
        elif min_res >= 20:
            return 3, scrambler
        elif min_res >= 7:
            return 2, scrambler
        else:
            return 2, 0


class AlphaUltraV2Policy(MettagridSemanticPolicy):
    """Ultra V2: chain expansion + light defense (1 scrambler)."""
    short_names = ["alpha-ultra-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaUltraV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaUltraV3 — Ultra chain expansion + aggressive idle-scramble by ALL aligners
# Insight: Aggressive sustains 8-9j through idle-scramble. Ultra peaks at 20j
# but collapses. V3: chain expansion + idle-scramble to sustain high junctions.
# ---------------------------------------------------------------------------

class AlphaUltraV3AgentPolicy(AlphaUltraAgentPolicy):
    """Ultra V3: chain expansion + Aggressive-style idle-scramble.

    Keeps Ultra's chain expansion and fast start but uses Aggressive's
    idle behavior: when no junctions to align, scramble enemy junctions
    to slow Clips. This should sustain the 20j peak much longer.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner with Aggressive-style idle-scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 15:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble if economy healthy (like Aggressive), mine if tight, explore if neither
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")
        if min_res < 14:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Max aligners + early scrambler like Aggressive, but with chain expansion."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res >= 50 and step >= 500:
                return min(3, num_agents - 1), 0
            return 2, 0

        # 5+ agents: match Aggressive budgets (which get 6.54 in tournament)
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        return aligner_budget, scrambler_budget


class AlphaUltraV3Policy(MettagridSemanticPolicy):
    """Ultra V3: chain expansion + Aggressive budgets + idle-scramble."""
    short_names = ["alpha-ultra-v3"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaUltraV3AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaUltraV4 — V3 with expansion_weight=25 (5% higher than V3's 20)
# ---------------------------------------------------------------------------

class AlphaUltraV4AgentPolicy(AlphaUltraV3AgentPolicy):
    """V4: expansion_weight=25 to push chain expansion harder."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._expansion_weight = 25.0
        self._expansion_cap = 150.0


class AlphaUltraV4Policy(MettagridSemanticPolicy):
    """Ultra V4: expansion_weight=25."""
    short_names = ["alpha-ultra-v4"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaUltraV4AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaUltraV5 — V3 + economy surge: heavier mining first 500 steps,
# then surge to max aligners when reserves are high
# ---------------------------------------------------------------------------

class AlphaUltraV5AgentPolicy(AlphaUltraV3AgentPolicy):
    """V5: V3 base + economy surge. Mine hard early, then max align when reserves peak."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy surge: mine first 300 steps, then scale aggressively."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 300 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 150:
                return 1, 0  # Economy-first
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res >= 100:
                return min(3, num_agents - 1), 0
            if min_res >= 30:
                return 2, 0
            return 1, 0

        # 5+ agents: economy surge
        if step < 30:
            return 1, 0  # All mine at start

        economy_crisis = min_res < 3 and not can_hearts
        if economy_crisis:
            return max(1, num_agents // 4), 0

        if step < 200:
            # Light pressure, heavy mining
            return 2, 0

        # Post-200: scale based on reserves (like Aggressive but with chain expansion)
        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, num_agents // 4)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = 1

        if min_res >= 200:
            return min(num_agents - 1 - scrambler_budget, 6), scrambler_budget
        elif min_res >= 100:
            return min(num_agents - 1 - scrambler_budget, 5), scrambler_budget
        elif min_res >= 50:
            return min(num_agents - 2 - scrambler_budget, 4), scrambler_budget
        elif min_res >= 20:
            return 3, scrambler_budget
        elif min_res >= 7:
            return 2, scrambler_budget
        else:
            return 2, 0


class AlphaUltraV5Policy(MettagridSemanticPolicy):
    """Ultra V5: chain expansion + economy surge + Aggressive idle-scramble."""
    short_names = ["alpha-ultra-v5"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaUltraV5AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaCoopPolicy — Aggressive but cooperative-friendly: zero scrambling,
# idle aligners mine to sustain economy. For cooperative tournament scoring.
# ---------------------------------------------------------------------------

class AlphaCoopAgentPolicy(AlphaAggressiveAgentPolicy):
    """Cooperative variant: Aggressive play but NEVER scramble.

    Since tournament scoring is cooperative (both teams get same score),
    scrambling opponent junctions HURTS our score. Instead:
    1. Zero scramblers in budgets
    2. Idle aligners mine (sustain economy longer)
    3. Everything else same as Aggressive
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > mine. Never scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: NEVER scramble (cooperative scoring). Mine to sustain economy.
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive budgets but zero scramblers for cooperative scoring."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res >= 50 and step >= 500:
                return min(3, num_agents - 1), 0
            return min(2, num_agents - 1), 0

        # 5+ agents: same as Aggressive but zero scramblers
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            return min(num_agents - 1, 7), 0
        elif step < 100:
            return 3, 0
        elif economy_crisis:
            return max(2, num_agents // 3), 0
        elif min_res < 7:
            return min(4, num_agents - 2), 0
        else:
            return min(5, num_agents - 2), 0


class AlphaCoopPolicy(MettagridSemanticPolicy):
    """Cooperative: Aggressive play, zero scrambling, idle-mine for economy."""
    short_names = ["alpha-coop"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCoopAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaEconMaxPolicy — More miners, fewer aligners, sustain economy longest.
# Hypothesis: if we sustain alignment for more ticks, total score increases.
# ---------------------------------------------------------------------------

class AlphaEconMaxAgentPolicy(AlphaCoopAgentPolicy):
    """Economy-maximizing: keep more miners to sustain alignment through full 10k steps."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Conservative budgets: keep 50%+ agents as miners to sustain economy."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # Max 2 aligners to keep at least 2 miners
            return min(2, num_agents - 2), 0

        # 5+ agents: keep at least half as miners
        min_miners = max(num_agents // 2, 3)
        max_aligners = num_agents - min_miners

        if step < 30:
            return min(2, max_aligners), 0

        if min_res < 3 and not can_hearts:
            return 1, 0
        elif min_res < 20:
            return min(2, max_aligners), 0
        elif min_res < 50:
            return min(3, max_aligners), 0
        else:
            return min(max_aligners, 4), 0


class AlphaEconMaxPolicy(MettagridSemanticPolicy):
    """EconMax: conservative alignment, maximum economy sustainability."""
    short_names = ["alpha-econ-max"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconMaxAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaFocusedPolicy — Hub-proximal junctions + economy sustain + fast ramp
# Hypothesis: concentrate alignment near hub for defensibility and fast re-align.
# Sustain economy longer by keeping miners focused on carbon bottleneck.
# ---------------------------------------------------------------------------

class AlphaFocusedAgentPolicy(AlphaAggressiveAgentPolicy):
    """Focused variant: hub-proximal junctions, carbon-biased mining, sustained economy.

    Key changes from Aggressive:
    1. Network weight 0.5 — prefer junctions near hub/network (shorter trips, easier re-align)
    2. More aligners from step 0 (3 instead of 2) for faster initial coverage
    3. Carbon-biased mining: more miners prioritize carbon (3x consumed by aligner gear)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # Prefer hub-proximal junctions
        self._network_weight = 0.5
        # Force carbon bias for even-numbered agents (50% carbon miners)
        if self._agent_id % 2 == 0:
            self._default_resource_bias = "carbon"
            self._resource_bias = "carbon"

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Faster ramp: 3 aligners from step 0, otherwise same as Aggressive."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 50:
                return 2, 0  # Faster: 2 aligners from step 0 (was 1)
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 200 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents: same as Aggressive (2 at step 0 — 3 burns too much carbon)
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaFocusedPolicy(MettagridSemanticPolicy):
    """Focused: hub-proximal junctions, carbon mining, sustained economy."""
    short_names = ["alpha-focused"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaFocusedAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaSustainV2Policy — Maximum economy sustain with Aggressive base.
# Key idea: keep 3+ miners always, scale alignment to economy health.
# Late-game idle aligners mine instead of scramble.
# ---------------------------------------------------------------------------

class AlphaSustainV2AgentPolicy(AlphaAggressiveAgentPolicy):
    """Sustain V2: tighter economy-responsive alignment that never over-extends.

    Key changes from Aggressive:
    1. Aligner budget STRICTLY capped by economy health
    2. More miners at all times — never fewer than 3 (for 8-agent)
    3. Idle aligners mine (not scramble) in late game to sustain economy
    4. Carbon-biased mining for all agents
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # All agents carbon-biased (carbon is the bottleneck)
        self._default_resource_bias = "carbon"
        self._resource_bias = "carbon"

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > mine. In late game, idle mine instead of scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Late game: idle mine to sustain economy (don't scramble)
        min_res = _h.team_min_resource(state)
        if step >= 3000 or min_res < 20:
            return self._miner_action(state, summary_prefix="idle_align_")

        # Early game: scramble if economy healthy
        if int(state.self_state.inventory.get("heart", 0)) > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy-gated alignment: never more aligners than economy can sustain."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        # Minimum 2 miners always
        min_miners = max(2, num_agents // 3)

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            max_aligners = num_agents - min_miners
            if min_res >= 50:
                return min(max_aligners, 2), 0
            return min(max_aligners, 1), 0

        # 5+ agents: economy-gated
        if step < 30:
            return 2, 0

        max_pressure = num_agents - min_miners

        if min_res < 3 and not can_hearts:
            return 1, 0
        elif min_res < 7:
            aligner_budget = min(2, max_pressure)
        elif min_res < 20:
            aligner_budget = min(3, max_pressure)
        elif min_res < 50:
            aligner_budget = min(4, max_pressure)
        elif min_res < 100:
            aligner_budget = min(5, max_pressure)
        else:
            aligner_budget = max_pressure

        # Scramblers only with surplus economy, early game only
        scrambler_budget = 0
        if step < 3000 and step >= 200 and min_res >= 14:
            scrambler_budget = min(1, aligner_budget // 3)
            aligner_budget = max(aligner_budget - scrambler_budget, 1)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaSustainV2Policy(MettagridSemanticPolicy):
    """SustainV2: economy-gated alignment, carbon-biased mining, late-game mining."""
    short_names = ["alpha-sustain-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSustainV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaCarbonBoostPolicy — Aggressive + carbon-biased mining.
# Simplest possible fix for carbon depletion bottleneck.
# ---------------------------------------------------------------------------

class AlphaCarbonBoostAgentPolicy(AlphaAggressiveAgentPolicy):
    """Aggressive but 50% of miners prioritize carbon (the #1 bottleneck).

    Carbon is consumed 3x by aligner gear. Default bias only gives 25% carbon miners.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # Even-numbered agents: carbon bias (50% vs default 25%)
        if self._agent_id % 2 == 0:
            self._default_resource_bias = "carbon"
            self._resource_bias = "carbon"


class AlphaAllCarbonAgentPolicy(AlphaAggressiveAgentPolicy):
    """100% carbon bias: ALL miners default to carbon.

    Carbon is 3x consumed. Resource_priority still sorts by lowest inventory,
    so miners will switch to other resources when carbon is abundant.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._default_resource_bias = "carbon"
        self._resource_bias = "carbon"


class AlphaAllCarbonPolicy(MettagridSemanticPolicy):
    """AllCarbon: Aggressive + 100% carbon-biased miners."""
    short_names = ["alpha-all-carbon"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAllCarbonAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCarbonBoostPolicy(MettagridSemanticPolicy):
    """CarbonBoost: Aggressive + 50% carbon-biased miners."""
    short_names = ["alpha-carbon-boost"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCarbonBoostAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaTeamFixPolicy — Aggressive but uses actual team size for budgets.
# The bug: policy_env_info.num_agents=8 (total) in 4v4, causing budget overflow.
# Fix: use len(shared_team_ids) for per-team agent count.
# ---------------------------------------------------------------------------

class AlphaTeamFixAgentPolicy(AlphaAggressiveAgentPolicy):
    """Aggressive with proper per-team budget allocation.

    Fixes the num_agents bug where total (8) is used instead of per-team (4).
    This means budget calculations correctly leave miners when team is small.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive budgets using actual team size."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        # Use actual team size, not total agents
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            # Keep at least 1 miner
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 200 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents: Aggressive scaling with proper team size
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        return aligner_budget, scrambler_budget


class AlphaTeamFixPolicy(MettagridSemanticPolicy):
    """TeamFix: Aggressive + proper per-team budget allocation."""
    short_names = ["alpha-team-fix"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTeamFixAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaSmartHybridPolicy — TeamFix budgets + carbon bias for 5+ agents
# Best of both worlds: proper budgets for 4a, carbon fix for 8a.
# ---------------------------------------------------------------------------

class AlphaSmartHybridAgentPolicy(AlphaTeamFixAgentPolicy):
    """Smart hybrid: TeamFix budgets + adaptive carbon bias.

    - 4 agents: balanced bias (all resources needed equally, can't afford carbon focus)
    - 5+ agents: carbon bias for even agents (carbon bottleneck with many agents)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._carbon_bias_applied = False

    def _preferred_miner_extractor(self, state: MettagridState) -> KnownEntity | None:
        """Apply carbon bias lazily once team size is known."""
        if not self._carbon_bias_applied:
            self._carbon_bias_applied = True
            team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
            if team_size >= 5 and self._agent_id % 2 == 0:
                self._default_resource_bias = "carbon"
                self._resource_bias = "carbon"
        return super()._preferred_miner_extractor(state)


class AlphaSmartHybridPolicy(MettagridSemanticPolicy):
    """SmartHybrid: TeamFix + carbon bias for large teams."""
    short_names = ["alpha-smart-hybrid"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSmartHybridAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaHighEffAgentPolicy — Aggressive + efficient mining (high deposit threshold)
# + no scrambling (idle mine instead) + carbon bias
# Combines best elements from testing.
# ---------------------------------------------------------------------------

class AlphaTeamCarbonAgentPolicy(AlphaTeamFixAgentPolicy):
    """TeamFix + CarbonBoost: proper team-size budgets + carbon bias.

    Combines the two most impactful fixes:
    1. TeamFix: correct per-team agent count for budgets
    2. CarbonBoost: 50% carbon-biased miners for the #1 bottleneck
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        if self._agent_id % 2 == 0:
            self._default_resource_bias = "carbon"
            self._resource_bias = "carbon"


class AlphaTeamCarbonPolicy(MettagridSemanticPolicy):
    """TeamCarbon: proper team-size budgets + carbon-biased mining."""
    short_names = ["alpha-team-carbon"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTeamCarbonAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaHighEffAgentPolicy(AlphaAggressiveAgentPolicy):
    """High efficiency: optimized mining + no scrambling waste + carbon bias.

    Key changes from Aggressive:
    1. Higher deposit threshold (20) — fewer trips to hub, more mining per trip
    2. Idle aligners mine instead of scramble — sustain economy, don't waste hearts
    3. Carbon bias for even agents — address carbon bottleneck
    4. No scramblers in early game — pure economy + alignment focus
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        if self._agent_id % 2 == 0:
            self._default_resource_bias = "carbon"
            self._resource_bias = "carbon"

    def _should_deposit_resources(self, state: MettagridState) -> bool:
        """Higher deposit threshold (20) for more efficient mining trips."""
        cargo = _h.resource_total(state)
        if cargo <= 0:
            return False
        # Miners: batch to 20 before depositing
        threshold = 20 if _h.has_role_gear(state, "miner") else 4
        if cargo >= threshold:
            return True
        # Near hub, deposit anything
        hub = self._nearest_hub(state)
        if hub is not None:
            dist = _h.manhattan(_h.absolute_position(state), hub.position)
            if dist <= 2 and cargo >= 4:
                return True
        # If retreating with cargo, deposit
        hp = int(state.self_state.inventory.get("hp", 0))
        if hp < 30 and cargo >= 8:
            return True
        return False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > mine. Never scramble (saves hearts)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: ALWAYS mine to sustain economy (never scramble — saves hearts for alignment)
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Same as Aggressive but fewer scramblers — pure economy + alignment."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if min_res >= 50 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0  # Zero scramblers

        # 5+ agents
        if step < 30:
            return 2, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Only 1 scrambler late game with surplus — pure alignment focus
        scrambler_budget = 0
        if step >= 5000 and min_res >= 50:
            scrambler_budget = 1

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaHighEffPolicy(MettagridSemanticPolicy):
    """HighEff: efficient mining, no scramble, carbon bias, pure alignment focus."""
    short_names = ["alpha-high-eff"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaHighEffAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaRushPolicy — Optimized for tournament score >10
#
# Combines: TeamFix budgets + Aggressive idle-scramble + silicon-priority
# mining + faster alignment start + reduced deaths
# ---------------------------------------------------------------------------

class AlphaRushAgentPolicy(AlphaAggressiveAgentPolicy):
    """Rush: fast alignment with silicon-aware economy and smart scrambling.

    Key changes from Aggressive:
    1. TeamFix: use actual team size for budgets (not total agents)
    2. Silicon priority: 50% of miners always target silicon (the #1 bottleneck)
    3. Smarter budgets: more aligners earlier, fewer when economy is weak
    4. Faster first alignment start
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        # 50% of agents get silicon bias (the bottleneck resource)
        if self._agent_id % 2 == 1:
            self._default_resource_bias = "silicon"
            self._resource_bias = "silicon"

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TeamFix budgets with aggressive alignment pressure."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        # Use actual team size
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 150 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 5 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 30 and step >= 400:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 100 and step >= 800:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 25:
            return 2, 0

        economy_surplus = min_res >= 80
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 80:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 2000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 150 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Prioritize silicon when it's low relative to others."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        others_min = min(v for k, v in resources.items() if k != "silicon")
        # If silicon is significantly lower, bias toward it
        if silicon < others_min * 0.7 and silicon < 50:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=_least_resource(resources))


class AlphaRushPolicy(MettagridSemanticPolicy):
    """Rush: fast alignment + silicon economy + TeamFix budgets."""
    short_names = ["alpha-rush"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaRushAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaEconRushPolicy — Rush variant with economy-first strategy
# ---------------------------------------------------------------------------

class AlphaEconRushAgentPolicy(AlphaRushAgentPolicy):
    """EconRush: build strong economy first, then overwhelm with alignment."""

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Economy-first: delay alignment pressure until resources are strong."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 10 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 150 or (min_res < 10 and not can_hearts):
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 150 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 50:
            return 1, 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 5 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 150:
            pressure_budget = 2
        elif economy_crisis:
            pressure_budget = max(1, num_agents // 4)
        elif min_res < 10:
            pressure_budget = min(3, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 20:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 300 and min_res >= 10:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaEconRushPolicy(MettagridSemanticPolicy):
    """EconRush: economy-first then overwhelm with alignment."""
    short_names = ["alpha-econ-rush"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaEconRushAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaTurboPolicy — Aggressive base (keep the num_agents "bug" since it
# accidentally creates better tournament budgets) + silicon-aware macro
# directive + earlier scrambling for network expansion
# ---------------------------------------------------------------------------

class AlphaTurboAgentPolicy(AlphaAggressiveAgentPolicy):
    """Turbo: Aggressive base + silicon-aware economy + faster scramble start.

    The num_agents bug actually HELPS in tournament because it makes
    small-team budgets more aggressive. Keep it.

    Changes from Aggressive:
    1. Silicon macro directive: bias all miners toward silicon when low
    2. Earlier scrambler start: step 100 instead of 200
    3. More scramblers in surplus: clear enemy junctions faster
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive budgets with earlier/more scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents  # Keep the bug!

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 400:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 100 and step >= 600:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 25:
            return 2, 0

        economy_surplus = min_res >= 80
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 80:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Earlier scramblers: start at step 100 (vs 200 in Aggressive)
        scrambler_budget = 0
        if step >= 2000 and min_res >= 14:
            scrambler_budget = min(3, pressure_budget // 3)
        elif step >= 100 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-aware: bias miners toward silicon when it's the bottleneck."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        others = [v for k, v in resources.items() if k != "silicon"]
        others_min = min(others) if others else 0
        # Strong silicon bias when it's clearly the bottleneck
        if silicon < others_min * 0.6:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=_least_resource(resources))


class AlphaTurboPolicy(MettagridSemanticPolicy):
    """Turbo: Aggressive + silicon-aware economy + earlier scramblers."""
    short_names = ["alpha-turbo"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTurboAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaMaxAlignV2Policy — Pure alignment pressure.
# Maximum aligners, minimal scramblers, no idle-mine for aligners.
# Hypothesis: in cooperative scoring, more alignment = higher score for both.
# ---------------------------------------------------------------------------

class AlphaMaxAlignV2AgentPolicy(AlphaAggressiveAgentPolicy):
    """MaxAlignV2: Maximum alignment pressure. Keep num_agents bug.

    Changes from Aggressive:
    1. Even more aligners: 1 more aligner in each bracket
    2. Idle aligners: explore for new junctions instead of scrambling
    3. Reduced scrambler budget: only 1 scrambler, only when surplus
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > explore. Minimal scrambling."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble only if economy is very healthy, otherwise mine
        min_res = _h.team_min_resource(state)
        if int(state.self_state.inventory.get("heart", 0)) > 0 and min_res >= 30:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Help economy by mining
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Maximum alignment pressure, minimal scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 150 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 5 and not can_hearts:
                return 1, 0
            aligner_budget = min(3, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                aligner_budget = num_agents - 1  # All but 1
            return aligner_budget, scrambler_budget

        # 5+ agents: max alignment
        if step < 25:
            return 2, 0

        economy_surplus = min_res >= 80
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            aligner_budget = min(num_agents - 1, 7)
            scrambler_budget = 1 if step >= 500 else 0
            return aligner_budget - scrambler_budget, scrambler_budget
        elif step < 80:
            return 3, 0
        elif economy_crisis:
            return max(2, num_agents // 3), 0
        elif min_res < 7:
            return min(5, num_agents - 2), 0
        else:
            return min(6, num_agents - 2), 0


class AlphaMaxAlignV2Policy(MettagridSemanticPolicy):
    """MaxAlignV2: maximum alignment pressure, minimal scramblers."""
    short_names = ["alpha-max-align-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMaxAlignV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaSustainablePolicy — KEY POLICY
#
# The core problem: Aggressive's budget bug causes 0 miners in small teams.
# Economy collapses after step 2000-3000, junctions lost, score drops.
# In 10000-step games, the second half (5000-10000) has near-zero junctions.
#
# Fix: Aggressive alignment pressure but GUARANTEE at least 1 miner.
# Use shared_team_ids for team sizing (accurate), but keep aggressive budgets.
# ---------------------------------------------------------------------------

class AlphaSustainableAgentPolicy(AlphaAggressiveAgentPolicy):
    """Sustainable: Aggressive alignment with guaranteed economy.

    Key insight: In 10000-step tournament games, economy sustainability
    matters more than front-loading alignment. Average junctions in the
    second half can drop to ~0 without miners.

    Changes from Aggressive:
    1. Budget caps ensure at least 1 miner per team at all times
    2. Uses actual team size for the MINER FLOOR only (keep aggressive num_agents for pressure)
    3. More aggressive idle-mine when economy is struggling
    4. Adaptive resource bias based on actual bottleneck
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive pressure but never exceed (team_size - 1) total roles."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        # Use big num_agents for aggressive budgets
        num_agents = self.policy_env_info.num_agents
        # But use actual team size to cap roles (guarantee 1 miner)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents
        max_roles = max(team_size - 1, 1)  # Always leave 1 miner

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return min(1, max_roles), 0

        if num_agents <= 4:
            if step < 100:
                return min(1, max_roles), 0
            if min_res < 7 and not can_hearts:
                return min(1, max_roles), 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 0
            if min_res >= 50 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            if min_res >= 200 and step >= 1000:
                scrambler_budget = 1
                aligner_budget = min(2, num_agents - 1 - scrambler_budget)
            # Cap to team_size - 1 (guarantee miner)
            total = aligner_budget + scrambler_budget
            if total > max_roles:
                scrambler_budget = min(scrambler_budget, max(0, max_roles - 1))
                aligner_budget = min(aligner_budget, max_roles - scrambler_budget)
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 30:
            return min(2, max_roles), 0

        economy_surplus = min_res >= 100
        economy_crisis = min_res < 3 and not can_hearts

        if economy_surplus:
            pressure_budget = min(num_agents - 1, 7)
        elif step < 100:
            pressure_budget = 3
        elif economy_crisis:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)

        # Cap to team_size - 1
        total = aligner_budget + scrambler_budget
        if total > max_roles:
            scrambler_budget = min(scrambler_budget, max(0, max_roles - 1))
            aligner_budget = min(aligner_budget, max_roles - scrambler_budget)

        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: idle-mine when economy is weak instead of scrambling."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble ONLY if economy very healthy; otherwise MINE
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 20:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Economy needs help: mine
        return self._miner_action(state, summary_prefix="idle_align_")


class AlphaSustainablePolicy(MettagridSemanticPolicy):
    """Sustainable: Aggressive alignment with guaranteed economy sustainability."""
    short_names = ["alpha-sustainable"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSustainableAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaSustainV3Policy — Sustainable + no scrambling + higher idle-mine threshold
#
# Hypothesis: in cooperative scoring, scrambling hurts BOTH teams.
# Save all hearts for alignment. Mine when not aligning.
# ---------------------------------------------------------------------------

class AlphaSustainV3AgentPolicy(AlphaSustainableAgentPolicy):
    """SustainV3: no scrambling, pure alignment + mining economy.

    Changes from Sustainable:
    1. Zero scramblers always (save hearts for alignment)
    2. Idle aligners ALWAYS mine (never scramble)
    3. Higher economy threshold for alignment pressure
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Same as Sustainable but zero scramblers."""
        aligner, _ = super()._pressure_budgets(state, objective=objective)
        return aligner, 0  # Never assign scramblers

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Aligner: align > expand > MINE. No scrambling ever."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Always mine when idle (never scramble — save hearts for alignment)
        return self._miner_action(state, summary_prefix="idle_align_")


class AlphaSustainV3Policy(MettagridSemanticPolicy):
    """SustainV3: no scrambling, pure alignment + mining."""
    short_names = ["alpha-sustain-v3"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSustainV3AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ---------------------------------------------------------------------------
# AlphaAdaptiveTeamPolicy — THE KEY POLICY
#
# Adapts strategy based on actual team size:
# - Large teams (4+): Use Aggressive (with bug) for max alignment pressure
# - Small teams (2-3): Use Sustainable (miner guarantee) for economy
# - 1 agent: Pure mining→alignment cycle
#
# Aggressive 8a 10k: 15.73 (amazing!)
# Sustainable 4a 10k: 8.63 (good, beats Aggressive's 6.45)
# ---------------------------------------------------------------------------

class AlphaAdaptiveTeamAgentPolicy(AlphaAggressiveAgentPolicy):
    """AdaptiveTeam: Aggressive base but caps budget for small teams.

    For large teams (4+): pure Aggressive (no cap, bug helps)
    For small teams (2-3): cap budget to team_size-1 (guarantee miner)
    Also: idle aligners mine instead of scramble when economy < 20
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Aggressive budgets, capped for small teams."""
        # Get base Aggressive budgets
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)

        # Always cap to ensure at least 1 miner per team
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        max_roles = max(team_size - 1, 1)
        total = aligner + scrambler
        if total > max_roles:
            scrambler = min(scrambler, max(0, max_roles - 1))
            aligner = min(aligner, max_roles - scrambler)

        return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """For small teams, idle-mine instead of idle-scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble when economy healthy, mine otherwise
        min_res = _h.team_min_resource(state)
        # Higher threshold for scrambling = more conservative economy
        scramble_threshold = 20  # Only scramble with healthy economy

        if hearts > 0 and min_res >= scramble_threshold:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < scramble_threshold:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaAdaptiveTeamPolicy(MettagridSemanticPolicy):
    """AdaptiveTeam: Aggressive for large teams, Sustainable for small teams."""
    short_names = ["alpha-adaptive-team"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveTeamAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveTeamV2AgentPolicy(AlphaAdaptiveTeamAgentPolicy):
    """AdaptiveTeamV2: Safer retreat + gentle late-game economy scaling.

    Key improvements over AdaptiveTeam:
    1. Slightly more conservative retreat for all roles (reduce deaths)
    2. Very late game (>7000): if economy truly dead, cap aligners at 1
    3. Keep aggressive alignment in early-mid game (DON'T cut on temp dips)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Standard budgets but reduce late-game when economy is truly dead."""
        step = state.step or self._step_index
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)

        # Only intervene in very late game when economy is genuinely collapsed
        if step >= 7000:
            resources = _shared_resources(state)
            min_res = min(resources.values())
            total_res = sum(resources.values())
            # Economy truly dead: can't make hearts (need 7 of each = 28 total)
            if min_res == 0 and total_res < 50:
                return min(aligner, 1), 0

        return aligner, scrambler

    def _should_retreat(self, state: MettagridState, role: str, safe_target: KnownEntity | None) -> bool:
        """Slightly more conservative retreat to reduce deaths."""
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role) + 5
        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 12  # vs 10 in Aggressive
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 3
        margin += min(_h.resource_total(state), 12) // 3
        if not _h.has_role_gear(state, role):
            margin += 10
        step = state.step or self._step_index
        if step >= 2_500:
            margin += 7 if role in {"aligner", "scrambler"} else 5
        if hp <= safe_steps + margin:
            return True
        if role == "miner" and safe_target is not None:
            pos = _h.absolute_position(state)
            dist = _h.manhattan(pos, safe_target.position)
            if dist > _MINER_MAX_HUB_DISTANCE and hp < dist + 20:
                return True
        return False


class AlphaAdaptiveTeamV2Policy(MettagridSemanticPolicy):
    """AdaptiveTeamV2: Late-game economy fixes."""
    short_names = ["alpha-adaptive-team-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveTeamV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveTeamV3AgentPolicy(AlphaAdaptiveTeamAgentPolicy):
    """AdaptiveTeamV3: Tournament-optimized with slower early ramp.

    Key insight from tournament analysis: in 4v4 matches, both policies
    independently allocate 3 aligners each = 6 aligners + 2 miners for
    8 total agents. This crashes the economy by step 500 (carbon drops to 0).

    Fix: slower early-game ramp to ensure mining builds economy before
    alignment ramps up. This trades some early alignment speed for
    much better economy sustainability in tournament.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Slower early ramp for tournament compatibility."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # Detect tournament: team_size < num_agents means partner policy exists
        in_tournament = team_size < num_agents

        if in_tournament:
            # Tournament mode: conservative early game, proportional allocation
            if step < 200:
                return 1, 0  # 1 aligner, rest mine — build economy first
            if step < 500:
                if min_res < 20:
                    return 1, 0  # Economy still building
                return min(2, team_size - 1), 0
            if step < 1000:
                if min_res < 10:
                    return 1, 0
                return min(2, team_size - 1), 0

            # After step 1000: standard AdaptiveTeam budgets
            aligner, scrambler = super()._pressure_budgets(state, objective=objective)
            # Extra economy check: if resources very low, pull back
            if min_res < 5:
                return min(aligner, 1), 0
            return aligner, scrambler
        else:
            # Self-play mode: use standard AdaptiveTeam budgets
            return super()._pressure_budgets(state, objective=objective)


class AlphaAdaptiveTeamV3Policy(MettagridSemanticPolicy):
    """AdaptiveTeamV3: Tournament-optimized with slower early ramp."""
    short_names = ["alpha-adaptive-team-v3"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveTeamV3AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV2AgentPolicy(AlphaAdaptiveV3AgentPolicy):
    """TournamentV2: Best tournament strategy.

    Based on AdaptiveV3 (best tournament scorer at 5.5) with improvements:
    1. Budget cap to guarantee miners in sub-team
    2. Lower idle-scramble threshold (opponents are weak in tournament)
    3. Keep silicon-priority mining from Balanced base
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """AdaptiveV3 budgets + team cap for tournament safety."""
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)

        # Cap by actual sub-team size to guarantee miners
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        max_roles = max(team_size - 1, 1)
        total = aligner + scrambler
        if total > max_roles:
            scrambler = min(scrambler, max(0, max_roles - 1))
            aligner = min(aligner, max_roles - scrambler)

        return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Lower scramble threshold since tournament opponents are weak."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble more aggressively (opponents are weak)
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Mine when economy tight
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV2Policy(MettagridSemanticPolicy):
    """TournamentV2: AdaptiveV3 base + team cap + aggressive idle scramble."""
    short_names = ["alpha-tournament-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveV4AgentPolicy(AlphaAdaptiveV3AgentPolicy):
    """AdaptiveV4: Faster ramp + lower idle-scramble threshold.

    Based on AdaptiveV3 (best tournament scorer) with tweaks:
    1. Lower resource thresholds for aligner ramp (20% faster scaling)
    2. Lower idle-scramble threshold (30 vs 50 in Balanced)
    3. No dedicated scramblers in budget (save hearts for aligners)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Slightly faster ramp than AdaptiveV3, no dedicated scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 70 and step >= 400:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        # 5+ agents: faster scaling, no dedicated scramblers
        if step < 30:
            return 2, 0

        if min_res < 7 and not can_hearts:
            return 1, 0
        elif min_res < 20:
            return 2, 0
        elif min_res < 60:
            return 3, 0
        elif min_res < 150:
            return 4, 0
        else:
            return min(5, num_agents - 2), 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Lower idle-scramble threshold (30 vs 50 in Balanced)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 30:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        return self._miner_action(state, summary_prefix="idle_align_")


class AlphaAdaptiveV4Policy(MettagridSemanticPolicy):
    """AdaptiveV4: Faster ramp + lower idle-scramble threshold."""
    short_names = ["alpha-adaptive-v4"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveV4AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveV5AgentPolicy(AlphaAdaptiveV3AgentPolicy):
    """AdaptiveV5: AdaptiveV3 with NO idle scrambling. Pure align + mine.

    Hypothesis: scrambling wastes hearts that could be used for alignment.
    Against weak opponents, pure alignment might score better.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Never idle-scramble — always mine when idle."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Always mine when idle — no scrambling
        return self._miner_action(state, summary_prefix="idle_align_")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """No dedicated scramblers — save all hearts for alignment."""
        aligner, _ = super()._pressure_budgets(state, objective=objective)
        return aligner, 0  # Zero scramblers always


class AlphaAdaptiveV5Policy(MettagridSemanticPolicy):
    """AdaptiveV5: Pure align + mine, no scrambling."""
    short_names = ["alpha-adaptive-v5"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveV5AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV3AgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV3: v348 but with more aggressive scrambling.

    Lowers idle-mine threshold from 30 to 15, meaning aligners scramble
    more often when idle. Also adds very aggressive economy scaling.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Even more aggressive idle scramble."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble even more aggressively
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 10:  # Was 14 in V2
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Only mine when economy truly struggling
        if min_res < 15:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV3Policy(MettagridSemanticPolicy):
    """TournamentV3: More aggressive scrambling variant of TournamentV2."""
    short_names = ["alpha-tournament-v3"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV3AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV4AgentPolicy(AlphaAdaptiveV4AgentPolicy):
    """TournamentV4: V4 faster ramp + V2 team cap + V2 idle scramble.

    Combines: AdaptiveV4 faster scaling + team-size cap + aggressive scramble.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """V4 budgets + team cap."""
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)

        # Cap by actual sub-team size
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        max_roles = max(team_size - 1, 1)
        total = aligner + scrambler
        if total > max_roles:
            scrambler = min(scrambler, max(0, max_roles - 1))
            aligner = min(aligner, max_roles - scrambler)

        return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """V2's aggressive idle scramble at min_res>=14."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV4Policy(MettagridSemanticPolicy):
    """TournamentV4: V4 faster ramp + V2 team cap + V2 idle scramble."""
    short_names = ["alpha-tournament-v4"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV4AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# Wide exploration offsets for discovering junctions far from hub (covers 88x88 map)
_WIDE_EXPLORE_OFFSETS = (
    (0, -36), (25, -25), (36, 0), (25, 25),
    (0, 36), (-25, 25), (-36, 0), (-25, -25),
    (0, -28), (20, -20), (28, 0), (20, 20),
    (0, 28), (-20, 20), (-28, 0), (-20, -20),
)


class AlphaExplorerV2AgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV2 + wide exploration when frontier is empty.

    Key insight: agents only discover ~14 of ~65 junctions because exploration
    radius is too small (~22). When frontier=0, this policy sends idle aligners
    on wide exploration patterns (radius 36) to discover new junction clusters
    that can extend the network chain.
    """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)
        self._wide_explore_index = 0
        self._frontier_empty_steps = 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TournamentV2 aligner + wide exploration when frontier empty."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            self._frontier_empty_steps = 0
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            self._frontier_empty_steps = 0
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._frontier_empty_steps = 0
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                self._frontier_empty_steps = 0
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # FRONTIER EMPTY — track how long we've been idle
        self._frontier_empty_steps += 1

        # Scramble if we have hearts and economy is OK (like TournamentV2)
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                # Alternate: 2/3 scramble + 1/3 explore to maintain pressure while discovering
                if self._frontier_empty_steps % 3 != 0:
                    return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # WIDE EXPLORATION: explore far from hub to discover new junction clusters
        if hub is not None and hp > 60:
            hub_pos = hub.position
            offset_idx = (self._wide_explore_index + self._agent_id * 3) % len(_WIDE_EXPLORE_OFFSETS)
            ox, oy = _WIDE_EXPLORE_OFFSETS[offset_idx]
            explore_target = (hub_pos[0] + ox, hub_pos[1] + oy)
            if _h.manhattan(current_pos, explore_target) <= 3:
                self._wide_explore_index += 1
                offset_idx = (self._wide_explore_index + self._agent_id * 3) % len(_WIDE_EXPLORE_OFFSETS)
                ox, oy = _WIDE_EXPLORE_OFFSETS[offset_idx]
                explore_target = (hub_pos[0] + ox, hub_pos[1] + oy)
            return self._move_to_position(state, explore_target, summary="wide_explore", vibe="change_vibe_aligner")

        # Low HP or no hub: mine to stay alive
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaExplorerV2Policy(MettagridSemanticPolicy):
    """TournamentV2 + wide exploration when frontier is empty."""
    short_names = ["alpha-explorer-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaExplorerV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCaptureAgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV2 + capture-optimized scramble targeting.

    Key insight: idle-scrambling is 72% of aligner time, but many scrambled
    junctions can't be immediately re-aligned (too far from network).
    This policy prioritizes scrambling enemy junctions within alignment range
    of our network, creating immediate capture (scramble→realign) cycles.
    """

    def _preferred_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        """Override: strongly prefer enemy junctions we can realign after scrambling."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)

        enemy_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )
        if not enemy_junctions:
            return None

        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda j: j.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]

        # Partition: capturable (within alignment range) vs non-capturable
        capturable = []
        non_capturable = []
        for ej in enemy_junctions:
            if _h.within_alignment_network(ej.position, network_sources):
                capturable.append(ej)
            else:
                non_capturable.append(ej)

        # Strongly prefer capturable targets (add -50 bonus)
        hub_pos = hub.position if hub else current_pos
        neutral_junctions = self._world_model.entities(
            entity_type="junction",
            predicate=lambda e: e.owner in {None, "neutral"},
        )

        best = None
        best_score = float("inf")
        for ej in enemy_junctions:
            base_score = _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_pos,
                candidate=ej,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            )[0]
            # Massive bonus for capturable junctions (within our alignment range)
            if ej in capturable:
                base_score -= 50.0
            if base_score < best_score:
                best_score = base_score
                best = ej

        return best


class AlphaCapturePolicy(MettagridSemanticPolicy):
    """TournamentV2 + capture-optimized scramble targeting."""
    short_names = ["alpha-capture"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCaptureAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaCapturePlusAgentPolicy(AlphaCaptureAgentPolicy):
    """Capture + faster heart cycle + lower batch target.

    Reduces heart batch target to 2 (from 3) so aligners spend less time
    at hub and more time in the scramble→realign cycle. With 10000 steps,
    every trip to hub costs ~30-60 steps of lost alignment time.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Lower batch threshold: go align with 2 hearts instead of waiting for 3."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Skip batching entirely in early game
        if step < 200:
            pass
        elif hearts < 2 and hub is not None:
            # Only batch to 2 hearts (not 3-6 like default)
            hub_dist = _h.manhattan(_h.absolute_position(state), hub.position)
            if hub_dist <= 1 and _h.team_can_refill_hearts(state):
                return self._move_to_known(state, hub, summary="quick_batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble (capture-optimized) at lower threshold
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 10:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaCapturePlusPolicy(MettagridSemanticPolicy):
    """Capture-optimized + faster heart cycle."""
    short_names = ["alpha-capture-plus"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCapturePlusAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV5AgentPolicy(AlphaCaptureAgentPolicy):
    """TournamentV5: Capture targeting + faster 4a ramp + lower scramble threshold.

    Optimized for tournament where opponents are weaker than self-play:
    1. Capture-optimized scramble targeting (from AlphaCapture)
    2. For 4a: start 2 aligners at step 50 (not 100), 3 at min_res >= 50
    3. Lower idle-scramble threshold: min_res >= 10 (from 14)
    4. Budget: all-mine phase only until can_hearts (step ~80), then max pressure
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Faster ramp for 4a, otherwise same as TournamentV2."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            # Faster ramp: 2 aligners as soon as hearts available
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 15:
                return 1, 0
            aligner_budget = 2
            # More aggressive 3-aligner threshold
            if min_res >= 50 and step >= 300:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        # 5+ agents: delegate to parent (AdaptiveV3 + team cap)
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)
        return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Lower scramble threshold + capture targeting (inherited)."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble at lower threshold (10 vs 14) — opponents are weak in tournament
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 10:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV5Policy(MettagridSemanticPolicy):
    """TournamentV5: Capture + faster 4a ramp + lower scramble threshold."""
    short_names = ["alpha-tournament-v5"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV5AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaPatrolAgentPolicy(AlphaCaptureAgentPolicy):
    """TournamentV2 + patrol mode: idle aligners stay near network edges.

    Instead of scrambling when idle, 1-2 agents patrol near the outermost
    friendly junctions to re-align quickly when enemy scrambles them.
    Other agents still idle-scramble for offensive pressure.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Mix of patrol (defense) and scramble (offense) when idle."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # IDLE: mix patrol and scramble based on agent ID
        # Even agents: patrol (defensive), Odd agents: scramble (offensive)
        min_res = _h.team_min_resource(state)
        team_id = _h.team_id(state)
        is_patrol_agent = (self._agent_id % 2 == 0)

        if is_patrol_agent and hearts > 0:
            # Patrol near outermost friendly junctions
            friendly = self._known_junctions(state, predicate=lambda j: j.owner == team_id)
            if friendly and hub is not None:
                hub_pos = hub.position
                # Find the outermost friendly junction (farthest from hub)
                outermost = max(friendly, key=lambda j: _h.manhattan(hub_pos, j.position))
                dist_to_patrol = _h.manhattan(current_pos, outermost.position)
                if dist_to_patrol > 5:
                    return self._move_to_known(state, outermost, summary="patrol_junction", vibe="change_vibe_aligner")
                # Already near patrol point: try scramble nearby enemy junctions
                if min_res >= 14:
                    scramble_target = self._preferred_scramble_target(state)
                    if scramble_target is not None:
                        # Only scramble nearby targets (within 20)
                        if _h.manhattan(current_pos, scramble_target.position) <= 20:
                            return self._move_to_known(state, scramble_target, summary="patrol_scramble", vibe="change_vibe_scrambler")
                return self._explore_action(state, role="aligner", summary="patrol_explore")

        # Odd agents or no hearts: standard idle scramble (like TournamentV2)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaPatrolPolicy(MettagridSemanticPolicy):
    """TournamentV2 + patrol mode for defensive junction holding."""
    short_names = ["alpha-patrol"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaPatrolAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaMaxPressure4aAgentPolicy(AlphaCaptureAgentPolicy):
    """Maximum pressure for 4a: 4 aligners + 0 miners when economy allows.

    With 4 agents, keeping 1 miner means only 3 aligners scramble/align.
    If economy is healthy (min_res > 50), switch to 4 aligners for max throughput.
    The idle-mine fallback in aligner_action handles economy maintenance.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """4a: go to 4 aligners when economy healthy."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            # More aggressive: 3 aligners at min_res >= 30
            aligner_budget = min(3, num_agents - 1)
            # At min_res >= 100 AND step >= 500: ALL aligners (0 dedicated miners)
            if min_res >= 100 and step >= 500:
                aligner_budget = num_agents  # All 4 agents = aligners
            return aligner_budget, 0

        # 5+ agents: delegate to parent (TournamentV2 + capture)
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)
        return aligner, scrambler


class AlphaMaxPressure4aPolicy(MettagridSemanticPolicy):
    """Max pressure 4a: 4 aligners when economy allows."""
    short_names = ["alpha-max-pressure-4a"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaMaxPressure4aAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveMapAgentPolicy(AlphaCaptureAgentPolicy):
    """Adapts behavior based on map quality (junction density near hub).

    After step 500, counts total known junctions. If few junctions nearby
    (<8), switches to wider exploration to find distant clusters. On good
    maps, behaves like TournamentV2 (pure scramble+realign).

    This should help on bad maps without hurting good maps.
    """

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        super().__init__(*args, **kwargs)
        self._map_quality = "unknown"  # "good", "bad", "unknown"
        self._wide_explore_index = 0

    def _assess_map_quality(self, state: MettagridState) -> str:
        """Detect good vs bad map based on junction count after step 500."""
        step = state.step or self._step_index
        if step < 500:
            return "unknown"
        team_id = _h.team_id(state)
        total_known = len(self._known_junctions(
            state, predicate=lambda j: True  # all junctions
        ))
        friendly = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        # Good map: we can see 12+ junctions by step 500
        # Bad map: < 8 total known
        if total_known >= 12:
            return "good"
        if total_known < 8:
            return "bad"
        return "ok"

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Adapt idle behavior based on map quality."""
        step = state.step or self._step_index
        self._map_quality = self._assess_map_quality(state)

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward known unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # BAD MAP: alternate explore and scramble to find more junctions
        if self._map_quality == "bad" and hp > 60 and hub is not None:
            # 50% explore, 50% scramble
            if step % 2 == 0:
                hub_pos = hub.position
                offset_idx = (self._wide_explore_index + self._agent_id * 3) % len(_WIDE_EXPLORE_OFFSETS)
                ox, oy = _WIDE_EXPLORE_OFFSETS[offset_idx]
                explore_target = (hub_pos[0] + ox, hub_pos[1] + oy)
                if _h.manhattan(current_pos, explore_target) <= 3:
                    self._wide_explore_index += 1
                    offset_idx = (self._wide_explore_index + self._agent_id * 3) % len(_WIDE_EXPLORE_OFFSETS)
                    ox, oy = _WIDE_EXPLORE_OFFSETS[offset_idx]
                    explore_target = (hub_pos[0] + ox, hub_pos[1] + oy)
                return self._move_to_position(state, explore_target, summary="bad_map_explore", vibe="change_vibe_aligner")

        # GOOD or OK MAP: standard idle scramble (proven strategy)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaCoopAgentPolicy2(AlphaTournamentV2AgentPolicy):
    """Cooperative-aware: only scramble Clips junctions, never opponent Cogs.

    CRITICAL INSIGHT: Tournament scoring is COOPERATIVE (both Cogs teams
    share the same score). Scrambling opponent Cogs junctions is wasteful
    (temporarily reduces shared score). Only scramble Clips/non-Cogs junctions
    to convert them to Cogs (net +1 for shared score).
    """

    def _preferred_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        """Only target non-Cogs junctions (Clips) for scrambling."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)

        # Get ALL enemy junctions (not our team, not neutral)
        all_enemy = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )
        if not all_enemy:
            return None

        # Filter: only keep non-Cogs junctions (i.e., Clips junctions)
        # Cogs team IDs are typically "cogs" or "red.X" patterns
        # We filter OUT any junction owned by a team that contains "cog" or "red"
        clips_only = [
            j for j in all_enemy
            if j.owner and "cog" not in str(j.owner).lower() and "red" not in str(j.owner).lower()
        ]

        # If no Clips junctions available, fall back to all enemy (including Cogs)
        # This handles the case where Clips own 0 junctions
        targets = clips_only if clips_only else all_enemy

        if not targets:
            return None

        hub_pos = hub.position if hub else current_pos
        neutral_junctions = self._world_model.entities(
            entity_type="junction",
            predicate=lambda e: e.owner in {None, "neutral"},
        )
        friendly_junctions = self._known_junctions(state, predicate=lambda j: j.owner == team_id)

        return min(
            targets,
            key=lambda entity: _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_pos,
                candidate=entity,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            ),
        )


class AlphaCoopPolicy2(MettagridSemanticPolicy):
    """Cooperative-aware: scramble only Clips, not opponent Cogs."""
    short_names = ["alpha-coop-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaCoopAgentPolicy2(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveMapPolicy(MettagridSemanticPolicy):
    """Adapts exploration vs scramble based on map junction density."""
    short_names = ["alpha-adaptive-map"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveMapAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# --- Wide-explore exploration offsets ---
# Inner ring (original): 8 points at ~20 tiles from hub
# Outer ring: 8 points at ~35 tiles from hub (covers more of 88x88 map)
_WIDE_EXPLORE_OFFSETS = (
    # Inner ring
    (0, -22), (16, -16), (22, 0), (16, 16),
    (0, 22), (-16, 16), (-22, 0), (-16, -16),
    # Outer ring - reach further into map corners
    (0, -38), (27, -27), (38, 0), (27, 27),
    (0, 38), (-27, 27), (-38, 0), (-27, -27),
)


class AlphaWideExploreAgentPolicy(AlphaTournamentV2AgentPolicy):
    """TV2 + wider exploration to discover more junctions.

    Key change: When idle (no frontier), alternate between scrambling and
    exploring a WIDER pattern (inner + outer ring). This discovers more of
    the ~65 junctions on the 88x88 map while maintaining scramble pressure.

    Also uses a dedicated explore phase: every 3rd idle cycle, explore
    instead of scramble to continuously discover new junction clusters.
    """

    def _explore_action(self, state: MettagridState, *, role: str, summary: str) -> tuple[Action, str]:
        """Use wider exploration pattern for aligners."""
        if role != "aligner":
            return super()._explore_action(state, role=role, summary=summary)

        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _WIDE_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary=summary, vibe=_h.role_vibe(role))

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV2 aligner with explore-first idle behavior."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # KEY CHANGE: alternate between scramble and explore when idle
        # Every 3rd idle cycle, explore instead of scramble to discover new junctions
        min_res = _h.team_min_resource(state)
        idle_cycle = (step // 50) % 3  # cycles every 150 steps

        if idle_cycle < 2:
            # 2/3 of time: scramble (proven critical for tournament)
            if hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")
        else:
            # 1/3 of time: explore wider to discover new junction clusters
            if hp > 40:
                return self._explore_action(state, role="aligner", summary="idle_wide_explore")

        # Fallback: scramble if explore phase skipped, or mine if economy tight
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaWideExplorePolicy(MettagridSemanticPolicy):
    """TV2 + wider exploration pattern for better junction discovery."""
    short_names = ["alpha-wide-explore"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaWideExploreAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaExploreFirstAgentPolicy(AlphaTournamentV2AgentPolicy):
    """TV2 but prioritizes exploration over scrambling when idle.

    Instead of 72% idle-scrambling, invest that time in exploring to
    discover more junctions. Only scramble when resources are very high.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Explore-first: discover junctions before scrambling."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # EXPLORE FIRST: prioritize discovering new junctions
        # Only scramble after step 1000 when economy is very strong
        min_res = _h.team_min_resource(state)

        if step < 1000 or min_res < 50:
            # Early/mid game: explore to discover junctions
            if hp > 30:
                return self._explore_action(state, role="aligner", summary="idle_explore_discover")

        # Late game with strong economy: scramble
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaExploreFirstPolicy(MettagridSemanticPolicy):
    """TV2 + explore-first when idle (less scrambling, more discovery)."""
    short_names = ["alpha-explore-first"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaExploreFirstAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaChainBuilderAgentPolicy(AlphaTournamentV2AgentPolicy):
    """TV2 + aggressive chain building toward far junctions.

    When idle, instead of scrambling, move toward the nearest unreachable
    neutral junction. If we align it AND it's near other unreachable ones,
    our network grows. Prioritize junctions with high "expansion_value"
    (number of currently-unreachable junctions they'd unlock).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV2 base + chain-building idle behavior."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        # CHAIN BUILDING: prefer junctions that unlock the most unreachable ones
        target = self._best_chain_target(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="chain_align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable neutral junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            # Prefer junctions that would unlock the most others
            def expansion_value(j: KnownEntity) -> int:
                return sum(
                    1 for other in unreachable
                    if other.position != j.position
                    and _h.manhattan(j.position, other.position) <= _h._JUNCTION_ALIGN_DISTANCE
                )
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            if safe_unreachable:
                # Pick the one with highest expansion value, tie-break by distance
                best = max(safe_unreachable, key=lambda j: (expansion_value(j), -_h.manhattan(current_pos, j.position)))
                return self._move_to_known(state, best, summary="chain_expand_junction", vibe="change_vibe_aligner")

        # Idle: scramble (still important)
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _best_chain_target(self, state: MettagridState) -> KnownEntity | None:
        """Pick alignable frontier junction that unlocks the most unreachable ones."""
        team_id = _h.team_id(state)
        hub = self._nearest_hub(state)
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda e: e.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]

        frontier = []
        all_neutral = self._known_junctions(state, predicate=lambda j: j.owner in {None, "neutral"})
        unreachable = []
        for j in all_neutral:
            if _h.within_alignment_network(j.position, network_sources):
                frontier.append(j)
            else:
                unreachable.append(j)

        if not frontier:
            return None

        current_pos = _h.absolute_position(state)
        hub_pos = hub.position if hub else None

        # Score each frontier junction by expansion value
        def chain_score(candidate: KnownEntity) -> float:
            expansion = sum(
                1 for u in unreachable
                if _h.manhattan(candidate.position, u.position) <= _h._JUNCTION_ALIGN_DISTANCE
            )
            dist = _h.manhattan(current_pos, candidate.position)
            claimed = _h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            )
            # Expansion value is primary, distance is secondary
            # Negative because min() is used
            return (-expansion * 100 + dist + (50 if claimed else 0))

        directed = self._directive_target_candidate(frontier)
        if directed is not None:
            return directed

        return min(frontier, key=chain_score)


class AlphaChainBuilderPolicy(MettagridSemanticPolicy):
    """TV2 + chain-building: prefer frontier junctions that unlock the most others."""
    short_names = ["alpha-chain-builder"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaChainBuilderAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaScoutExploreAgentPolicy(AlphaTournamentV2AgentPolicy):
    """TV2 but dedicates 1 miner to scouting instead.

    On maps where we only discover 14/65 junctions, having a scout
    that permanently explores can double junction discovery. The
    scout brings back junction locations to the shared world model.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Same as TV2 but ensure at least 2 miners (1 will be converted to scout)."""
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)
        return aligner, scrambler

    def _desired_role(self, state: MettagridState, *, objective: str | None = None) -> str:
        """Convert the highest-ID agent to permanent scout role."""
        team_ids = self._shared_team_ids
        if team_ids:
            max_id = max(team_ids)
            if self._agent_id == max_id:
                return "scout"
        return super()._desired_role(state, objective=objective)

    def _choose_action(self, state: MettagridState, role: str) -> tuple[Action, str]:
        """Scout role: permanently explore the map to discover junctions."""
        if role == "scout":
            return self._scout_action(state)
        return super()._choose_action(state, role)

    def _scout_action(self, state: MettagridState) -> tuple[Action, str]:
        """Scout explores a wide pattern to discover junctions."""
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))

        # If low HP, go deposit and heal
        if hp < 30:
            hub = self._nearest_hub(state)
            if hub is not None:
                return self._move_to_known(state, hub, summary="scout_heal", vibe="change_vibe_heart")

        # If carrying resources, deposit
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="scout_deposit", vibe="change_vibe_aligner")

        # Explore wide pattern
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _WIDE_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="scout_explore", vibe="change_vibe_aligner")


class AlphaScoutExplorePolicy(MettagridSemanticPolicy):
    """TV2 + 1 dedicated scout for junction discovery."""
    short_names = ["alpha-scout-explore"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScoutExploreAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# Dense spiral exploration: covers the full 88x88 map systematically
# 13x13 observation = 6 tiles in each direction, so spacing of ~12 gives full coverage
_SPIRAL_EXPLORE_OFFSETS = (
    # Ring 1: ~12 tiles from hub
    (0, -12), (12, 0), (0, 12), (-12, 0),
    # Ring 2: ~24 tiles
    (0, -24), (17, -17), (24, 0), (17, 17),
    (0, 24), (-17, 17), (-24, 0), (-17, -17),
    # Ring 3: ~36 tiles (covers far corners of 88x88)
    (0, -36), (25, -25), (36, 0), (25, 25),
    (0, 36), (-25, 25), (-36, 0), (-25, -25),
    # Ring 4: ~44 tiles (map edge for center hubs)
    (0, -44), (31, -31), (44, 0), (31, 31),
    (0, 44), (-31, 31), (-44, 0), (-31, -31),
)


class AlphaScoutV2AgentPolicy(AlphaScoutExploreAgentPolicy):
    """Scout V2: denser spiral pattern for better map coverage.

    The scout uses a 4-ring spiral pattern spaced 12 tiles apart,
    which matches the 13x13 observation window for full coverage.
    """

    def _scout_action(self, state: MettagridState) -> tuple[Action, str]:
        """Scout uses dense spiral pattern."""
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))

        # If low HP, heal
        if hp < 30:
            hub = self._nearest_hub(state)
            if hub is not None:
                return self._move_to_known(state, hub, summary="scout_heal", vibe="change_vibe_heart")

        # If carrying resources, deposit (help economy while scouting)
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="scout_deposit", vibe="change_vibe_aligner")

        # If scout finds an alignable junction nearby, align it!
        hearts = int(state.self_state.inventory.get("heart", 0))
        if hearts > 0:
            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                dist = _h.manhattan(current_pos, target.position)
                if dist <= 10:  # Only align if close (don't abandon scouting)
                    return self._move_to_known(state, target, summary="scout_align_nearby", vibe="change_vibe_aligner")

        # Dense spiral exploration
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _SPIRAL_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target_offset = offsets[offset_index]
        absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target_offset = offsets[offset_index]
            absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        return self._move_to_position(state, absolute_target, summary="scout_spiral", vibe="change_vibe_aligner")


class AlphaScoutV2Policy(MettagridSemanticPolicy):
    """TV2 + scout with dense spiral pattern."""
    short_names = ["alpha-scout-v2"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScoutV2AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaScoutChainAgentPolicy(AlphaScoutV2AgentPolicy):
    """ScoutV2 + chain-building: prefer junctions that unlock others.

    Combines the best of both approaches:
    - 1 dedicated scout for junction discovery (proven +28%)
    - Aligners prefer frontier junctions that expand the network
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV2 aligner with chain-building targeting."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        # Use chain-building targeting: prefer junctions that unlock others
        target = self._chain_or_normal_target(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="chain_align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble (proven critical)
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _chain_or_normal_target(self, state: MettagridState) -> KnownEntity | None:
        """Pick alignable junction, preferring ones that expand the network."""
        team_id = _h.team_id(state)
        hub = self._nearest_hub(state)
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda e: e.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]

        all_neutral = self._known_junctions(state, predicate=lambda j: j.owner in {None, "neutral"})
        frontier = []
        unreachable = []
        for j in all_neutral:
            if _h.within_alignment_network(j.position, network_sources):
                frontier.append(j)
            else:
                unreachable.append(j)

        if not frontier:
            return None

        current_pos = _h.absolute_position(state)
        hub_pos = hub.position if hub else None

        # Score: prefer junctions that unlock more unreachable ones
        def chain_score(candidate: KnownEntity) -> tuple:
            expansion = sum(
                1 for u in unreachable
                if _h.manhattan(candidate.position, u.position) <= _h._JUNCTION_ALIGN_DISTANCE
            )
            dist = _h.manhattan(current_pos, candidate.position)
            claimed = _h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            )
            return (-expansion, dist + (50 if claimed else 0))

        directed = self._directive_target_candidate(frontier)
        if directed is not None:
            return directed

        return min(frontier, key=chain_score)


class AlphaScoutChainPolicy(MettagridSemanticPolicy):
    """ScoutV2 + chain-building alignment targeting."""
    short_names = ["alpha-scout-chain"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScoutChainAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTwoScoutsAgentPolicy(AlphaScoutV2AgentPolicy):
    """TV2 with 2 dedicated scouts instead of 1.

    If 1 scout gave +28%, 2 scouts might discover even more junctions.
    Trade-off: fewer miners but more junction discovery.
    """

    def _desired_role(self, state: MettagridState, *, objective: str | None = None) -> str:
        """Convert the 2 highest-ID agents to permanent scout role."""
        team_ids = self._shared_team_ids
        if team_ids and len(team_ids) >= 4:
            sorted_ids = sorted(team_ids)
            if self._agent_id in sorted_ids[-2:]:
                return "scout"
        elif team_ids:
            max_id = max(team_ids)
            if self._agent_id == max_id:
                return "scout"
        return super(AlphaScoutV2AgentPolicy, self)._desired_role(state, objective=objective)


class AlphaTwoScoutsPolicy(MettagridSemanticPolicy):
    """TV2 + 2 dedicated scouts for maximum junction discovery."""
    short_names = ["alpha-two-scouts"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTwoScoutsAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSmartScoutAgentPolicy(AlphaTwoScoutsAgentPolicy):
    """TwoScouts but scouts use original (proven) wide explore pattern,
    AND scouts grab hearts + align junctions they find on the way.

    The ScoutV2 dense spiral pattern performed WORSE than original wide pattern.
    So use the proven _WIDE_EXPLORE_OFFSETS but give scouts aligner ability.
    """

    def _scout_action(self, state: MettagridState) -> tuple[Action, str]:
        """Scout that also aligns junctions found along the way."""
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hearts = int(state.self_state.inventory.get("heart", 0))

        # If low HP, heal
        if hp < 30:
            hub = self._nearest_hub(state)
            if hub is not None:
                return self._move_to_known(state, hub, summary="scout_heal", vibe="change_vibe_heart")

        # If carrying resources, deposit
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="scout_deposit", vibe="change_vibe_aligner")

        # If no hearts, grab one when near hub
        if hearts <= 0:
            hub = self._nearest_hub(state)
            if hub is not None and _h.manhattan(current_pos, hub.position) <= 15:
                if _h.team_can_refill_hearts(state):
                    return self._move_to_known(state, hub, summary="scout_get_heart", vibe="change_vibe_heart")

        # If scout has hearts and finds an alignable junction close by, align it!
        if hearts > 0:
            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                dist = _h.manhattan(current_pos, target.position)
                if dist <= 15:  # Align if relatively close
                    return self._move_to_known(state, target, summary="scout_align", vibe="change_vibe_aligner")

        # If scout sees an enemy junction close by and has hearts, scramble it
        if hearts > 0:
            scramble = self._preferred_scramble_target(state)
            if scramble is not None:
                dist = _h.manhattan(current_pos, scramble.position)
                if dist <= 8:  # Only scramble if very close
                    return self._move_to_known(state, scramble, summary="scout_scramble", vibe="change_vibe_scrambler")

        # Explore using the ORIGINAL wide pattern (proven better than dense spiral)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _WIDE_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target_offset = offsets[offset_index]
        absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target_offset = offsets[offset_index]
            absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        return self._move_to_position(state, absolute_target, summary="scout_explore", vibe="change_vibe_aligner")


class AlphaSmartScoutPolicy(MettagridSemanticPolicy):
    """TwoScouts + scouts align junctions they discover."""
    short_names = ["alpha-smart-scout"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSmartScoutAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaThreeScoutsAgentPolicy(AlphaTwoScoutsAgentPolicy):
    """TV2 with 3 dedicated scouts."""

    def _desired_role(self, state: MettagridState, *, objective: str | None = None) -> str:
        """Convert the 3 highest-ID agents to permanent scout role."""
        team_ids = self._shared_team_ids
        if team_ids and len(team_ids) >= 6:
            sorted_ids = sorted(team_ids)
            if self._agent_id in sorted_ids[-3:]:
                return "scout"
        elif team_ids and len(team_ids) >= 4:
            sorted_ids = sorted(team_ids)
            if self._agent_id in sorted_ids[-2:]:
                return "scout"
        elif team_ids:
            if self._agent_id == max(team_ids):
                return "scout"
        return super(AlphaTwoScoutsAgentPolicy, self)._desired_role(state, objective=objective)


class AlphaThreeScoutsPolicy(MettagridSemanticPolicy):
    """TV2 + 3 dedicated scouts."""
    short_names = ["alpha-three-scouts"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaThreeScoutsAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaAdaptiveScoutAgentPolicy(AlphaScoutExploreAgentPolicy):
    """Adaptive scouting: only scout when team has enough agents.

    Scouting costs 1 agent (no mining/alignment). Data shows:
    - 8 agents: Scout is +28% (1 of 8 = 12.5% overhead)
    - 4 agents: Scout is -62% at 10K! (1 of 4 = 25% overhead → economy collapse)
    - 2 agents: Never scout (50% overhead)

    So only scout with 6+ agents where the overhead is manageable.
    """

    def _desired_role(self, state: MettagridState, *, objective: str | None = None) -> str:
        """Only scout when team has 6+ agents."""
        team_ids = self._shared_team_ids
        if team_ids and len(team_ids) >= 6:
            max_id = max(team_ids)
            if self._agent_id == max_id:
                return "scout"
        return super(AlphaScoutExploreAgentPolicy, self)._desired_role(state, objective=objective)


class AlphaAdaptiveScoutPolicy(MettagridSemanticPolicy):
    """TV2 + adaptive scouting (only with 4+ agents)."""
    short_names = ["alpha-adaptive-scout"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaAdaptiveScoutAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaScoutPlusAgentPolicy(AlphaScoutExploreAgentPolicy):
    """ScoutExplore + aggressive idle scrambling after early discovery.

    Scouts discover junctions early. After step 1500, if scout has
    visited all exploration points, it switches to scrambling idle.
    This combines early discovery advantage with late-game pressure.
    """

    def _scout_action(self, state: MettagridState) -> tuple[Action, str]:
        """Scout with phase transition: explore early, scramble late."""
        step = state.step or self._step_index
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hearts = int(state.self_state.inventory.get("heart", 0))

        # If low HP, heal
        if hp < 30:
            hub = self._nearest_hub(state)
            if hub is not None:
                return self._move_to_known(state, hub, summary="scout_heal", vibe="change_vibe_heart")

        # If carrying resources, deposit
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="scout_deposit", vibe="change_vibe_aligner")

        # Phase transition: after step 2000, switch to aligner behavior
        if step >= 2000:
            # Scout has explored enough; become a regular aligner
            return self._aligner_action(state)

        # Early game: explore wide pattern
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _WIDE_EXPLORE_OFFSETS
        offset_index = (self._explore_index + self._agent_id) % len(offsets)
        target_offset = offsets[offset_index]
        absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target_offset = offsets[offset_index]
            absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
        return self._move_to_position(state, absolute_target, summary="scout_explore", vibe="change_vibe_aligner")


class AlphaScoutPlusPolicy(MettagridSemanticPolicy):
    """ScoutExplore + scout transitions to aligner after step 2000."""
    short_names = ["alpha-scout-plus"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaScoutPlusAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaSustainV4AgentPolicy(AlphaTournamentV2AgentPolicy):
    """SustainV4: Fix silicon depletion that crashes economy after step 5000.

    Key changes from TournamentV2:
    1. Always-silicon mining bias (silicon has fewest extractors: 45 vs 50-58)
    2. Silicon-aware budget scaling — reduce aligners when silicon < 150
    3. More conservative late-game: cap aligners at 4 (not 5) for 8-agent
    4. Better idle behavior: mine silicon when idle instead of scrambling
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Always-silicon bias: silicon is the structural bottleneck (fewest extractors)."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        carbon = resources.get("carbon", 0)

        # Silicon is almost always the bottleneck due to fewer extractors
        # Only switch to carbon if carbon is critically lower
        if carbon < silicon - 50:
            return MacroDirective(resource_bias="carbon")
        return MacroDirective(resource_bias="silicon")

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Silicon-aware budgets: pull back aligners when silicon is dropping."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # 2 agents: mine first 150 steps (not 200), then 1 aligner
            if step < 150 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if silicon < 30:
                return 1, 0  # Silicon crisis: pull back hard
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and silicon >= 80 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        # 5+ agents: silicon-gated scaling
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif silicon < 50:
            # Silicon crisis: max 2 aligners, rest mine
            return 2, 0
        elif silicon < 150 or min_res < 30:
            return 2, 0
        elif silicon < 300:
            return 3, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 500 else 0
            return 3, scrambler
        else:
            # Rich: moderate pressure, keep 4+ miners for sustainability
            scrambler = 1 if step >= 500 else 0
            aligner = min(4, num_agents - 3 - scrambler)
            return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Idle aligners mine silicon instead of scrambling when economy is tight."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: prioritize economy sustain over scrambling
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        min_res = _h.team_min_resource(state)

        # Only scramble when economy is very healthy AND silicon is abundant
        if hearts > 0 and min_res >= 14 and silicon >= 100:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Mine when economy needs help (prioritize silicon via macro_directive)
        if min_res < 50 or silicon < 200:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaSustainV4Policy(MettagridSemanticPolicy):
    """SustainV4: Silicon-focused economy sustain for long games."""
    short_names = ["alpha-sustain-v4"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaSustainV4AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV6AgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV6: TV2 base + smarter economy awareness.

    Key changes from TournamentV2:
    1. Dynamic idle-scramble threshold based on silicon reserves
    2. Faster 2-agent ramp (mine 100 steps not 200)
    3. When economy weakening, idle aligners mine instead of scramble
    4. Higher budget threshold for 5th aligner
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV2 budgets + silicon-aware 5th aligner gate."""
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)

        # Gate the 5th aligner on silicon reserves
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        if aligner >= 5 and silicon < 200:
            aligner = 4

        return aligner, scrambler

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV2 aligner with economy-adaptive idle behavior."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: economy-adaptive scramble threshold
        min_res = _h.team_min_resource(state)
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)

        # Scramble only when silicon is healthy
        scramble_threshold = 14 if silicon >= 150 else 50
        if hearts > 0 and min_res >= scramble_threshold:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Mine when economy tight
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV6Policy(MettagridSemanticPolicy):
    """TournamentV6: TV2 base + silicon-aware economy management."""
    short_names = ["alpha-tournament-v6"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV6AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV7AgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV7: TV2 + 2-agent cooperation optimization.

    Key insight: With 2 agents, we're paired with 6 opponent agents on our team.
    Our best strategy is to be excellent miners/supporters, not to try to align.

    Changes from TV2:
    1. 2-agent: Both mine for first 500 steps (build economy for the team)
    2. 2-agent: After 500, 1 aligner if economy healthy, else keep mining
    3. 4-agent: Same as TV2 but start alignment at step 50 (faster ramp)
    4. 6-agent: Same as TV2 but explore more aggressively when idle
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV2 budgets + 2-agent optimization."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # With 2 agents, the other 6 handle alignment
            # We focus on economy support for longer
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0  # Pure mining team support
            # Only align if economy is very healthy
            if min_res >= 50:
                return 1, 0
            return 0, 0  # Keep mining to support the team

        # 4+ agents: use parent's budgets with team cap
        return super()._pressure_budgets(state, objective=objective)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """More aggressive explore when idle to discover junction chains."""
        # Use parent's aligner action but with enhanced idle behavior
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle: scramble aggressively (opponents are weak in tournament)
        min_res = _h.team_min_resource(state)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Alternate between exploring and mining when idle
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV7Policy(MettagridSemanticPolicy):
    """TournamentV7: TV2 base + 2-agent cooperation + explore idle."""
    short_names = ["alpha-tournament-v7"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV7AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV8AgentPolicy(AlphaTournamentV2AgentPolicy):
    """TournamentV8: TV2 + wider exploration + more explore when idle.

    Key insight: The 88x88 map has junctions far from hub. Current explore
    offsets only reach radius 22. Wider exploration discovers more junctions
    which allows more alignment.

    Changes:
    1. Wider explore pattern for idle aligners (radius 30-40)
    2. When idle with healthy economy, explore instead of scramble
    3. Keep scrambling but alternate with exploration
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV2 aligner but with exploration-first idle behavior."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions (wider range)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 15  # Less conservative
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 15:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Idle behavior: alternate between scramble and explore
        min_res = _h.team_min_resource(state)
        phase = (step // 100) % 3  # 0=scramble, 1=explore, 2=explore

        if phase == 0 and hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        # Explore using wider pattern to discover more junctions
        if hp > 60:
            return self._wide_explore_action(state)

        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _wide_explore_action(self, state: MettagridState) -> tuple[Action, str]:
        """Wider exploration pattern to discover distant junctions."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        # Wider offsets covering more of the 88x88 map
        wide_offsets = (
            (0, -35), (25, -25), (35, 0), (25, 25),
            (0, 35), (-25, 25), (-35, 0), (-25, -25),
            (0, -20), (15, -15), (20, 0), (15, 15),
            (0, 20), (-15, 15), (-20, 0), (-15, -15),
        )
        offset_index = (self._explore_index + self._agent_id * 3) % len(wide_offsets)
        target = wide_offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 3) % len(wide_offsets)
            target = wide_offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="wide_explore", vibe="change_vibe_aligner")


class AlphaTournamentV8Policy(MettagridSemanticPolicy):
    """TournamentV8: TV2 + wider exploration for distant junction discovery."""
    short_names = ["alpha-tournament-v8"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV8AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


class AlphaTournamentV9AgentPolicy(AlphaTournamentV7AgentPolicy):
    """TournamentV9: TV7 + fix 2-agent timing.

    TV7's 2-agent behavior (mine 500 steps, align only if min_res >= 50)
    is too conservative against weak teammates. Fix:
    1. Start alignment at step 200 (not 500) — same as TV2 baseline
    2. Always allocate 1 aligner after step 200, regardless of economy
    3. Keep TV7's idle behavior improvements for 4+ agent games
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV7 budgets but with fixed 2-agent timing."""
        num_agents = self.policy_env_info.num_agents

        if num_agents <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            # Standard TV2 timing: align after 200 steps
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        # 4+ agents: use TV7/TV2 parent budgets
        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV9Policy(MettagridSemanticPolicy):
    """TournamentV9: TV7 + fixed 2-agent timing."""
    short_names = ["alpha-tournament-v9"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV9AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV10: Double scramble pressure + faster ramp ─────────────────────────

class AlphaTournamentV10AgentPolicy(AlphaTournamentV7AgentPolicy):
    """TournamentV10: TV7 + more scramblers + faster ramp + silicon focus.

    Analysis shows junction count drops from 21→12 while Clips grows from 0→13
    between steps 1000-5000. Silicon runs out (377→80) killing heart production.

    Changes from TV7:
    1. 2 scramblers instead of 1 for 6+ agents — slows Clips expansion
    2. Faster initial ramp: 3 aligners at step 30 for 8 agents
    3. Silicon-priority mining when silicon < 150 (crisis threshold)
    4. Keep TV7's 2-agent mining optimization
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV7 budgets + double scramble + faster ramp."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: same as TV7
        if num_agents <= 2:
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            # 4 agents: same as TV2/AdaptiveV3
            return super()._pressure_budgets(state, objective=objective)

        # 6+ agents: faster ramp + double scramble
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents
        max_roles = max(team_size - 2, 1)  # Keep at least 2 miners

        if step < 30:
            return min(3, max_roles), 0  # Faster: 3 aligners immediately

        if min_res < 10 and not can_hearts:
            return 1, 0  # Crisis

        if min_res < 30:
            return min(2, max_roles), 0

        # 2 scramblers when economy permits
        scrambler = 0
        if step >= 300 and min_res >= 14:
            scrambler = min(2, max_roles - 2)
        elif step >= 200 and min_res >= 7:
            scrambler = 1

        if min_res < 80:
            aligner = min(3, max_roles - scrambler)
        elif min_res < 200:
            aligner = min(4, max_roles - scrambler)
        else:
            aligner = min(5, max_roles - scrambler)

        return aligner, scrambler

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining when silicon gets low."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        if silicon < 150:
            return MacroDirective(resource_bias="silicon")
        least = _least_resource(resources)
        least_amount = resources[least]
        if silicon <= least_amount + 20:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV10Policy(MettagridSemanticPolicy):
    """TournamentV10: TV7 + double scramble + faster ramp + silicon focus."""
    short_names = ["alpha-tournament-v10"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV10AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV11: Early burst exploration + chain push ────────────────────────────

# Systematic grid covering the 88x88 map (offsets from hub)
# 20 waypoints: traverse in sequence to discover junctions across the map
_SYSTEMATIC_EXPLORE_GRID = (
    # Inner ring (radius ~15) — quick discovery near hub
    (15, 0), (0, 15), (-15, 0), (0, -15),
    # Mid ring (radius ~25) — where alignment network starts
    (20, -20), (20, 20), (-20, 20), (-20, -20),
    # Outer ring (radius ~35) — distant junctions
    (35, 0), (0, 35), (-35, 0), (0, -35),
    (25, -25), (25, 25), (-25, 25), (-25, -25),
    # Far corners (radius ~40) — map edges
    (40, -10), (10, -40), (-40, 10), (-10, 40),
)


class AlphaTournamentV11AgentPolicy(AlphaTournamentV9AgentPolicy):
    """TournamentV11: TV9 + early burst exploration + chain push.

    Key insight: Score is avg junctions/tick, so early discovery is
    disproportionately valuable. Maps vary wildly in junction accessibility.

    Changes from TV9:
    1. Last miner agent systematically explores the map for first 400 steps
       instead of mining, feeding junctions into shared world model
    2. After aligning a frontier junction, aligners explore in that direction
       for up to 20 steps to discover chain extensions
    3. TV9 base for budgets and 2-agent behavior
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._explore_waypoint_idx = 0
        self._chain_push_steps = 0
        self._chain_push_direction: tuple[int, int] | None = None
        self._last_aligned_position: tuple[int, int] | None = None

    def _miner_action(self, state: MettagridState, summary_prefix: str = "") -> tuple[Action, str]:
        """Last agent: systematic map exploration for first 400 steps."""
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents
        # Use highest-ID agent as explorer (it's always a miner in priority order)
        explorer_id = max(self._shared_team_ids) if self._shared_team_ids else num_agents - 1

        # Only the explorer agent, only for first 400 steps, only for 4+ agents
        if self._agent_id == explorer_id and step < 400 and num_agents >= 4:
            hp = int(state.self_state.inventory.get("hp", 0))

            # Retreat to hub when HP gets low
            if hp < 30:
                hub = self._nearest_hub(state)
                if hub is not None:
                    return self._move_to_known(state, hub, summary="explorer_retreat", vibe="change_vibe_miner")

            # Deposit any resources picked up along the way
            if self._should_deposit_resources(state):
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="explorer_deposit", vibe="change_vibe_miner")

            # Systematic grid exploration
            current_pos = _h.absolute_position(state)
            hub = self._nearest_hub(state)
            center = (hub.global_x, hub.global_y) if hub is not None else current_pos

            waypoint = _SYSTEMATIC_EXPLORE_GRID[self._explore_waypoint_idx % len(_SYSTEMATIC_EXPLORE_GRID)]
            target = (center[0] + waypoint[0], center[1] + waypoint[1])

            # Clamp to map bounds (0-87)
            target = (max(2, min(85, target[0])), max(2, min(85, target[1])))

            if _h.manhattan(current_pos, target) <= 3:
                self._explore_waypoint_idx += 1
                waypoint = _SYSTEMATIC_EXPLORE_GRID[self._explore_waypoint_idx % len(_SYSTEMATIC_EXPLORE_GRID)]
                target = (center[0] + waypoint[0], center[1] + waypoint[1])
                target = (max(2, min(85, target[0])), max(2, min(85, target[1])))

            return self._move_to_position(state, target, summary="systematic_explore", vibe="change_vibe_miner")

        # Normal miner behavior
        return super()._miner_action(state, summary_prefix=summary_prefix)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV9 aligner + chain push: after aligning a frontier junction,
        continue exploring in that direction to discover more junctions."""
        hub = self._nearest_hub(state)
        current_pos = _h.absolute_position(state)

        # If in chain push mode, continue exploring toward frontier
        if self._chain_push_steps > 0:
            self._chain_push_steps -= 1
            hp = int(state.self_state.inventory.get("hp", 0))

            # Abort chain push if HP too low
            if hp < 25 or self._chain_push_direction is None:
                self._chain_push_steps = 0
                self._chain_push_direction = None
            else:
                # Check if we found a new alignable junction during push
                new_target = self._preferred_alignable_neutral_junction(state)
                if new_target is not None:
                    self._chain_push_steps = 0
                    self._chain_push_direction = None
                    self._claim_target(new_target.position)
                    self._set_sticky_target(new_target.position, new_target.entity_type)
                    return self._move_to_known(state, new_target, summary="chain_align_junction", vibe="change_vibe_aligner")

                # Continue pushing in the direction
                push_target = (
                    current_pos[0] + self._chain_push_direction[0] * 5,
                    current_pos[1] + self._chain_push_direction[1] * 5,
                )
                push_target = (max(2, min(85, push_target[0])), max(2, min(85, push_target[1])))
                return self._move_to_position(state, push_target, summary="chain_push_explore", vibe="change_vibe_aligner")

        # Get the parent's aligner action
        action, summary = super()._aligner_action(state)

        # After aligning, initiate chain push toward the frontier
        if summary == "align_junction" and self._current_target_position is not None:
            target_pos = self._current_target_position
            hub_pos = (hub.global_x, hub.global_y) if hub is not None else current_pos
            # Direction from hub toward the junction we're aligning
            dx = target_pos[0] - hub_pos[0]
            dy = target_pos[1] - hub_pos[1]
            dist = max(abs(dx) + abs(dy), 1)
            # Normalize to unit direction (-1, 0, or 1)
            norm_dx = 1 if dx > 0 else (-1 if dx < 0 else 0)
            norm_dy = 1 if dy > 0 else (-1 if dy < 0 else 0)
            # Only chain push if this junction is at the frontier (far from hub)
            if dist >= 15:
                self._chain_push_steps = 20
                self._chain_push_direction = (norm_dx, norm_dy)
                self._last_aligned_position = target_pos

        return action, summary


class AlphaTournamentV11Policy(MettagridSemanticPolicy):
    """TournamentV11: TV9 + early burst exploration + chain push."""
    short_names = ["alpha-tournament-v11"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV11AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV11b: Chain push only (no burst explorer) ───────────────────────────

class AlphaTournamentV11bAgentPolicy(AlphaTournamentV9AgentPolicy):
    """TV11b: TV9 + chain push only (no burst explorer).

    After aligning a frontier junction (>= 15 from hub), explore 15 steps
    in that direction to discover chain extensions.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._chain_push_steps = 0
        self._chain_push_direction: tuple[int, int] | None = None

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        hub = self._nearest_hub(state)
        current_pos = _h.absolute_position(state)

        if self._chain_push_steps > 0:
            self._chain_push_steps -= 1
            hp = int(state.self_state.inventory.get("hp", 0))

            if hp < 25 or self._chain_push_direction is None:
                self._chain_push_steps = 0
                self._chain_push_direction = None
            else:
                new_target = self._preferred_alignable_neutral_junction(state)
                if new_target is not None:
                    self._chain_push_steps = 0
                    self._chain_push_direction = None
                    self._claim_target(new_target.position)
                    self._set_sticky_target(new_target.position, new_target.entity_type)
                    return self._move_to_known(state, new_target, summary="chain_align_junction", vibe="change_vibe_aligner")

                push_target = (
                    current_pos[0] + self._chain_push_direction[0] * 5,
                    current_pos[1] + self._chain_push_direction[1] * 5,
                )
                push_target = (max(2, min(85, push_target[0])), max(2, min(85, push_target[1])))
                return self._move_to_position(state, push_target, summary="chain_push_explore", vibe="change_vibe_aligner")

        action, summary = super()._aligner_action(state)

        if summary == "align_junction" and self._current_target_position is not None:
            target_pos = self._current_target_position
            hub_pos = (hub.global_x, hub.global_y) if hub is not None else current_pos
            dx = target_pos[0] - hub_pos[0]
            dy = target_pos[1] - hub_pos[1]
            dist = max(abs(dx) + abs(dy), 1)
            norm_dx = 1 if dx > 0 else (-1 if dx < 0 else 0)
            norm_dy = 1 if dy > 0 else (-1 if dy < 0 else 0)
            if dist >= 15:
                self._chain_push_steps = 15
                self._chain_push_direction = (norm_dx, norm_dy)

        return action, summary


class AlphaTournamentV11bPolicy(MettagridSemanticPolicy):
    """TV11b: TV9 + chain push only."""
    short_names = ["alpha-tournament-v11b"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV11bAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV11c: Short chain push (10 steps) + no explorer ─────────────────────

class AlphaTournamentV11cAgentPolicy(AlphaTournamentV9AgentPolicy):
    """TV11c: TV9 + short chain push (10 steps) after frontier alignment."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._chain_push_steps = 0
        self._chain_push_direction: tuple[int, int] | None = None

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        hub = self._nearest_hub(state)
        current_pos = _h.absolute_position(state)

        if self._chain_push_steps > 0:
            self._chain_push_steps -= 1
            hp = int(state.self_state.inventory.get("hp", 0))

            if hp < 30 or self._chain_push_direction is None:
                self._chain_push_steps = 0
                self._chain_push_direction = None
            else:
                new_target = self._preferred_alignable_neutral_junction(state)
                if new_target is not None:
                    self._chain_push_steps = 0
                    self._chain_push_direction = None
                    self._claim_target(new_target.position)
                    self._set_sticky_target(new_target.position, new_target.entity_type)
                    return self._move_to_known(state, new_target, summary="chain_align_junction", vibe="change_vibe_aligner")

                push_target = (
                    current_pos[0] + self._chain_push_direction[0] * 5,
                    current_pos[1] + self._chain_push_direction[1] * 5,
                )
                push_target = (max(2, min(85, push_target[0])), max(2, min(85, push_target[1])))
                return self._move_to_position(state, push_target, summary="chain_push_explore", vibe="change_vibe_aligner")

        action, summary = super()._aligner_action(state)

        if summary == "align_junction" and self._current_target_position is not None:
            target_pos = self._current_target_position
            hub_pos = (hub.global_x, hub.global_y) if hub is not None else current_pos
            dx = target_pos[0] - hub_pos[0]
            dy = target_pos[1] - hub_pos[1]
            dist = max(abs(dx) + abs(dy), 1)
            norm_dx = 1 if dx > 0 else (-1 if dx < 0 else 0)
            norm_dy = 1 if dy > 0 else (-1 if dy < 0 else 0)
            if dist >= 15:
                self._chain_push_steps = 10
                self._chain_push_direction = (norm_dx, norm_dy)

        return action, summary


class AlphaTournamentV11cPolicy(MettagridSemanticPolicy):
    """TV11c: TV9 + short chain push."""
    short_names = ["alpha-tournament-v11c"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV11cAgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV12: Adaptive stagnation exploration ────────────────────────────────

class AlphaTournamentV12AgentPolicy(AlphaTournamentV7AgentPolicy):
    """TournamentV12: TV7 + stagnation-adaptive exploration.

    Detects when junction discovery stagnates and switches idle
    aligners to wider exploration to find distant junction clusters.
    Self-play avg 11.15 vs TV7's 10.29 (+8%).

    Key design:
    - Track peak friendly junction count
    - If no new peak in 300 steps AND peak >= 5: enter stagnation mode
    - Stagnation idle: 2/3 explore (3-ring offsets), 1/3 scramble
    - Normal idle: TV7's proven scramble/explore/mine alternation
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._peak_junction_count = 0
        self._last_junction_growth_step = 0
        self._stagnation_mode = False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV7 aligner + stagnation-adaptive exploration."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        # Standard aligner logic (same as TV7)
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # STAGNATION: explore aggressively with wider offsets
            # Still scramble 1/3 of the time to maintain Clips pressure
            if step % 300 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        # NOT stagnating: use TV7's proven idle behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Wider exploration when junction discovery has stagnated."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        # 3 rings: inner (22), mid (30), outer (35)
        offsets = (
            (0, -22), (16, -16), (22, 0), (16, 16),
            (0, 22), (-16, 16), (-22, 0), (-16, -16),
            (0, -30), (21, -21), (30, 0), (21, 21),
            (0, 30), (-21, 21), (-30, 0), (-21, -21),
            (0, -35), (25, -25), (35, 0), (25, 25),
            (0, 35), (-25, 25), (-35, 0), (-25, -25),
        )
        offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="stag_explore", vibe="change_vibe_aligner")


class AlphaTournamentV12Policy(MettagridSemanticPolicy):
    """TournamentV12: TV7 + adaptive stagnation exploration."""
    short_names = ["alpha-tournament-v12"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV12AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV13: TV12 stagnation + TV11 systematic explore + chain push ─────────

class AlphaTournamentV13AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV13: TV12 stagnation detection + TV11 systematic exploration.

    Combines two orthogonal improvements:
    1. TV12: stagnation-triggered wider exploration for idle aligners
    2. TV11: last miner systematically explores map for first 400 steps
    3. TV11: chain push after aligning frontier junctions
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._explore_waypoint_idx = 0
        self._chain_push_steps = 0
        self._chain_push_direction: tuple[int, int] | None = None

    def _miner_action(self, state: MettagridState, summary_prefix: str = "") -> tuple[Action, str]:
        """Last agent: systematic map exploration for first 400 steps."""
        step = state.step or self._step_index
        num_agents = self.policy_env_info.num_agents
        explorer_id = max(self._shared_team_ids) if self._shared_team_ids else num_agents - 1

        if self._agent_id == explorer_id and step < 400 and num_agents >= 4:
            hp = int(state.self_state.inventory.get("hp", 0))

            if hp < 30:
                hub = self._nearest_hub(state)
                if hub is not None:
                    return self._move_to_known(state, hub, summary="explorer_retreat", vibe="change_vibe_miner")

            if self._should_deposit_resources(state):
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="explorer_deposit", vibe="change_vibe_miner")

            current_pos = _h.absolute_position(state)
            hub = self._nearest_hub(state)
            center = (hub.global_x, hub.global_y) if hub is not None else current_pos

            waypoint = _SYSTEMATIC_EXPLORE_GRID[self._explore_waypoint_idx % len(_SYSTEMATIC_EXPLORE_GRID)]
            target = (center[0] + waypoint[0], center[1] + waypoint[1])
            target = (max(2, min(85, target[0])), max(2, min(85, target[1])))

            if _h.manhattan(current_pos, target) <= 3:
                self._explore_waypoint_idx += 1
                waypoint = _SYSTEMATIC_EXPLORE_GRID[self._explore_waypoint_idx % len(_SYSTEMATIC_EXPLORE_GRID)]
                target = (center[0] + waypoint[0], center[1] + waypoint[1])
                target = (max(2, min(85, target[0])), max(2, min(85, target[1])))

            return self._move_to_position(state, target, summary="systematic_explore", vibe="change_vibe_miner")

        return super()._miner_action(state, summary_prefix=summary_prefix)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV12 aligner + chain push after frontier alignment."""
        hub = self._nearest_hub(state)
        current_pos = _h.absolute_position(state)

        # Chain push mode: continue exploring toward frontier
        if self._chain_push_steps > 0:
            self._chain_push_steps -= 1
            hp = int(state.self_state.inventory.get("hp", 0))

            if hp < 25 or self._chain_push_direction is None:
                self._chain_push_steps = 0
                self._chain_push_direction = None
            else:
                new_target = self._preferred_alignable_neutral_junction(state)
                if new_target is not None:
                    self._chain_push_steps = 0
                    self._chain_push_direction = None
                    self._claim_target(new_target.position)
                    self._set_sticky_target(new_target.position, new_target.entity_type)
                    return self._move_to_known(state, new_target, summary="chain_align_junction", vibe="change_vibe_aligner")

                push_target = (
                    current_pos[0] + self._chain_push_direction[0] * 5,
                    current_pos[1] + self._chain_push_direction[1] * 5,
                )
                push_target = (max(2, min(85, push_target[0])), max(2, min(85, push_target[1])))
                return self._move_to_position(state, push_target, summary="chain_push_explore", vibe="change_vibe_aligner")

        # Get TV12 parent's aligner action (with stagnation)
        action, summary = super()._aligner_action(state)

        # After aligning, initiate chain push toward frontier
        if summary == "align_junction" and self._current_target_position is not None:
            target_pos = self._current_target_position
            hub_pos = (hub.global_x, hub.global_y) if hub is not None else current_pos
            dx = target_pos[0] - hub_pos[0]
            dy = target_pos[1] - hub_pos[1]
            dist = max(abs(dx) + abs(dy), 1)
            norm_dx = 1 if dx > 0 else (-1 if dx < 0 else 0)
            norm_dy = 1 if dy > 0 else (-1 if dy < 0 else 0)
            if dist >= 15:
                self._chain_push_steps = 15  # Shorter than TV11's 20
                self._chain_push_direction = (norm_dx, norm_dy)

        return action, summary


class AlphaTournamentV13Policy(MettagridSemanticPolicy):
    """TournamentV13: TV12 stagnation + TV11 systematic explore + chain push."""
    short_names = ["alpha-tournament-v13"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV13AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV14: TV12 stagnation + TV9 2-agent fix + silicon priority ────────────

class AlphaTournamentV14AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV14: TV12 stagnation + TV9 2-agent fix + silicon priority.

    Self-play avg 12.70 at 10K steps.
    Tournament improvements over TV12:
    1. TV9's 2-agent timing fix (align at 200, not 500)
    2. Silicon priority when silicon < 100
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV9's fixed 2-agent budgets, TV7/TV12 for 4+ agents."""
        num_agents = self.policy_env_info.num_agents

        if num_agents <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        return super()._pressure_budgets(state, objective=objective)

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon-priority mining when silicon gets low."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        if silicon < 100:
            return MacroDirective(resource_bias="silicon")
        least = _least_resource(resources)
        least_amount = resources[least]
        if silicon <= least_amount + 30:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV14Policy(MettagridSemanticPolicy):
    """TournamentV14: TV12 stagnation + TV9 2-agent + silicon priority."""
    short_names = ["alpha-tournament-v14"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV14AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV15: Aggressive idle exploration to break stagnation ──────────────

# Full-map exploration waypoints: systematic grid covering 88x88 map
_TV15_FULL_MAP_WAYPOINTS = (
    # Inner ring (radius ~15)
    (0, -15), (11, -11), (15, 0), (11, 11),
    (0, 15), (-11, 11), (-15, 0), (-11, -11),
    # Mid ring (radius ~25)
    (0, -25), (18, -18), (25, 0), (18, 18),
    (0, 25), (-18, 18), (-25, 0), (-18, -18),
    # Outer ring (radius ~35)
    (0, -35), (25, -25), (35, 0), (25, 25),
    (0, 35), (-25, 25), (-35, 0), (-25, -25),
    # Corner probes (reach map edges)
    (30, -30), (30, 30), (-30, 30), (-30, -30),
    (-38, 0), (38, 0), (0, -38), (0, 38),
)


class AlphaTournamentV15AgentPolicy(AlphaTournamentV9AgentPolicy):
    """TournamentV15: TV9 + aggressive idle exploration.

    Key insight: When all nearby junctions are aligned, aligners loop on
    idle_align_scramble forever at the same position. The default explore
    offsets only reach radius 22, but the 88x88 map has junctions out to
    radius ~40.

    Fix:
    1. Track consecutive "idle" steps (no align target available)
    2. After 150 idle steps: switch to full-map exploration using
       waypoints that cover the entire map (inner/mid/outer/corners)
    3. Exploration has priority over idle scrambling when exploring
    4. Once a new frontier junction is found, immediately return to alignment
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._idle_steps = 0
        self._exploring_map = False
        self._map_waypoint_index = 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV9 aligner with aggressive exploration when idle."""
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)
        step = state.step or self._step_index

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            self._idle_steps = 0
            self._exploring_map = False
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            self._idle_steps = 0
            self._exploring_map = False
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._idle_steps = 0
            self._exploring_map = False
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()

        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                self._idle_steps = 0
                self._exploring_map = False
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        self._idle_steps += 1
        min_res = _h.team_min_resource(state)

        if self._idle_steps >= 150 and hp > 40:
            self._exploring_map = True

        if self._exploring_map:
            if step % 400 < 50 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="explore_scramble", vibe="change_vibe_scrambler")
            return self._full_map_explore(state)

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _full_map_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Visit waypoints across the entire 88x88 map."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos

        waypoints = _TV15_FULL_MAP_WAYPOINTS
        idx = (self._map_waypoint_index + self._agent_id * 7) % len(waypoints)
        offset = waypoints[idx]
        absolute_target = (center[0] + offset[0], center[1] + offset[1])

        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._map_waypoint_index += 1
            idx = (self._map_waypoint_index + self._agent_id * 7) % len(waypoints)
            offset = waypoints[idx]
            absolute_target = (center[0] + offset[0], center[1] + offset[1])

        return self._move_to_position(state, absolute_target, summary="map_explore", vibe="change_vibe_aligner")


class AlphaTournamentV15Policy(MettagridSemanticPolicy):
    """TournamentV15: TV9 + aggressive idle exploration."""
    short_names = ["alpha-tournament-v15"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV15AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV16: Tournament economy fix + TV12 stagnation ─────────────────────────

class AlphaTournamentV16AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV16: TV12 stagnation + tournament-aware economy budgets.

    Key insight from tournament logs: with 4 of 8 agents, running 3 aligners
    + 1 miner causes silicon depletion by step 1500, crashing the economy.

    Fixes:
    1. Detect tournament (num_agents != our team size) → conservative budgets
    2. Cap aligners at 2 for 4-agent sub-team (ensures 2 miners)
    3. Silicon priority mining in tournament when silicon < 100
    4. TV9's 2-agent fix
    5. TV12's stagnation exploration for idle aligners
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        if team_size <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        in_tournament = team_size < num_agents

        if in_tournament:
            max_aligners = max(team_size - 2, 1)
            if step < 100 or (min_res < 10 and not can_hearts) or min_res < 50:
                return min(1, max_aligners), 0
            return min(2, max_aligners), 0

        return super()._pressure_budgets(state, objective=objective)

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents
        resources = _shared_resources(state)

        if team_size < num_agents:
            silicon = resources.get("silicon", 0)
            if silicon < 100:
                return MacroDirective(resource_bias="silicon")

        return MacroDirective(resource_bias=_least_resource(resources))


class AlphaTournamentV16Policy(MettagridSemanticPolicy):
    """TournamentV16: TV12 stagnation + tournament economy fix."""
    short_names = ["alpha-tournament-v16"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV16AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV17: Adaptive partner policy ─────────────────────────────────────────

class AlphaTournamentV17AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV17: TV12 stagnation + adaptive partner response.

    Key insight: tournament score is average across all partner pairings.
    When paired with a weak partner, we need to carry (more alignment).
    When paired with a strong partner, we should support (more mining).

    Adaptation:
    - Track junction growth rate from step 500-1000 (after initial ramp)
    - If growth is strong (>15 junctions by step 1000): partner is good → support mode
    - If growth is weak (<10 junctions by step 1000): partner is weak → carry mode

    Carry mode: 3 aligners, 1 miner (maximize our alignment contribution)
    Support mode: 1-2 aligners, 2-3 miners (boost economy for strong partner)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._partner_strength_assessed = False
        self._strong_partner = False
        self._junction_at_1000 = 0

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV9 fix
        if team_size <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            return 1, 0

        # Self-play: use parent budgets
        if team_size >= num_agents:
            return super()._pressure_budgets(state, objective=objective)

        # Tournament: assess partner at step 1000
        if step >= 1000 and not self._partner_strength_assessed:
            self._partner_strength_assessed = True
            team_id = _h.team_id(state)
            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            self._junction_at_1000 = friendly_count
            # If partner is contributing, we have >15 junctions by step 1000
            self._strong_partner = friendly_count >= 15

        max_roles = max(team_size - 1, 1)

        if step < 500:
            # Early game: standard ramp
            if step < 100 or (min_res < 10 and not can_hearts):
                return min(1, max_roles), 0
            return min(2, max_roles), 0

        if self._strong_partner:
            # Strong partner: support with more mining
            if min_res < 30:
                return min(1, max_roles), 0
            return min(2, max_roles), 0
        else:
            # Weak partner: carry with more alignment
            if min_res < 10 and not can_hearts:
                return min(1, max_roles), 0
            if min_res < 30:
                return min(2, max_roles), 0
            return min(3, max_roles), 0

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Least resource bias, silicon priority when low in tournament."""
        resources = _shared_resources(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if team_size < num_agents:
            silicon = resources.get("silicon", 0)
            if silicon < 80:
                return MacroDirective(resource_bias="silicon")

        return MacroDirective(resource_bias=_least_resource(resources))


class AlphaTournamentV17Policy(MettagridSemanticPolicy):
    """TournamentV17: TV12 stagnation + adaptive partner response."""
    short_names = ["alpha-tournament-v17"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV17AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV18: TV12 stagnation + light silicon priority (no budget changes) ────

class AlphaTournamentV18AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV18: TV12 stagnation + light silicon priority only.

    Learnings from v370-v373: changing aligner budgets HURTS tournament.
    TV12's budgets are already near-optimal.

    Only change: silicon priority when it's the lowest resource OR when < 50.
    This prevents the step 1500 silicon crash without reducing alignment capacity.
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)

        # Only prioritize silicon when it's genuinely the bottleneck
        if least == "silicon" or silicon < 50:
            return MacroDirective(resource_bias="silicon")

        return MacroDirective(resource_bias=least)


class AlphaTournamentV18Policy(MettagridSemanticPolicy):
    """TournamentV18: TV12 + light silicon priority."""
    short_names = ["alpha-tournament-v18"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV18AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV19: One-explorer + silicon priority ──────────────────────────────

# Full-map exploration waypoints covering 88x88 map
_TV19_FULL_MAP_WAYPOINTS = (
    (0, -15), (11, -11), (15, 0), (11, 11),
    (0, 15), (-11, 11), (-15, 0), (-11, -11),
    (0, -25), (18, -18), (25, 0), (18, 18),
    (0, 25), (-18, 18), (-25, 0), (-18, -18),
    (0, -35), (25, -25), (35, 0), (25, 25),
    (0, 35), (-25, 25), (-35, 0), (-25, -25),
    (30, -30), (30, 30), (-30, 30), (-30, -30),
    (-38, 0), (38, 0), (0, -38), (0, 38),
)


class AlphaTournamentV19AgentPolicy(AlphaTournamentV9AgentPolicy):
    """TournamentV19: Agent 0 uses wide explore offsets + silicon priority.

    Instead of a special "explore mode" that never triggers (alignment targets
    keep resetting the idle counter), agent 0 ALWAYS uses wider explore offsets
    (radius 22+32+38 vs standard 22) during normal idle explore actions.

    This means agent 0 naturally discovers farther junctions during the
    `step % 200 < 100` explore cycle that TV9 already does.

    Other agents keep standard r22 offsets for faster local discovery.
    Also adds silicon priority mining from TV18.
    """

    def _explore_action(self, state: MettagridState, *, role: str, summary: str) -> tuple[Action, str]:
        """Agent 0 aligner uses wide offsets when team has 5+ agents."""
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if role != "aligner" or self._agent_id != 0 or team_size < 5:
            return super()._explore_action(state, role=role, summary=summary)

        # Agent 0 with 5+ agents: cycle through full-map waypoints
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _TV19_FULL_MAP_WAYPOINTS
        offset_index = (self._explore_index + self._agent_id * 7) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 7) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary=summary, vibe=_h.role_vibe(role))


class AlphaTournamentV19Policy(MettagridSemanticPolicy):
    """TournamentV19: One-explorer aligner + silicon priority."""
    short_names = ["alpha-tournament-v19"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV19AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV20: TV12 stagnation + full-map explore + silicon priority ─────────

class AlphaTournamentV20AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV20: TV12 stagnation + full-map exploration + silicon awareness.

    Improvements over TV12:
    1. Replace TV12's 3-ring stagnation explore (max r=35) with TV15's
       full-map waypoints (reaches r=38, corners, edges).
    2. During stagnation, explore-ONLY (no scrambling) — save hearts
       for aligning newly-discovered junctions.
    3. Silicon priority mining (TV18 style): only when genuinely bottleneck.
    4. Track idle steps as secondary trigger (150 idle → explore).
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._idle_steps = 0
        self._map_waypoint_index = 0

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Full-map exploration when stagnating (replaces TV12's 3-ring)."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        waypoints = _TV15_FULL_MAP_WAYPOINTS
        idx = (self._map_waypoint_index + self._agent_id * 7) % len(waypoints)
        offset = waypoints[idx]
        absolute_target = (center[0] + offset[0], center[1] + offset[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._map_waypoint_index += 1
            idx = (self._map_waypoint_index + self._agent_id * 7) % len(waypoints)
            offset = waypoints[idx]
            absolute_target = (center[0] + offset[0], center[1] + offset[1])
        return self._move_to_position(state, absolute_target, summary="stag_map_explore", vibe="change_vibe_aligner")

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV12 aligner + full-map stagnation explore + idle tracking."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation (same as TV12)
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            self._idle_steps = 0
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            self._idle_steps = 0
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._idle_steps = 0
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                self._idle_steps = 0
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        self._idle_steps += 1
        min_res = _h.team_min_resource(state)

        # STAGNATION: full-map explore, skip scrambling to save hearts
        if (self._stagnation_mode or self._idle_steps >= 150) and hp > 40:
            return self._stagnation_explore(state)

        # Normal idle: TV12 behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon priority when it's the bottleneck (TV18 style)."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        if least == "silicon" or silicon < 50:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV20Policy(MettagridSemanticPolicy):
    """TournamentV20: TV12 stagnation + full-map explore + silicon priority."""
    short_names = ["alpha-tournament-v20"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV20AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV21: TV18 silicon + TV19 one-explorer + TV12 stagnation ────────────

class AlphaTournamentV21AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV21: TV12 stagnation + TV18 silicon + TV19 one-explorer.

    Best of all proven tournament improvements:
    - TV12's stagnation detection (proven +0.12 over TV9)
    - TV18's light silicon priority (v378 #1 at 10.13)
    - TV19's agent-0 wide explore offsets (v379 #3 at 9.14)
    """

    def _explore_action(self, state: MettagridState, *, role: str, summary: str) -> tuple[Action, str]:
        """Agent 0 aligner uses wide offsets when team has 5+ agents."""
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if role != "aligner" or self._agent_id != 0 or team_size < 5:
            return super()._explore_action(state, role=role, summary=summary)

        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = _TV19_FULL_MAP_WAYPOINTS
        offset_index = (self._explore_index + self._agent_id * 7) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 2:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 7) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary=summary, vibe=_h.role_vibe(role))

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon priority when bottleneck (TV18 style)."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        if least == "silicon" or silicon < 50:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV21Policy(MettagridSemanticPolicy):
    """TournamentV21: TV12 stagnation + TV18 silicon + TV19 one-explorer."""
    short_names = ["alpha-tournament-v21"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV21AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV22: TV18 silicon + faster stagnation response ────────────────────

class AlphaTournamentV22AgentPolicy(AlphaTournamentV12AgentPolicy):
    """TournamentV22: TV12 stagnation (faster trigger) + TV18 silicon.

    TV18 (v378) is #1 at 10.22. Only change:
    - Stagnation triggers after 200 steps (vs 300) of no junction growth
    - This means we explore sooner when stuck, potentially finding more junctions
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV12 aligner with faster stagnation trigger (200 vs 300 steps)."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # 200 vs 300
              and step > 400):  # 400 vs 500
            self._stagnation_mode = True

        # Rest is identical to TV12
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 300 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Silicon priority when bottleneck (TV18 style)."""
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)
        if least == "silicon" or silicon < 50:
            return MacroDirective(resource_bias="silicon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV22Policy(MettagridSemanticPolicy):
    """TournamentV22: TV12 faster stagnation + TV18 silicon."""
    short_names = ["alpha-tournament-v22"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV22AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV23: TV18 + territory loss recovery ───────────────────────────────

class AlphaTournamentV23AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV23: TV18 + territory loss recovery mode.

    Key insight from match analysis: when friendly junctions drop significantly
    from peak (>50% loss), the problem isn't stagnation (can't find junctions)
    but territory loss (enemy scrambling our junctions). In this case:
    - Don't explore far away (wastes HP and time)
    - Scramble enemy junctions near our network to create alignable targets
    - Stay closer to hub to maintain HP for the fight

    v378 (TV18) is #1 at 10.16. This only adds loss recovery behavior,
    leaving all other mechanics identical.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._territory_loss_mode = False

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV18 aligner + territory loss recovery."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation AND territory loss
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
            self._territory_loss_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            # Distinguish territory loss from true stagnation
            if friendly_count <= self._peak_junction_count * 0.5:
                self._territory_loss_mode = True
                self._stagnation_mode = False
            else:
                self._stagnation_mode = True
                self._territory_loss_mode = False

        # Standard aligner logic (same as TV12/TV18)
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # TERRITORY LOSS: prioritize scrambling enemy near hub to create targets
        if self._territory_loss_mode and hp > 40:
            if hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="recovery_scramble", vibe="change_vibe_scrambler")
            # Explore near hub (not far away) to find re-alignable junctions
            if hub is not None:
                return self._close_explore(state, hub)
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        if self._stagnation_mode and hp > 50:
            # STAGNATION: explore aggressively with wider offsets
            if step % 300 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        # NOT stagnating: use TV7's proven idle behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")

    def _close_explore(self, state: MettagridState, hub) -> tuple[Action, str]:
        """Explore within hub range (25) to find re-alignable junctions."""
        current_pos = _h.absolute_position(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        # Tighter exploration: stay within alignment range of hub (25)
        offsets = (
            (0, -12), (9, -9), (12, 0), (9, 9),
            (0, 12), (-9, 9), (-12, 0), (-9, -9),
            (0, -20), (14, -14), (20, 0), (14, 14),
            (0, 20), (-14, 14), (-20, 0), (-14, -14),
        )
        offset_index = (self._explore_index + self._agent_id * 3) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 3) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="recovery_explore", vibe="change_vibe_aligner")


class AlphaTournamentV23Policy(MettagridSemanticPolicy):
    """TournamentV23: TV18 + territory loss recovery."""
    short_names = ["alpha-tournament-v23"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV23AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV24: TV18 + tighter stagnation exploration ──────────────────────────

class AlphaTournamentV24AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV24: TV18 + tighter stagnation exploration radius.

    v378 (TV18) stagnation explore uses offsets at range 22-35 from hub.
    Problem: agents wander far, lose HP, and retreat instead of aligning.
    Junctions must be within 15 of network or 25 of hub to be alignable.

    Fix: tighten stagnation offsets to range 15-25, staying within hub
    alignment range. This finds alignable junctions instead of wasting HP.
    All other mechanics (budgets, silicon priority, scramble) unchanged.
    """

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Tighter stagnation exploration within hub alignment range."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        # 2 rings within hub alignment range (25):
        # Inner ring at 15, outer ring at 22
        offsets = (
            (0, -15), (11, -11), (15, 0), (11, 11),
            (0, 15), (-11, 11), (-15, 0), (-11, -11),
            (0, -22), (16, -16), (22, 0), (16, 16),
            (0, 22), (-16, 16), (-22, 0), (-16, -16),
        )
        offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="stag_explore", vibe="change_vibe_aligner")


class AlphaTournamentV24Policy(MettagridSemanticPolicy):
    """TournamentV24: TV18 + tighter stagnation radius."""
    short_names = ["alpha-tournament-v24"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV24AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV25: TV18 + scramble-only stagnation (no far explore) ──────────────

class AlphaTournamentV25AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV25: TV18 + scramble-focused stagnation.

    When stagnating, instead of exploring far (which wastes HP), focus on
    scrambling enemy junctions to create alignable neutral targets.
    Alternate between scramble and near-hub explore (no far offsets).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV18 aligner with scramble-focused stagnation."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # STAGNATION: scramble more, explore less far
            # 50% scramble (vs TV12's 33%) to create alignable targets
            if step % 200 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            # Regular explore (not far stagnation explore) — stay productive
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # NOT stagnating: TV7's proven idle behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV25Policy(MettagridSemanticPolicy):
    """TournamentV25: TV18 + scramble-focused stagnation."""
    short_names = ["alpha-tournament-v25"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV25AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV26: TV18 + full-map stagnation waypoints (from TV15) ──────────���────

class AlphaTournamentV26AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV26: TV18 + TV15-style full-map stagnation waypoints.

    TV12's stagnation explore uses 3 rings at r=22/30/35 (24 points).
    TV15's full-map waypoints use 3 rings at r=15/25/35 + corner probes (32 points).

    TV15's waypoints:
    - Start closer (r=15 vs r=22) — finds nearby junctions first
    - Include corner probes (r=30-38) — covers map edges
    - More points (32 vs 24) — more thorough coverage

    All other mechanics identical to TV18.
    """

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Full-map waypoint exploration during stagnation (TV15 style)."""
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        waypoints = _TV15_FULL_MAP_WAYPOINTS
        offset_index = (self._explore_index + self._agent_id * 7) % len(waypoints)
        target = waypoints[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 7) % len(waypoints)
            target = waypoints[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="stag_explore", vibe="change_vibe_aligner")


class AlphaTournamentV26Policy(MettagridSemanticPolicy):
    """TournamentV26: TV18 + full-map stagnation waypoints."""
    short_names = ["alpha-tournament-v26"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV26AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV27: TV18 + idle-step exploration (TV15 style trigger) ─────────────

class AlphaTournamentV27AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV27: TV18 + TV15-style idle step exploration.

    Adds TV15's idle-step trigger ON TOP of TV12's stagnation detection:
    - Track consecutive steps with no align target
    - After 150 idle steps: switch to full-map waypoint exploration
    - This catches cases where stagnation mode doesn't trigger
      (e.g., peak < 5 so stagnation never fires)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._idle_aligner_steps = 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV18 aligner + idle step exploration."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation (from TV12)
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
            self._idle_aligner_steps = 0
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._idle_aligner_steps = 0
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._idle_aligner_steps = 0
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._idle_aligner_steps = 0
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        # No alignment target — increment idle counter
        self._idle_aligner_steps += 1

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                self._idle_aligner_steps = 0
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # TV15-style idle exploration (supplements TV12 stagnation)
        if self._idle_aligner_steps >= 150 and hp > 50:
            waypoints = _TV15_FULL_MAP_WAYPOINTS
            center = (hub.global_x, hub.global_y) if hub is not None else current_pos
            offset_index = (self._explore_index + self._agent_id * 7) % len(waypoints)
            target_offset = waypoints[offset_index]
            absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
            if _h.manhattan(current_pos, absolute_target) <= 3:
                self._explore_index += 1
                offset_index = (self._explore_index + self._agent_id * 7) % len(waypoints)
                target_offset = waypoints[offset_index]
                absolute_target = (center[0] + target_offset[0], center[1] + target_offset[1])
            return self._move_to_position(state, absolute_target, summary="idle_map_explore", vibe="change_vibe_aligner")

        if self._stagnation_mode and hp > 50:
            if step % 300 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV27Policy(MettagridSemanticPolicy):
    """TournamentV27: TV18 + idle-step exploration."""
    short_names = ["alpha-tournament-v27"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV27AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV28: TV18 + prefer re-aligning scrambled junctions ──────────────────

class AlphaTournamentV28AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV28: TV18 + negative hotspot weight (prefer re-alignment).

    Key insight: When enemy scramblers unalign our junctions, they become
    neutral again but are still in our network range. Re-aligning them is
    cheaper than discovering new junctions. Default hotspot_weight=8.0
    penalizes these, but we WANT to re-align them quickly.

    Change: hotspot_weight = -10.0 (strong preference for re-alignment)
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0  # Prefer re-aligning scrambled junctions


class AlphaTournamentV28Policy(MettagridSemanticPolicy):
    """TournamentV28: TV18 + prefer re-aligning scrambled junctions."""
    short_names = ["alpha-tournament-v28"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV28AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV29: TV18 + faster 4-agent alignment ramp ──────────────────────────

class AlphaTournamentV29AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV29: TV18 + faster alignment start for 4-agent games.

    In 4v4 tournament games, TV18 waits until step 100 before first aligner
    (inherited from AdaptiveV3). Starting alignment at step 50 with even just
    1 aligner could accumulate more score in the early game.

    Also: in 2-agent games, try aligning earlier (step 300 instead of 500)
    if economy is healthy.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Earlier alignment for 2-agent: step 300 instead of 500
            if step < 300 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 30:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            # Start alignment at step 50 instead of 100
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        # 5+ agents: use parent (TV7 -> TV2 -> AdaptiveV3)
        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV29Policy(MettagridSemanticPolicy):
    """TournamentV29: TV18 + faster alignment ramp."""
    short_names = ["alpha-tournament-v29"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV29AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV30: TV18 + TV28 re-alignment + TV29 faster ramp (combined) ────────

class AlphaTournamentV30AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV30: TV18 + re-alignment preference + faster ramp.

    Combines TV28 (negative hotspot weight) and TV29 (faster alignment ramp).
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 300 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 30:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, num_agents - 1)
            return aligner_budget, 0

        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV30Policy(MettagridSemanticPolicy):
    """TournamentV30: TV18 + re-alignment + faster ramp (combined)."""
    short_names = ["alpha-tournament-v30"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV30AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV31: TV18 + mild re-alignment preference ───────────────────────────

class AlphaTournamentV31AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV31: TV18 + mild negative hotspot weight (-5.0).

    TV28 uses -10.0 for aggressive re-alignment. TV31 uses -5.0 for
    a milder preference to see which weight works better in tournament.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -5.0


class AlphaTournamentV31Policy(MettagridSemanticPolicy):
    """TournamentV31: TV18 + mild re-alignment preference."""
    short_names = ["alpha-tournament-v31"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV31AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV32: TV18 + zero hotspot weight (no bias either way) ───────────────

class AlphaTournamentV32AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV32: TV18 + zero hotspot weight.

    TV18 default avoids scrambled junctions (weight=8.0).
    TV28 strongly prefers them (weight=-10.0).
    TV32 is neutral (weight=0.0) — treats scrambled same as any other junction.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = 0.0


class AlphaTournamentV32Policy(MettagridSemanticPolicy):
    """TournamentV32: TV18 + zero hotspot weight (neutral)."""
    short_names = ["alpha-tournament-v32"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV32AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV33: TV18 + hotspot=-3.0 (between 0 and -5) ───────────────────────

class AlphaTournamentV33AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV33: TV18 + hotspot weight -3.0."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -3.0


class AlphaTournamentV33Policy(MettagridSemanticPolicy):
    """TournamentV33: TV18 + hotspot=-3.0."""
    short_names = ["alpha-tournament-v33"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV33AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV34: TV18 + hotspot=-7.0 (between -5 and -10) ─────────────────────

class AlphaTournamentV34AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV34: TV18 + hotspot weight -7.0."""

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -7.0


class AlphaTournamentV34Policy(MettagridSemanticPolicy):
    """TournamentV34: TV18 + hotspot=-7.0."""
    short_names = ["alpha-tournament-v34"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV34AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV35: TV28 + bolder expansion toward unreachable junctions ──────────

class AlphaTournamentV35AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV35: TV28 (hotspot=-10) + bolder expansion.

    TV28's hotspot change helps re-align scrambled junctions. TV35 also
    reduces the safety margin for expanding toward unreachable junctions
    from hp-20 to hp-12, allowing aligners to reach farther junctions.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV12 aligner + bolder expansion margin."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation (from TV12)
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        # Standard aligner logic
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions — BOLDER margin (12 instead of 20)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 12  # Bolder: 12 vs 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 12:  # Bolder: 12 vs 20
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 300 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV35Policy(MettagridSemanticPolicy):
    """TournamentV35: TV28 + bolder expansion."""
    short_names = ["alpha-tournament-v35"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV35AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV36: TV28 + stronger silicon priority ──────────────────────────────

class AlphaTournamentV36AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV36: TV28 (hotspot=-10) + stronger silicon priority.

    Silicon extractors are scarcer (45 vs avg 52 for other resources).
    TV18 triggers silicon bias at silicon < 50 or when silicon is least.
    TV36 raises the threshold to < 100 to keep miners on silicon longer,
    preventing the late-game silicon crash.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        resources = _shared_resources(state)
        silicon = resources.get("silicon", 0)
        least = _least_resource(resources)

        # Stronger silicon priority: < 100 instead of < 50
        if least == "silicon" or silicon < 100:
            return MacroDirective(resource_bias="silicon")

        return MacroDirective(resource_bias=least)


class AlphaTournamentV36Policy(MettagridSemanticPolicy):
    """TournamentV36: TV28 + stronger silicon priority."""
    short_names = ["alpha-tournament-v36"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV36AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV37: TV28 + pure explore during stagnation (no scramble) ───────────

class AlphaTournamentV37AgentPolicy(AlphaTournamentV18AgentPolicy):
    """TournamentV37: TV28 + no scramble during stagnation.

    TV12 stagnation mode scrambles 1/3 of the time, but each scramble costs
    a heart that could be used for alignment. TV37 skips scrambling entirely
    during stagnation to maximize exploration and potential alignments.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV12 aligner with pure-explore stagnation (no scramble)."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation (from TV12)
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        # Standard aligner logic
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # PURE EXPLORE: no scramble during stagnation (save hearts)
            return self._stagnation_explore(state)

        # NOT stagnating: use TV7's proven idle behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV37Policy(MettagridSemanticPolicy):
    """TournamentV37: TV28 + pure explore stagnation."""
    short_names = ["alpha-tournament-v37"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV37AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV38: TV28 + enemy-directed stagnation exploration ────────────────────

class AlphaTournamentV38AgentPolicy(AlphaTournamentV28AgentPolicy):
    """TournamentV38: TV28 + enemy-directed exploration during stagnation.

    When stagnating, instead of using fixed-offset exploration patterns,
    move toward the centroid of known enemy junctions. This focuses
    exploration in contested areas where scrambling creates frontier
    opportunities, rather than wandering into empty map regions.

    Also: increase stagnation scramble ratio from 1/3 to 1/2 — in
    stagnation, scrambling IS the most productive activity since it
    creates new frontier for aligners.
    """

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Move toward enemy junction clusters instead of fixed offsets."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)

        # Find enemy junctions
        enemy_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )

        if enemy_junctions:
            # Move toward the nearest enemy junction cluster centroid
            # Group by proximity: find the cluster with most junctions
            # Simple heuristic: move toward the nearest enemy junction
            # that has the most enemy neighbors within range 15
            best_target = None
            best_score = -1
            for ej in enemy_junctions:
                neighbors = sum(
                    1 for other in enemy_junctions
                    if other is not ej and _h.manhattan(ej.position, other.position) <= 15
                )
                dist = _h.manhattan(current_pos, ej.position)
                # Score: more neighbors = better target, penalize distance
                score = neighbors * 10.0 - dist
                if score > best_score:
                    best_score = score
                    best_target = ej
            if best_target is not None:
                return self._move_to_known(state, best_target, summary="stag_enemy_explore", vibe="change_vibe_aligner")

        # Fallback to standard stagnation explore if no enemy junctions known
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = (
            (0, -22), (16, -16), (22, 0), (16, 16),
            (0, 22), (-16, 16), (-22, 0), (-16, -16),
            (0, -30), (21, -21), (30, 0), (21, 21),
            (0, 30), (-21, 21), (-30, 0), (-21, -21),
            (0, -35), (25, -25), (35, 0), (25, 25),
            (0, 35), (-25, 25), (-35, 0), (-25, -25),
        )
        offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="stag_explore", vibe="change_vibe_aligner")

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV28 aligner + more aggressive stagnation scramble (1/2 instead of 1/3)."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        # Track junction count — detect stagnation (same as TV12)
        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        # Standard aligner logic
        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Expand toward unreachable junctions
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # STAGNATION: more scramble (1/2 instead of 1/3) — scrambling creates frontier
            if step % 200 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        # NOT stagnating: use TV7's proven idle behavior
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV38Policy(MettagridSemanticPolicy):
    """TournamentV38: TV28 + enemy-directed stagnation exploration."""
    short_names = ["alpha-tournament-v38"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV38AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV39: TV28 + earlier scrambler deployment ─────────────────────────────

class AlphaTournamentV39AgentPolicy(AlphaTournamentV28AgentPolicy):
    """TournamentV39: TV28 + earlier scrambler at step 100 for 5+ agents.

    Theory: denying opponent score early prevents them from building
    momentum. Start first scrambler at step 100 (vs 200) when economy
    can support it (min_res >= 7).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Earlier scrambler: step 100 instead of 200 for 5+ agents."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents: EARLIER scrambler at step 100
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)

        # Scramblers: start at step 100 (was 200) for 5+ agents
        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 100 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTournamentV39Policy(MettagridSemanticPolicy):
    """TournamentV39: TV28 + earlier scrambler deployment."""
    short_names = ["alpha-tournament-v39"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV39AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV40: TV28 + reduced hub_penalty for mid-range junctions ──────────────

class AlphaTournamentV40AgentPolicy(AlphaTournamentV28AgentPolicy):
    """TournamentV40: TV28 + reduced hub penalty for junctions at distance 15-25.

    The current hub_penalty has a steep jump at distance 15 and 25.
    Junctions at distance 15-25 get penalized (hub_dist-15)*3.0+10.0,
    which can be 10-40 points. This may cause aligners to ignore
    good junctions just outside the inner zone.

    Reduce the mid-range penalty to expand the effective alignment radius.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Override targeting with reduced mid-range hub penalty."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _reduced_hub_penalty_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hub_position: tuple[int, int] | None,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            hub_penalty = 0.0
            if hub_position is not None:
                hub_dist = float(_h.manhattan(hub_position, candidate.position))
                if hub_dist > 25:
                    hub_penalty = (hub_dist - 25) * 8.0 + 30.0  # Reduced from 50.0
                elif hub_dist > 15:
                    hub_penalty = (hub_dist - 15) * 1.5 + 5.0  # Reduced from 3.0/10.0
                elif hub_dist > 10:
                    hub_penalty = (hub_dist - 10) * 1.0 + 1.0  # Reduced from 1.5/2.0
                else:
                    hub_penalty = hub_dist * 0.2  # Slightly reduced
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            return (
                distance
                - min(expansion * 5.0, 30.0)
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hub_penalty
                + hotspot_penalty,
                -float(expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _reduced_hub_penalty_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )


class AlphaTournamentV40Policy(MettagridSemanticPolicy):
    """TournamentV40: TV28 + reduced hub penalty."""
    short_names = ["alpha-tournament-v40"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV40AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV41: TV40 (reduced hub penalty) + TV38 (enemy-directed explore) ──────

class AlphaTournamentV41AgentPolicy(AlphaTournamentV40AgentPolicy):
    """TournamentV41: Combines TV40's reduced hub penalty with TV38's
    enemy-directed stagnation exploration and increased scramble ratio.

    Both changes are orthogonal:
    - TV40: aligners consider wider range of junctions (reduced mid-range penalty)
    - TV38: when stagnating, explore toward enemy clusters + scramble more
    """

    def _stagnation_explore(self, state: MettagridState) -> tuple[Action, str]:
        """Move toward enemy junction clusters (from TV38)."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)

        enemy_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )

        if enemy_junctions:
            best_target = None
            best_score = -1
            for ej in enemy_junctions:
                neighbors = sum(
                    1 for other in enemy_junctions
                    if other is not ej and _h.manhattan(ej.position, other.position) <= 15
                )
                dist = _h.manhattan(current_pos, ej.position)
                score = neighbors * 10.0 - dist
                if score > best_score:
                    best_score = score
                    best_target = ej
            if best_target is not None:
                return self._move_to_known(state, best_target, summary="stag_enemy_explore", vibe="change_vibe_aligner")

        # Fallback to standard stagnation explore
        center = (hub.global_x, hub.global_y) if hub is not None else current_pos
        offsets = (
            (0, -22), (16, -16), (22, 0), (16, 16),
            (0, 22), (-16, 16), (-22, 0), (-16, -16),
            (0, -30), (21, -21), (30, 0), (21, 21),
            (0, 30), (-21, 21), (-30, 0), (-21, -21),
            (0, -35), (25, -25), (35, 0), (25, 25),
            (0, 35), (-25, 25), (-35, 0), (-25, -25),
        )
        offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
        target = offsets[offset_index]
        absolute_target = (center[0] + target[0], center[1] + target[1])
        if _h.manhattan(current_pos, absolute_target) <= 3:
            self._explore_index += 1
            offset_index = (self._explore_index + self._agent_id * 5) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
        return self._move_to_position(state, absolute_target, summary="stag_explore", vibe="change_vibe_aligner")

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV40 targeting + TV38 stagnation behavior (1/2 scramble ratio)."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # More scramble (1/2) during stagnation
            if step % 200 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._stagnation_explore(state)

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV41Policy(MettagridSemanticPolicy):
    """TournamentV41: TV40 + TV38 (reduced hub penalty + enemy-directed explore)."""
    short_names = ["alpha-tournament-v41"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV41AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV42: TV28 + zero hub penalty ────────────────────────────────────────

class AlphaTournamentV42AgentPolicy(AlphaTournamentV28AgentPolicy):
    """TournamentV42: TV28 + zero hub penalty.

    Complete removal of hub distance penalty from targeting.
    Aligners choose targets purely by distance + expansion + enemy avoidance.
    Test if hub proximity matters at all for targeting.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Override targeting with zero hub penalty."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _zero_hub_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            return (
                distance
                - min(expansion * 5.0, 30.0)
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hotspot_penalty,
                -float(expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _zero_hub_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )


class AlphaTournamentV42Policy(MettagridSemanticPolicy):
    """TournamentV42: TV28 + zero hub penalty."""
    short_names = ["alpha-tournament-v42"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV42AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV43: TV40 + higher expansion weight ─────────────────────────────────

class AlphaTournamentV43AgentPolicy(AlphaTournamentV40AgentPolicy):
    """TournamentV43: TV40 + double expansion weight.

    TV40 reduced hub penalty lets aligners reach farther junctions.
    TV43 also increases the expansion bonus (from 5.0 to 8.0 per unlocked
    junction) to more strongly prefer junctions that chain into new clusters.
    Cap remains at 30.0 → now needs only 4 junctions instead of 6 for max bonus.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """TV40 targeting with higher expansion weight."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _high_expansion_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hub_position: tuple[int, int] | None,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            # TV40's reduced hub penalty
            hub_penalty = 0.0
            if hub_position is not None:
                hub_dist = float(_h.manhattan(hub_position, candidate.position))
                if hub_dist > 25:
                    hub_penalty = (hub_dist - 25) * 8.0 + 30.0
                elif hub_dist > 15:
                    hub_penalty = (hub_dist - 15) * 1.5 + 5.0
                elif hub_dist > 10:
                    hub_penalty = (hub_dist - 10) * 1.0 + 1.0
                else:
                    hub_penalty = hub_dist * 0.2
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            return (
                distance
                - min(expansion * 8.0, 30.0)  # Higher expansion weight (8.0 vs 5.0)
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hub_penalty
                + hotspot_penalty,
                -float(expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _high_expansion_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )


class AlphaTournamentV43Policy(MettagridSemanticPolicy):
    """TournamentV43: TV40 + higher expansion weight."""
    short_names = ["alpha-tournament-v43"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV43AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV45: TV40 + zero scramblers (test cooperative scoring hypothesis) ───

class AlphaTournamentV45AgentPolicy(AlphaTournamentV40AgentPolicy):
    """TournamentV45: TV40 + zero scramblers.

    Test hypothesis: if scoring is cooperative/shared, scramblers are wasteful.
    All pressure agents become aligners. Idle aligners mine instead of scramble.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV40 budgets but with zero scramblers."""
        aligner, _scrambler = super()._pressure_budgets(state, objective=objective)
        # Move all scrambler budget to aligners
        total = aligner + _scrambler
        return total, 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV40 aligner but NEVER scramble when idle."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # NO scrambling — only explore
            return self._stagnation_explore(state)

        # NO scrambling when idle — alternate between explore and mine
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV45Policy(MettagridSemanticPolicy):
    """TournamentV45: TV40 + zero scramblers."""
    short_names = ["alpha-tournament-v45"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV45AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV44: TV40 + late-game defensive scrambler shift ─────────────────────

class AlphaTournamentV44AgentPolicy(AlphaTournamentV40AgentPolicy):
    """TournamentV44: TV40 + shift to more scramblers after step 5000.

    In late game (step 5000+), resources are depleting and new alignments
    are rare. Shift 1 extra agent to scrambling to deny opponent score
    while we hold existing junctions. Also: reduce late-game heart batch
    target since economy is tighter.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV40 budgets + more scramblers after step 5000."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if objective == "economy_bootstrap":
                return 0, 0
            return 1, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 500 and num_agents >= 4 and min_res >= 14 else 0
            # Late game: shift to 1a+1s instead of 2a
            if step >= 5000 and scrambler_budget == 0 and min_res >= 7:
                scrambler_budget = 1
                aligner_budget = max(aligner_budget - 1, 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)

        scrambler_budget = 0
        if step >= 5000 and min_res >= 7:
            # Late game: 2 scramblers (up from 1) to maintain defensive pressure
            scrambler_budget = min(2, pressure_budget // 2)
        elif step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)
        aligner_budget = max(pressure_budget - scrambler_budget, 0)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTournamentV44Policy(MettagridSemanticPolicy):
    """TournamentV44: TV40 + late-game defensive scrambler shift."""
    short_names = ["alpha-tournament-v44"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV44AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV46: TV25 (tournament #1) + TV40's reduced hub penalty ──────────────

class AlphaTournamentV46AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV46: TV25 (scramble-focused stagnation, #1 at 12.38) +
    TV40's reduced hub penalty for mid-range junctions.

    TV25 is the tournament champion: near-hub explore + 50% scramble during
    stagnation. TV40's reduced hub penalty lets aligners reach farther junctions.
    Combines both improvements.
    """

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self._hotspot_weight = -10.0  # TV28's re-alignment preference

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """TV40's reduced hub penalty targeting."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _reduced_hub_penalty_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hub_position: tuple[int, int] | None,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            hub_penalty = 0.0
            if hub_position is not None:
                hub_dist = float(_h.manhattan(hub_position, candidate.position))
                if hub_dist > 25:
                    hub_penalty = (hub_dist - 25) * 8.0 + 30.0
                elif hub_dist > 15:
                    hub_penalty = (hub_dist - 15) * 1.5 + 5.0
                elif hub_dist > 10:
                    hub_penalty = (hub_dist - 10) * 1.0 + 1.0
                else:
                    hub_penalty = hub_dist * 0.2
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            return (
                distance
                - min(expansion * 5.0, 30.0)
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hub_penalty
                + hotspot_penalty,
                -float(expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _reduced_hub_penalty_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )


class AlphaTournamentV46Policy(MettagridSemanticPolicy):
    """TournamentV46: TV25 (#1) + TV40 reduced hub penalty + TV28 hotspot."""
    short_names = ["alpha-tournament-v46"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV46AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV47: TV25 + earlier stagnation trigger (200 steps instead of 300) ──

class AlphaTournamentV47AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV47: TV25 + earlier stagnation trigger.

    TV25 triggers stagnation after 300 steps with no junction growth.
    TV47 triggers after 200 steps — faster switch to scramble-focused mode.
    If the 12.38 score comes from scrambling, earlier stagnation should help.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with 200-step stagnation trigger."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # Earlier: 200 vs 300
              and step > 400):  # Earlier: 400 vs 500
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV47Policy(MettagridSemanticPolicy):
    """TournamentV47: TV25 + earlier stagnation trigger (200 steps)."""
    short_names = ["alpha-tournament-v47"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV47AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV48: TV25 + higher scramble ratio during stagnation (70%) ──────────

class AlphaTournamentV48AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV48: TV25 + 70% scramble ratio during stagnation.

    TV25 uses 50% scramble during stagnation (step % 200 < 100).
    TV48 uses 70% (step % 200 < 140) — more scrambling creates more
    neutral junctions to realign, potentially boosting shared score.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with 70% scramble during stagnation."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 70% scramble ratio (step % 200 < 140) vs TV25's 50%
            if step % 200 < 140 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV48Policy(MettagridSemanticPolicy):
    """TournamentV48: TV25 + 70% scramble during stagnation."""
    short_names = ["alpha-tournament-v48"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV48AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV49: TV25 + always-scramble stagnation (100% scramble) ─────────────

class AlphaTournamentV49AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV49: TV25 + 100% scramble during stagnation.

    If scrambling creates reusable neutral junctions, go all-in during
    stagnation. No explore at all — just scramble enemy junctions and
    realign them when they become neutral.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with 100% scramble during stagnation."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 100% scramble during stagnation — all-in on creating neutral targets
            if hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            # Fallback: explore if no scramble targets available
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV49Policy(MettagridSemanticPolicy):
    """TournamentV49: TV25 + 100% scramble during stagnation."""
    short_names = ["alpha-tournament-v49"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV49AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV50: TV25 + lower scramble resource threshold (7 instead of 14) ────

class AlphaTournamentV50AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV50: TV25 + lower min_res threshold for scramble.

    TV25 requires min_res >= 14 before idle/stagnation scrambling.
    TV50 lowers to 7 — scramblers deploy with lighter economy.
    In tournament against weaker opponents, this shouldn't matter (resources
    stay high). But if it helps in tighter games, it's a net win.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with lower scramble threshold."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # Lower threshold: min_res >= 7 (vs TV25's 14)
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # Lower threshold for idle scramble too
        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV50Policy(MettagridSemanticPolicy):
    """TournamentV50: TV25 + lower scramble resource threshold."""
    short_names = ["alpha-tournament-v50"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV50AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV51: TV48 (70% scramble) + TV47 (earlier stagnation) combo ─────────

class AlphaTournamentV51AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV51: 70% scramble + earlier stagnation trigger.

    Combines best of TV47 and TV48:
    - 200-step stagnation trigger (vs 300)
    - 70% scramble ratio during stagnation (vs 50%)
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Combined 70% scramble + earlier stagnation."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # TV47: earlier
              and step > 400):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # TV48: 70% scramble ratio
            if step % 200 < 140 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV51Policy(MettagridSemanticPolicy):
    """TournamentV51: TV48 (70% scramble) + TV47 (earlier stagnation)."""
    short_names = ["alpha-tournament-v51"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV51AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV52: TV51 + early dedicated scrambler at step 500 ──────────────────

class AlphaTournamentV52AgentPolicy(AlphaTournamentV51AgentPolicy):
    """TournamentV52: TV51 + dedicated scrambler from step 500.

    TV51 has no dedicated scramblers in budget (0 from TV2/TV7 base).
    TV52 adds 1 dedicated scrambler from step 500 when economy is
    healthy (min_res >= 14). This creates a constant stream of neutral
    junctions for aligners to recapture, complementing stagnation scramble.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV51 budgets + 1 dedicated scrambler from step 500."""
        aligner_budget, scrambler_budget = super()._pressure_budgets(state, objective=objective)
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        num_agents = self.policy_env_info.num_agents

        # Add 1 scrambler from step 500 if economy supports it
        if (step >= 500
            and min_res >= 14
            and num_agents >= 5
            and objective not in {"resource_coverage", "economy_bootstrap"}
            and aligner_budget >= 3):
            scrambler_budget = max(scrambler_budget, 1)
            aligner_budget = max(aligner_budget - 1, 2)  # Trade 1 aligner for scrambler

        return aligner_budget, scrambler_budget


class AlphaTournamentV52Policy(MettagridSemanticPolicy):
    """TournamentV52: TV51 + early dedicated scrambler."""
    short_names = ["alpha-tournament-v52"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV52AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV53: TV48 (70% scramble) + TV46 (reduced hub penalty + hotspot) ────

class AlphaTournamentV53AgentPolicy(AlphaTournamentV46AgentPolicy):
    """TournamentV53: TV46's reduced hub penalty + TV48's 70% scramble.

    TV46 (v406) is averaging 12.59 in early tournament (potential new #1).
    TV48 (70% scramble) shows 2x improvement in self-play.
    Combines both: better targeting + more aggressive stagnation scramble.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV46 aligner with 70% scramble during stagnation (from TV48)."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 70% scramble ratio from TV48 (step % 200 < 140)
            if step % 200 < 140 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV53Policy(MettagridSemanticPolicy):
    """TournamentV53: TV46 (reduced hub penalty) + TV48 (70% scramble)."""
    short_names = ["alpha-tournament-v53"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV53AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV54: TV51 (best self-play) + TV46 (hub penalty + hotspot) ──────────

class AlphaTournamentV54AgentPolicy(AlphaTournamentV46AgentPolicy):
    """TournamentV54: TV46's targeting + TV51's 70% scramble + earlier stagnation.

    Best of everything:
    - TV46: reduced hub penalty + hotspot inversion (tournament 12.59)
    - TV48: 70% scramble ratio during stagnation (2x self-play improvement)
    - TV47: earlier stagnation trigger (200 steps)
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV46 targeting + TV51 stagnation behavior."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # TV47: earlier
              and step > 400):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # TV48: 70% scramble ratio
            if step % 200 < 140 and hearts > 0 and min_res >= 14:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV54Policy(MettagridSemanticPolicy):
    """TournamentV54: TV46 targeting + TV51 stagnation (best combo)."""
    short_names = ["alpha-tournament-v54"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV54AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV55: TV46 targeting (reduced hub penalty) + TV50 threshold (min_res >= 7) ──

class AlphaTournamentV55AgentPolicy(AlphaTournamentV46AgentPolicy):
    """TournamentV55: TV46's reduced hub penalty + TV50's lower scramble threshold.

    Combines the two best tournament elements:
    - TV46's targeting: reduced hub penalty, hotspot=-10 (v406 = 11.97)
    - TV50's scramble threshold: min_res >= 7 (v410 = 12.69, #1)
    TV53 combined TV46 + 70% scramble (12.57, #2). TV55 instead uses TV50's threshold.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV46 targeting + TV50's lower scramble threshold."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # TV50's lower threshold: min_res >= 7 (vs TV25's 14)
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # TV50's lower threshold for idle scramble
        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV55Policy(MettagridSemanticPolicy):
    """TournamentV55: TV46 targeting + TV50 threshold (best combo)."""
    short_names = ["alpha-tournament-v55"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV55AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV56: TV50 + even lower threshold (min_res >= 3) ──────────────────────

class AlphaTournamentV56AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV56: TV25 + very low scramble threshold (min_res >= 3).

    TV50 lowered from 14 to 7 and became #1. TV56 pushes further to 3.
    If the pattern holds (lower = better), this could improve further.
    Risk: too-low threshold may trigger scrambling before economy is ready.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with min_res >= 3 scramble threshold."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # Very low threshold: min_res >= 3
            if step % 200 < 100 and hearts > 0 and min_res >= 3:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # Very low threshold for idle scramble
        if hearts > 0 and min_res >= 3:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV56Policy(MettagridSemanticPolicy):
    """TournamentV56: TV25 + very low scramble threshold (min_res >= 3)."""
    short_names = ["alpha-tournament-v56"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV56AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV57: TV50 threshold (7) + 70% scramble during stagnation ─────────────

class AlphaTournamentV57AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV57: TV50's threshold (7) + TV48's 70% scramble ratio.

    TV50 (threshold=7) is #1 at 12.69. TV48 (70% scramble) was 11.75.
    TV53 combined TV46+TV48 for 12.57 (#2).
    TV57 combines TV50's threshold with higher scramble ratio.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV50 threshold + 70% scramble ratio during stagnation."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 70% scramble (step % 200 < 140) + low threshold (min_res >= 7)
            if step % 200 < 140 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # TV50's lower threshold for idle scramble
        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV57Policy(MettagridSemanticPolicy):
    """TournamentV57: TV50 threshold + 70% scramble ratio."""
    short_names = ["alpha-tournament-v57"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV57AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV58: TV55 (TV46+TV50) + 70% scramble — kitchen sink best combo ───────

class AlphaTournamentV58AgentPolicy(AlphaTournamentV46AgentPolicy):
    """TournamentV58: TV46 targeting + TV50 threshold + 70% scramble.

    The kitchen sink: all three proven tournament improvements combined.
    - TV46 reduced hub penalty targeting
    - TV50 lower scramble threshold (min_res >= 7)
    - TV48 70% scramble ratio during stagnation
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV46 targeting + TV50 threshold + 70% scramble."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 70% scramble + low threshold (7)
            if step % 200 < 140 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV58Policy(MettagridSemanticPolicy):
    """TournamentV58: TV46 + TV50 + 70% scramble (kitchen sink)."""
    short_names = ["alpha-tournament-v58"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV58AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV59: TV55 + aggressive 2-agent (align earlier with 2 agents) ──────────

class AlphaTournamentV59AgentPolicy(AlphaTournamentV55AgentPolicy):
    """TournamentV59: TV55 (TV46+TV50) + more aggressive 2-agent play.

    2-agent matches drag tournament scores. TV7's strategy (pure mine until 500,
    then only align at min_res >= 50) is too conservative. TV59 starts aligning
    earlier with 2 agents: step 200 with min_res >= 14.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV55 budgets + more aggressive 2-agent play."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # More aggressive: start aligning at step 200 with min_res >= 14
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0  # One aligner, one miner
            return 0, 0

        # 4+ agents: use parent's budgets
        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV59Policy(MettagridSemanticPolicy):
    """TournamentV59: TV55 + aggressive 2-agent play."""
    short_names = ["alpha-tournament-v59"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV59AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV60: TV50 + intermediate threshold (min_res >= 10) ───────────────────

class AlphaTournamentV60AgentPolicy(AlphaTournamentV25AgentPolicy):
    """TournamentV60: TV25 + intermediate scramble threshold (min_res >= 10).

    TV25 uses 14, TV50 uses 7, TV56 uses 3. TV60 tests the middle ground.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV25 aligner with min_res >= 10 scramble threshold."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # Intermediate threshold: min_res >= 10
            if step % 200 < 100 and hearts > 0 and min_res >= 10:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 10:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV60Policy(MettagridSemanticPolicy):
    """TournamentV60: TV25 + intermediate scramble threshold (min_res >= 10)."""
    short_names = ["alpha-tournament-v60"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV60AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV61: TV58 + 80% scramble during stagnation ───────────────────────────

class AlphaTournamentV61AgentPolicy(AlphaTournamentV58AgentPolicy):
    """TournamentV61: TV58 (kitchen sink) + 80% scramble ratio.

    TV58 uses 70% scramble (step % 200 < 140). TV61 pushes to 80% (step % 200 < 160).
    TV58 is leading at 12.87 — testing if more scramble helps further.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 80% scramble + low threshold (7)
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV61Policy(MettagridSemanticPolicy):
    """TournamentV61: TV58 + 80% scramble ratio."""
    short_names = ["alpha-tournament-v61"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV61AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV62: TV58 + earlier stagnation (200 steps instead of 300) ────────────

class AlphaTournamentV62AgentPolicy(AlphaTournamentV58AgentPolicy):
    """TournamentV62: TV58 (kitchen sink) + earlier stagnation trigger.

    TV58 triggers stagnation after 300 steps of no junction growth.
    TV62 triggers after 200 steps — switch to scramble mode faster.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # Earlier: 200 vs 300
              and step > 400):  # Earlier: 400 vs 500
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 70% scramble + low threshold (7) — same as TV58
            if step % 200 < 140 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV62Policy(MettagridSemanticPolicy):
    """TournamentV62: TV58 + earlier stagnation (200 steps)."""
    short_names = ["alpha-tournament-v62"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV62AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV63: TV58 + aggressive 2-agent play ──────────────────────────────────

class AlphaTournamentV63AgentPolicy(AlphaTournamentV58AgentPolicy):
    """TournamentV63: TV58 (kitchen sink) + aggressive 2-agent play.

    Combines TV58's improvements with TV59's 2-agent aggression.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """TV58 budgets + aggressive 2-agent play."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV63Policy(MettagridSemanticPolicy):
    """TournamentV63: TV58 + aggressive 2-agent play."""
    short_names = ["alpha-tournament-v63"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV63AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV64: Always-scramble idle aligners (no stagnation detection needed) ───

class AlphaTournamentV64AgentPolicy(AlphaTournamentV46AgentPolicy):
    """TournamentV64: TV46 targeting + always scramble when idle (no stagnation gate).

    All top variants use stagnation detection to decide when idle aligners should
    scramble vs explore. TV64 removes the stagnation gate entirely: idle aligners
    ALWAYS try to scramble (with min_res >= 7). This is simpler and may work
    better since stagnation detection has a 300-step delay.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV46 targeting + always-scramble idle behavior."""
        step = state.step or self._step_index

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Always scramble when idle (no stagnation gate)
        if hearts > 0 and min_res >= 7 and hp > 40:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_scramble", vibe="change_vibe_scrambler")

        # Alternate explore and mine when can't scramble
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV64Policy(MettagridSemanticPolicy):
    """TournamentV64: TV46 targeting + always-scramble idle aligners."""
    short_names = ["alpha-tournament-v64"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV64AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV65: TV55 + more miners early (3 miners first 200 steps) ─────────────

class AlphaTournamentV65AgentPolicy(AlphaTournamentV55AgentPolicy):
    """TournamentV65: TV55 + stronger early economy.

    TV55 uses TV7's budgets which ramp from 2 aligners at step 30.
    TV65 delays alignment until step 200 with a stronger economic base,
    then ramps aggressively. Hypothesis: better economy → more hearts → 
    more sustained alignment.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            return super()._pressure_budgets(state, objective=objective)

        # 5+ agents: delayed start for stronger economy
        if step < 200:
            return 1, 0  # Only 1 aligner, rest mine
        
        # After 200: same as TV7/TV2 budgets
        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV65Policy(MettagridSemanticPolicy):
    """TournamentV65: TV55 + stronger early economy."""
    short_names = ["alpha-tournament-v65"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV65AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV66: TV61 + earlier dedicated scrambler (step 100) ────────────────────

class AlphaTournamentV66AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV66: TV61 (80% scramble) + earlier dedicated scrambler budget.

    TV61 (12.98 at 3m) uses TV7's budgets which don't add scramblers until
    step 200 with min_res >= 7, and only 1 scrambler. TV66 adds a dedicated
    scrambler from step 100, since scrambling is our dominant strategy.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Earlier and more scramblers."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 100 and min_res >= 7 else 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif min_res < 3 and not can_hearts:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Scramblers: earlier (step 100) and more (up to 2)
        scrambler_budget = 0
        if step >= 100 and min_res >= 7:
            scrambler_budget = 1
        if step >= 1000 and min_res >= 14:
            scrambler_budget = 2

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTournamentV66Policy(MettagridSemanticPolicy):
    """TournamentV66: TV61 + earlier dedicated scramblers."""
    short_names = ["alpha-tournament-v66"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV66AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV67: TV64 (always-scramble) + lower HP threshold ─────────────────────

class AlphaTournamentV67AgentPolicy(AlphaTournamentV64AgentPolicy):
    """TournamentV67: TV64's always-scramble + lower HP threshold for scrambling.

    TV64 requires hp > 40 to scramble. TV67 lowers to hp > 25 to be more
    aggressive — agents spend less time retreating and more time scrambling.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV64 always-scramble but with lower HP threshold."""
        step = state.step or self._step_index

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Always scramble when idle — lower HP threshold (25 instead of 40)
        if hearts > 0 and min_res >= 7 and hp > 25:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_scramble", vibe="change_vibe_scrambler")

        # Alternate explore and mine when can't scramble
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV67Policy(MettagridSemanticPolicy):
    """TournamentV67: TV64 always-scramble + lower HP threshold."""
    short_names = ["alpha-tournament-v67"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV67AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV68: TV61 + defensive scramble priority ──────────────────────────────

class AlphaTournamentV68AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV68: TV61 (80% scramble) + prioritize scrambling enemy junctions
    that threaten our friendly junctions.

    The default scramble_target_score has a threat_bonus for junctions near our
    network (10 per threatened friendly junction). TV68 doubles this to 20,
    making defensive scrambling much more attractive.
    """

    def _preferred_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        """Prioritize enemy junctions threatening our network (2x threat weight)."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_position = current_pos if hub is None else hub.position

        enemy_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )
        if not enemy_junctions:
            return None

        friendly_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        )
        neutral_junctions = self._world_model.entities(
            entity_type="junction",
            predicate=lambda entity: entity.owner in {None, "neutral"},
        )

        def defensive_scramble_score(candidate: KnownEntity) -> float:
            distance = float(_h.manhattan(current_pos, candidate.position))
            blocked_neutrals = sum(
                1 for neutral in neutral_junctions
                if _h.manhattan(candidate.position, neutral.position) <= 15
            )
            corner_pressure = min(_h.manhattan(hub_position, candidate.position) / 8.0, 10.0)
            # Doubled threat bonus: 20 per threatened friendly junction (vs 10 default)
            threatened = sum(
                1 for f in friendly_junctions
                if _h.manhattan(candidate.position, f.position) <= 15
            )
            threat_bonus = threatened * 20.0
            return distance - blocked_neutrals * 4.0 - corner_pressure - threat_bonus

        # Use sticky target logic
        sticky = self._sticky_scramble_target(state)
        best = min(enemy_junctions, key=defensive_scramble_score)

        if sticky is None:
            return best

        sticky_score = defensive_scramble_score(sticky)
        best_score = defensive_scramble_score(best)

        if best.position != sticky.position and best_score + 5.0 < sticky_score:
            return best
        return sticky


class AlphaTournamentV68Policy(MettagridSemanticPolicy):
    """TournamentV68: TV61 + defensive scramble priority."""
    short_names = ["alpha-tournament-v68"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV68AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV69: TV61 + 90% scramble + no HP gate ─────────────────────────────────

class AlphaTournamentV69AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV69: TV61 but push scramble even harder.

    TV61 uses 80% scramble during stagnation with hp > 50 and min_res >= 7.
    TV69: 90% scramble during stagnation, no HP gate for idle scramble,
    and always scramble when idle (no stagnation gate needed) if min_res >= 7.
    Essentially combining TV61's 90% stagnation scramble with TV64's no-gate approach.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Ultra-aggressive scrambling: 90% stagnation + no-gate idle."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Always scramble when idle if we can (no stagnation gate, no HP gate)
        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_scramble", vibe="change_vibe_scrambler")

        # During stagnation: 90% scramble even with lower resources
        if self._stagnation_mode and hearts > 0 and min_res >= 3:
            if step % 200 < 180:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV69Policy(MettagridSemanticPolicy):
    """TournamentV69: TV61 + ultra-aggressive scrambling."""
    short_names = ["alpha-tournament-v69"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV69AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV70: TV61 + smarter 2-agent play ──────────────────────────────────────

class AlphaTournamentV70AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV70: TV61 + improved 2-agent performance.

    In 2-agent games, we're paired with 6 opponent bots. Our agents are 2 of
    8 on a team. Current TV7 2-agent strategy: both mine for 500 steps, then
    maybe 1 aligner if min_res >= 50. This is too conservative.

    TV70: Start 1 aligner at step 200 (instead of 500) if economy is okay.
    Also: in 2-agent games, idle aligners should always scramble since the
    other 6 bots on our team are likely aligning.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Less conservative 2-agent play + parent's 5+ agent budgets."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Less conservative: start 1 aligner at step 200
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            # 1 aligner when economy can support it
            if min_res >= 14:
                return 1, 0
            return 0, 0

        # For 4+ agents, use parent's budgets
        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV70Policy(MettagridSemanticPolicy):
    """TournamentV70: TV61 + improved 2-agent play."""
    short_names = ["alpha-tournament-v70"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV70AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV71: TV61 + faster early alignment (3 aligners at step 10) ────────────

class AlphaTournamentV71AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV71: TV61 + aggressive early alignment.

    Score = avg aligned junctions per tick. Early alignments accumulate more
    ticks, so getting junctions early matters disproportionately.
    TV71 starts with 3 aligners at step 10 (vs 2 at step 30), skips early
    batching to send aligners out immediately.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Faster early ramp: 3 aligners at step 10."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Same as TV7
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            # Start 1 aligner immediately, ramp to 2 at step 50
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and min_res >= 7 else 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: aggressive early
        if step < 10:
            return 3, 0  # 3 aligners from step 10 (vs 2 at step 30)
        elif step < 100:
            pressure_budget = 4  # More aggressive early
        elif min_res < 3 and not can_hearts:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Scramblers from step 200
        scrambler_budget = 0
        if step >= 200 and min_res >= 7:
            scrambler_budget = 1

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Same as TV61 but skip batching for first 100 steps to align faster."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Skip batching for first 100 steps — align ASAP with whatever hearts we have
        if step < 100:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 80% scramble during stagnation (same as TV61)
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV71Policy(MettagridSemanticPolicy):
    """TournamentV71: TV61 + aggressive early alignment."""
    short_names = ["alpha-tournament-v71"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV71AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV72: TV69 + TV70 + TV71 combined (all improvements) ──────────────────

class AlphaTournamentV72AgentPolicy(AlphaTournamentV69AgentPolicy):
    """TournamentV72: Kitchen sink v2 — all improvements combined.

    - TV69's ultra-aggressive scrambling (no gates, 90% stag, min_res >= 3)
    - TV70's improved 2-agent play (aligner at step 200 if min_res >= 14)
    - TV71's aggressive early alignment (3 aligners at step 10)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """All-in: faster early ramp + less conservative 2-agent."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # TV70's less conservative 2-agent
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            scrambler_budget = 1 if step >= 200 and min_res >= 7 else 0
            if objective == "economy_bootstrap":
                return min(aligner_budget, 1), 0
            return aligner_budget, scrambler_budget

        # 5+ agents: TV71's aggressive early
        if step < 10:
            return 3, 0
        elif step < 100:
            pressure_budget = 4
        elif min_res < 3 and not can_hearts:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        scrambler_budget = 0
        if step >= 200 and min_res >= 7:
            scrambler_budget = 1

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTournamentV72Policy(MettagridSemanticPolicy):
    """TournamentV72: Kitchen sink v2 — all improvements."""
    short_names = ["alpha-tournament-v72"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV72AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV73: TV61 but 85% scramble during stagnation ──────────────────────────

class AlphaTournamentV73AgentPolicy(AlphaTournamentV58AgentPolicy):
    """TournamentV73: TV58 (kitchen sink) + 85% scramble ratio.

    TV61 uses 80% (step % 200 < 160). TV73 bumps to 85% (step % 200 < 170).
    Testing if there's a sweet spot between 80% (12.83) and 100% (9.05).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 85% scramble + low threshold (7)
            if step % 200 < 170 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV73Policy(MettagridSemanticPolicy):
    """TournamentV73: TV58 + 85% scramble ratio."""
    short_names = ["alpha-tournament-v73"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV73AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV74: TV61 + more aligners (6 for 8-agent) ────────────────────────────

class AlphaTournamentV74AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV74: TV61 + more aligners at mid-game.

    With 8 agents, TV7 budgets max at 5 pressure (aligners+scramblers) + 3 miners.
    TV74 bumps to 6 pressure + 2 miners when economy is healthy (min_res >= 50).
    More aligners = more junctions aligned AND more idle aligners scrambling.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """More aligners when economy is healthy."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Same as TV7
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            return super()._pressure_budgets(state, objective=objective)

        # 5+ agents: economy-responsive with more pressure when healthy
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif min_res < 3 and not can_hearts:
            pressure_budget = max(2, num_agents // 3)
        elif min_res < 7:
            pressure_budget = min(4, num_agents - 2)
        elif min_res >= 50:
            # Economy healthy: max pressure (6 for 8 agents)
            pressure_budget = min(6, num_agents - 2)
        else:
            pressure_budget = min(5, num_agents - 2)

        # Scramblers
        scrambler_budget = 0
        if step >= 3000 and min_res >= 14:
            scrambler_budget = min(2, pressure_budget // 3)
        elif step >= 200 and min_res >= 7:
            scrambler_budget = min(1, pressure_budget // 3)

        aligner_budget = max(pressure_budget - scrambler_budget, 1)
        if objective == "economy_bootstrap":
            return min(aligner_budget, 2), 0
        return aligner_budget, scrambler_budget


class AlphaTournamentV74Policy(MettagridSemanticPolicy):
    """TournamentV74: TV61 + more aligners when economy healthy."""
    short_names = ["alpha-tournament-v74"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV74AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV75: TV61 + lower heart batch target (2 instead of 3) ─────────────────

class AlphaTournamentV75AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV75: TV61 + lower heart batch target.

    Default batch target is 3 hearts for aligners. TV75 lowers to 2, so aligners
    leave hub faster to align. Each alignment only costs 1 heart, so 2 is enough
    for 2 alignments per trip.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Same as TV61 but with lower batch target via modified batching check."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Modified batching: only batch if hearts < 2 (instead of default 3)
        # and we're adjacent to hub
        if step >= 200 and hearts < 2 and hub is not None:
            hub_pos = hub.position if hub else None
            if hub_pos and _h.manhattan(_h.absolute_position(state), hub_pos) <= 1:
                if _h.team_can_refill_hearts(state):
                    return self._move_to_known(state, hub, summary="batch_hearts_low", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 80% scramble (same as TV61)
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV75Policy(MettagridSemanticPolicy):
    """TournamentV75: TV61 + lower heart batch target."""
    short_names = ["alpha-tournament-v75"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV75AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV76: TV61 + chain-value targeting + smarter expansion ──────────────────

def _chain_expansion_value(
    candidate_pos: tuple[int, int],
    unreachable: list[KnownEntity],
    network_positions: set[tuple[int, int]],
    max_depth: int = 3,
) -> int:
    """Compute transitive expansion value: how many junctions become reachable
    through a chain of alignments starting from candidate.

    Unlike simple expansion (1-hop count), this counts the full tree of junctions
    unlocked by aligning the candidate, then aligning newly reachable junctions, etc.
    """
    unreachable_by_pos = {e.position: e for e in unreachable}
    reached: set[tuple[int, int]] = set()
    frontier = {candidate_pos}
    combined_network = network_positions | {candidate_pos}

    for _ in range(max_depth):
        new_frontier: set[tuple[int, int]] = set()
        for src in frontier:
            for pos, entity in unreachable_by_pos.items():
                if pos in reached or pos in combined_network:
                    continue
                if _h.manhattan(src, pos) <= 15:  # _JUNCTION_ALIGN_DISTANCE
                    reached.add(pos)
                    new_frontier.add(pos)
        if not new_frontier:
            break
        frontier = new_frontier
        combined_network |= new_frontier

    return len(reached)


class AlphaTournamentV76AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV76: TV61 + chain-value targeting + smarter expand-toward.

    Two key changes:
    1. When scoring alignment targets, use chain expansion (transitive reachability)
       instead of 1-hop expansion. This prefers junctions that bridge to clusters.
    2. When no reachable junctions exist (expand_toward_junction), pick the unreachable
       junction with the highest chain value as the expansion target rather than just
       the nearest one.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """TV46 reduced hub penalty targeting + chain-value expansion scoring."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        network_positions = {e.position for e in network_sources}
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _chain_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hub_position: tuple[int, int] | None,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            # Chain-value: count transitive reachability (depth 3)
            chain_val = _chain_expansion_value(
                candidate.position, unreachable, network_positions, max_depth=3
            )
            # Also count immediate expansion for tiebreaking
            imm_expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            # TV46 reduced hub penalty
            hub_penalty = 0.0
            if hub_position is not None:
                hub_dist = float(_h.manhattan(hub_position, candidate.position))
                if hub_dist > 25:
                    hub_penalty = (hub_dist - 25) * 8.0 + 30.0
                elif hub_dist > 15:
                    hub_penalty = (hub_dist - 15) * 1.5 + 5.0
                elif hub_dist > 10:
                    hub_penalty = (hub_dist - 10) * 1.0 + 1.0
                else:
                    hub_penalty = hub_dist * 0.2
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            # Chain value weighted at 7.0 per junction (stronger than imm 5.0), cap 70
            chain_bonus = min(chain_val * 7.0, 70.0)
            return (
                distance
                - chain_bonus
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hub_penalty
                + hotspot_penalty,
                -float(chain_val * 10 + imm_expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _chain_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Smart expand-toward: pick unreachable junction with best chain value
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            # Score by chain value (how many junctions this unlocks) minus distance
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # 80% scramble + low threshold (7) — same as TV61
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV76Policy(MettagridSemanticPolicy):
    """TournamentV76: TV61 + chain-value targeting + smarter expansion."""
    short_names = ["alpha-tournament-v76"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV76AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV77: TV76 + adaptive scramble ratio (score-based) ──────────────────────

class AlphaTournamentV77AgentPolicy(AlphaTournamentV76AgentPolicy):
    """TournamentV77: TV76 (chain-value) + adaptive scramble ratio.

    Instead of fixed 80% scramble during stagnation, dynamically adjust:
    - If we have many junctions (>= peak * 0.9): scramble 90% (protect lead)
    - If we have moderate junctions (>= peak * 0.7): scramble 80% (TV61 default)
    - If below that: scramble 60% and explore more (need to find new territory)
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Smart expand-toward (same as TV76)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # Adaptive scramble ratio based on current junction count vs peak
            if self._peak_junction_count > 0:
                ratio = friendly_count / self._peak_junction_count
            else:
                ratio = 0.0

            if ratio >= 0.9:
                scramble_steps = 180  # 90% scramble — protecting strong lead
            elif ratio >= 0.7:
                scramble_steps = 160  # 80% scramble — TV61 default
            else:
                scramble_steps = 120  # 60% scramble — need more exploration

            if step % 200 < scramble_steps and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV77Policy(MettagridSemanticPolicy):
    """TournamentV77: TV76 + adaptive scramble ratio."""
    short_names = ["alpha-tournament-v77"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV77AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV78: TV61 + smarter expand-toward only (no chain targeting) ────────────

class AlphaTournamentV78AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV78: TV61 + smarter expand-toward using chain value.

    Keep TV61's targeting unchanged (proven), but when no reachable junctions
    exist, use chain value to pick a better expansion target instead of just
    the nearest one.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward (only change from TV61)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV78Policy(MettagridSemanticPolicy):
    """TournamentV78: TV61 + chain-value expand-toward only."""
    short_names = ["alpha-tournament-v78"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV78AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV79: TV61 + bridge-aware scramble targeting ────────────────────────────

class AlphaTournamentV79AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV79: TV61 + network-bridging scramble priority.

    When choosing which enemy junction to scramble, add bonus for junctions that,
    once neutralized and re-aligned, would extend our network toward unreachable
    neutral junctions. This turns scrambling from pure offense into network expansion.
    """

    def _best_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        neutral_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner in {None, "neutral"})
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        enemy_junctions = self._known_junctions(
            state, predicate=lambda entity: entity.owner not in {None, "neutral", team_id},
        )
        if not enemy_junctions:
            return None
        directed_candidate = self._directive_target_candidate(enemy_junctions)
        if directed_candidate is not None:
            return directed_candidate

        hub_position = current_pos if hub is None else hub.position

        # Build network positions for chain-value calculation
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        # Unreachable neutrals — junctions we'd like to reach
        network_sources = [*hubs, *friendly_junctions]
        unreachable_neutrals = [
            j for j in neutral_junctions
            if not _h.within_alignment_network(j.position, network_sources)
        ]

        def _bridge_scramble_score(candidate: KnownEntity) -> tuple[float, float]:
            base_score, base_tie = _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_position,
                candidate=candidate,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            )
            # Check: if we scramble this enemy junction, it becomes neutral.
            # If it's within alignment range of our network, we can re-align it.
            # If re-aligned, what chain value does it unlock?
            bridge_bonus = 0.0
            if _h.within_alignment_network(candidate.position, network_sources):
                # This enemy junction is within our network range — scrambling + re-aligning
                # would extend our network. Calculate chain value from it.
                chain_val = _chain_expansion_value(
                    candidate.position, unreachable_neutrals, network_positions, max_depth=2
                )
                bridge_bonus = chain_val * 8.0  # Strong bonus per unlocked junction

            return (base_score - bridge_bonus, base_tie - bridge_bonus)

        return min(
            enemy_junctions,
            key=lambda entity: (
                _bridge_scramble_score(entity),
                entity.position,
            ),
        )


class AlphaTournamentV79Policy(MettagridSemanticPolicy):
    """TournamentV79: TV61 + bridge-aware scramble targeting."""
    short_names = ["alpha-tournament-v79"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV79AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV80: TV61 + earlier stagnation exit when junction lost ─────────────────

class AlphaTournamentV80AgentPolicy(AlphaTournamentV61AgentPolicy):
    """TournamentV80: TV61 + dynamic stagnation exit.

    TV61 enters stagnation mode after 300 steps with no growth and stays there.
    TV80 adds: exit stagnation temporarily when we lose junctions (enemy scramble)
    to prioritize re-alignment over offensive scrambling.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))

        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        # TV80 addition: if we've lost junctions recently (below 80% of peak),
        # temporarily suppress stagnation to prioritize re-alignment
        re_alignment_priority = False
        if self._stagnation_mode and self._peak_junction_count > 0:
            if friendly_count < self._peak_junction_count * 0.8:
                re_alignment_priority = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50 and not re_alignment_priority:
            # 80% scramble (same as TV61) when not in re-alignment priority
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if re_alignment_priority:
            # Re-alignment priority: 50% scramble, more exploration to find
            # recently-scrambled junctions to re-align (hotspot_weight=-10 helps)
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="realign_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="realign_explore")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV80Policy(MettagridSemanticPolicy):
    """TournamentV80: TV61 + dynamic stagnation exit on junction loss."""
    short_names = ["alpha-tournament-v80"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV80AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV81: TV79 (bridge scramble) + TV70 (improved 2-agent) ─────────────────

class AlphaTournamentV81AgentPolicy(AlphaTournamentV79AgentPolicy):
    """TournamentV81: TV79 (bridge-aware scramble) + TV70 (improved 2-agent play).

    TV79's bridge-aware scramble scored 3.33 in self-play (best so far).
    TV70's 2-agent improvement scored 12.06 in tournament (2nd best).
    Combining both — bridge scramble could help especially in 2-agent games
    where efficient network expansion matters more.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # TV70 improvement: skip batching for 2-agent games until step 200
        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV81Policy(MettagridSemanticPolicy):
    """TournamentV81: TV79 + TV70 (bridge scramble + 2-agent)."""
    short_names = ["alpha-tournament-v81"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV81AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV82: TV81 (bridge scramble + 2-agent) + chain-value alignment targeting ──

class AlphaTournamentV82AgentPolicy(AlphaTournamentV81AgentPolicy):
    """TournamentV82: TV81 + chain-value alignment targeting from TV76.

    TV81 has bridge-aware scramble + 2-agent improvement but uses TV61's simple
    alignment targeting. TV76's chain-value scoring prefers junctions that unlock
    chains of further junctions. Combining should give best-of-all-worlds.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """TV76-style chain-value targeting with TV46 reduced hub penalty."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        network_positions = {e.position for e in network_sources}
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate
        enemy_junctions = self._known_junctions(
            state, predicate=lambda junction: junction.owner not in {None, "neutral", team_id},
        )
        unreachable = [
            entity
            for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"})
            if entity not in candidates
        ]

        def _chain_score(
            current_position: tuple[int, int],
            candidate: KnownEntity,
            unreachable: list[KnownEntity],
            enemy_junctions: list[KnownEntity],
            claimed_by_other: bool,
            hub_position: tuple[int, int] | None,
            hotspot_count: int,
            hotspot_weight: float,
        ) -> tuple[float, float]:
            distance = float(_h.manhattan(current_position, candidate.position))
            chain_val = _chain_expansion_value(
                candidate.position, unreachable, network_positions, max_depth=3
            )
            imm_expansion = sum(
                1 for entity in unreachable if _h.manhattan(candidate.position, entity.position) <= 15
            )
            enemy_aoe = (
                1.0
                if any(_h.manhattan(candidate.position, enemy.position) <= 5 for enemy in enemy_junctions)
                else 0.0
            )
            hub_penalty = 0.0
            if hub_position is not None:
                hub_dist = float(_h.manhattan(hub_position, candidate.position))
                if hub_dist > 25:
                    hub_penalty = (hub_dist - 25) * 8.0 + 30.0
                elif hub_dist > 15:
                    hub_penalty = (hub_dist - 15) * 1.5 + 5.0
                elif hub_dist > 10:
                    hub_penalty = (hub_dist - 10) * 1.0 + 1.0
                else:
                    hub_penalty = hub_dist * 0.2
            hotspot_penalty = min(hotspot_count, 3) * hotspot_weight
            chain_bonus = min(chain_val * 7.0, 70.0)
            return (
                distance
                - chain_bonus
                + enemy_aoe * 8.0
                + (20.0 if claimed_by_other else 0.0)
                + hub_penalty
                + hotspot_penalty,
                -float(chain_val * 10 + imm_expansion),
            )

        return min(
            candidates,
            key=lambda entity: (
                _chain_score(
                    current_position=current_pos,
                    candidate=entity,
                    unreachable=unreachable,
                    enemy_junctions=enemy_junctions,
                    claimed_by_other=_h.is_claimed_by_other(
                        claims=self._shared_claims,
                        candidate=entity.position,
                        agent_id=self._agent_id,
                        step=self._step_index,
                    ),
                    hub_position=hub_pos,
                    hotspot_count=self._junction_hotspot_count(entity, hub),
                    hotspot_weight=self._hotspot_weight,
                )
            ),
        )


class AlphaTournamentV82Policy(MettagridSemanticPolicy):
    """TournamentV82: TV81 + chain-value alignment targeting."""
    short_names = ["alpha-tournament-v82"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV82AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV83: TV81 + coordinated scramble targeting ──────────────────────────────

class AlphaTournamentV83AgentPolicy(AlphaTournamentV81AgentPolicy):
    """TournamentV83: TV81 + coordinated scramble targets via shared_claims.

    When multiple agents scramble, they often target the same enemy junction,
    wasting hearts. TV83 adds a penalty for scramble targets already claimed
    by another agent, spreading pressure across more enemy junctions.
    """

    def _best_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        neutral_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner in {None, "neutral"})
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        enemy_junctions = self._known_junctions(
            state, predicate=lambda entity: entity.owner not in {None, "neutral", team_id},
        )
        if not enemy_junctions:
            return None
        directed_candidate = self._directive_target_candidate(enemy_junctions)
        if directed_candidate is not None:
            return directed_candidate

        hub_position = current_pos if hub is None else hub.position

        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}
        network_sources = [*hubs, *friendly_junctions]
        unreachable_neutrals = [
            j for j in neutral_junctions
            if not _h.within_alignment_network(j.position, network_sources)
        ]

        def _coordinated_bridge_score(candidate: KnownEntity) -> tuple[float, float]:
            base_score, base_tie = _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_position,
                candidate=candidate,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            )
            bridge_bonus = 0.0
            if _h.within_alignment_network(candidate.position, network_sources):
                chain_val = _chain_expansion_value(
                    candidate.position, unreachable_neutrals, network_positions, max_depth=2
                )
                bridge_bonus = chain_val * 8.0
            claimed_by_other = _h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            )
            coordination_penalty = 30.0 if claimed_by_other else 0.0
            return (base_score - bridge_bonus + coordination_penalty, base_tie - bridge_bonus)

        best = min(
            enemy_junctions,
            key=lambda entity: (
                _coordinated_bridge_score(entity),
                entity.position,
            ),
        )
        self._shared_claims[best.position] = (self._agent_id, self._step_index)
        return best


class AlphaTournamentV83Policy(MettagridSemanticPolicy):
    """TournamentV83: TV81 + coordinated scramble targeting."""
    short_names = ["alpha-tournament-v83"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV83AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV84: TV82 + TV83 (chain-value targeting + coordinated scramble) ─────────

class AlphaTournamentV84AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV84: TV82 (chain-value targeting) + TV83 (coordinated scramble).

    Combines all improvements:
    - Bridge-aware scramble targeting (TV79)
    - 2-agent improvement (TV70)
    - Chain-value alignment targeting (TV76)
    - Coordinated scramble via shared_claims (TV83)
    """

    def _best_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        """Same coordinated bridge scramble as TV83."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        neutral_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner in {None, "neutral"})
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        enemy_junctions = self._known_junctions(
            state, predicate=lambda entity: entity.owner not in {None, "neutral", team_id},
        )
        if not enemy_junctions:
            return None
        directed_candidate = self._directive_target_candidate(enemy_junctions)
        if directed_candidate is not None:
            return directed_candidate

        hub_position = current_pos if hub is None else hub.position
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}
        network_sources = [*hubs, *friendly_junctions]
        unreachable_neutrals = [
            j for j in neutral_junctions
            if not _h.within_alignment_network(j.position, network_sources)
        ]

        def _coordinated_bridge_score(candidate: KnownEntity) -> tuple[float, float]:
            base_score, base_tie = _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_position,
                candidate=candidate,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            )
            bridge_bonus = 0.0
            if _h.within_alignment_network(candidate.position, network_sources):
                chain_val = _chain_expansion_value(
                    candidate.position, unreachable_neutrals, network_positions, max_depth=2
                )
                bridge_bonus = chain_val * 8.0
            claimed_by_other = _h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            )
            coordination_penalty = 30.0 if claimed_by_other else 0.0
            return (base_score - bridge_bonus + coordination_penalty, base_tie - bridge_bonus)

        best = min(
            enemy_junctions,
            key=lambda entity: (
                _coordinated_bridge_score(entity),
                entity.position,
            ),
        )
        self._shared_claims[best.position] = (self._agent_id, self._step_index)
        return best


class AlphaTournamentV84Policy(MettagridSemanticPolicy):
    """TournamentV84: chain-value targeting + coordinated scramble."""
    short_names = ["alpha-tournament-v84"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV84AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV85: TV84 + chain-value expand-toward ───────────────────────────────────

class AlphaTournamentV85AgentPolicy(AlphaTournamentV84AgentPolicy):
    """TournamentV85: TV84 + chain-value scoring for expand_toward_junction.

    TV84 uses chain-value for alignment targeting and coordinated scramble,
    but expand-toward still picks nearest unreachable junction. TV85 picks
    the unreachable junction with highest chain value (bridge potential),
    weighted against distance. Same as TV76's expand-toward logic.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # TV70 improvement: skip batching for 2-agent games until step 200
        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward: pick unreachable junction with best chain value
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            # Score by chain value minus distance (prefer high-value even if farther)
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV85Policy(MettagridSemanticPolicy):
    """TournamentV85: all improvements + chain-value expand-toward."""
    short_names = ["alpha-tournament-v85"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV85AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV86: TV82 + chain-value expand-toward ───────────────────────────────────

class AlphaTournamentV86AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV86: TV82 (best so far) + chain-value expand-toward.

    TV82 has chain-value alignment targeting + bridge scramble + 2-agent.
    TV86 adds chain-value scoring for expand_toward_junction (pick unreachable
    junctions with highest bridge potential, not just nearest).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV86Policy(MettagridSemanticPolicy):
    """TournamentV86: TV82 + chain-value expand-toward."""
    short_names = ["alpha-tournament-v86"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV86AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV87: TV82 + light scramble coordination (penalty=15) ────────────────────

class AlphaTournamentV87AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV87: TV82 + light coordinated scramble targeting.

    TV83 used penalty=30 for coordinated scramble but TV84 (which included it)
    scored lower than TV82 (which doesn't). Try lighter penalty=15 to gently
    spread scramble targets without over-penalizing good targets.
    """

    def _best_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        neutral_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner in {None, "neutral"})
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        enemy_junctions = self._known_junctions(
            state, predicate=lambda entity: entity.owner not in {None, "neutral", team_id},
        )
        if not enemy_junctions:
            return None
        directed_candidate = self._directive_target_candidate(enemy_junctions)
        if directed_candidate is not None:
            return directed_candidate

        hub_position = current_pos if hub is None else hub.position
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda e: e.team == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}
        network_sources = [*hubs, *friendly_junctions]
        unreachable_neutrals = [
            j for j in neutral_junctions
            if not _h.within_alignment_network(j.position, network_sources)
        ]

        def _light_coordinated_score(candidate: KnownEntity) -> tuple[float, float]:
            base_score, base_tie = _h.scramble_target_score(
                current_position=current_pos,
                hub_position=hub_position,
                candidate=candidate,
                neutral_junctions=neutral_junctions,
                friendly_junctions=friendly_junctions,
            )
            bridge_bonus = 0.0
            if _h.within_alignment_network(candidate.position, network_sources):
                chain_val = _chain_expansion_value(
                    candidate.position, unreachable_neutrals, network_positions, max_depth=2
                )
                bridge_bonus = chain_val * 8.0
            # Light coordination: penalty=15 instead of 30
            claimed_by_other = _h.is_claimed_by_other(
                claims=self._shared_claims,
                candidate=candidate.position,
                agent_id=self._agent_id,
                step=self._step_index,
            )
            coordination_penalty = 15.0 if claimed_by_other else 0.0
            return (base_score - bridge_bonus + coordination_penalty, base_tie - bridge_bonus)

        best = min(
            enemy_junctions,
            key=lambda entity: (
                _light_coordinated_score(entity),
                entity.position,
            ),
        )
        self._shared_claims[best.position] = (self._agent_id, self._step_index)
        return best


class AlphaTournamentV87Policy(MettagridSemanticPolicy):
    """TournamentV87: TV82 + light scramble coordination."""
    short_names = ["alpha-tournament-v87"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV87AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV88: TV82 + TV70's pressure_budgets fix ─────────────────────────────────

class AlphaTournamentV88AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV88: TV82 + proper 2-agent pressure_budgets from TV70.

    BUG FIX: TV81/TV82 inherit TV7's _pressure_budgets which requires
    min_res >= 50 for 2-agent aligner activation. TV70 lowered this to
    min_res >= 14. TV81 copied TV70's _aligner_action changes but missed
    the _pressure_budgets override. TV88 adds it back.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # TV70's less conservative 2-agent play
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        return super()._pressure_budgets(state, objective=objective)


class AlphaTournamentV88Policy(MettagridSemanticPolicy):
    """TournamentV88: TV82 + proper 2-agent budget fix."""
    short_names = ["alpha-tournament-v88"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV88AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV89: TV88 + chain-value expand-toward ────────────────────────────────────

class AlphaTournamentV89AgentPolicy(AlphaTournamentV88AgentPolicy):
    """TournamentV89: TV88 (TV82 + budget fix) + chain-value expand-toward.

    The full kitchen sink: chain-value alignment targeting, bridge-aware scramble,
    proper 2-agent budget, and chain-value expand-toward scoring.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV89Policy(MettagridSemanticPolicy):
    """TournamentV89: full kitchen sink with budget fix."""
    short_names = ["alpha-tournament-v89"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV89AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV90: TV88 + aggressive 2-agent aligner (min_res >= 7) ────────────────────

class AlphaTournamentV90AgentPolicy(AlphaTournamentV88AgentPolicy):
    """TournamentV90: TV88 + lower 2-agent aligner threshold.

    TV88 requires min_res >= 14 before activating an aligner in 2-agent games.
    TV90 lowers to min_res >= 7 (just enough to afford hearts). This gets the
    aligner out earlier, which is critical in 2v6 where we need to align fast.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV90Policy(MettagridSemanticPolicy):
    """TournamentV90: TV88 + aggressive 2-agent aligner."""
    short_names = ["alpha-tournament-v90"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV90AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV91: TV88 + lower heart batch for small teams ────────────────────────────

class AlphaTournamentV91AgentPolicy(AlphaTournamentV88AgentPolicy):
    """TournamentV91: TV88 + reduced heart batching for 2-agent games.

    In 2-agent games, waiting at hub for 3 hearts wastes precious time.
    Each heart costs 7 of each element. With only 2 miners and hub starting
    with 6 of each, the wait is very long. Reduce batch to 2 for faster
    aligner deployment.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        # Override heart batching for small teams
        step = state.step or self._step_index
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        if num_agents <= 2:
            team_id = _h.team_id(state)
            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            # Reduced batch: only batch to 2 hearts (not 3) for 2-agent teams
            if step < 200:
                pass
            elif hearts < 2 and hub is not None and _h.manhattan(_h.absolute_position(state), hub.position) <= 1:
                if _h.team_can_refill_hearts(state):
                    return self._move_to_known(state, hub, summary="batch_hearts_2", vibe="change_vibe_heart")

            # Rest is same as parent
            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()
            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            # Chain-value expand-toward (from TV89)
            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
            friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
            network_positions = {e.position for e in [*hubs, *friendly_junctions]}
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                best = min(targets, key=lambda j: (
                    _h.manhattan(current_pos, j.position)
                    - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
                ))
                dist = _h.manhattan(current_pos, best.position)
                if dist < hp - 20:
                    return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

            min_res = _h.team_min_resource(state)

            # Skip scrambling in 2-agent mode — too costly
            if step % 200 < 100:
                return self._explore_action(state, role="aligner", summary="find_neutral_junction")
            if min_res < 30:
                return self._miner_action(state, summary_prefix="idle_align_")
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        return super()._aligner_action(state)


class AlphaTournamentV91Policy(MettagridSemanticPolicy):
    """TournamentV91: TV88 + reduced heart batch for 2-agent."""
    short_names = ["alpha-tournament-v91"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV91AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV92: TV90 + TV91 (aggressive aligner + reduced batch) ───────────────────

class AlphaTournamentV92AgentPolicy(AlphaTournamentV91AgentPolicy):
    """TournamentV92: TV90 (aggressive 2-agent aligner) + TV91 (reduced batch).

    Combines both 2-agent improvements:
    - Lower aligner threshold: min_res >= 7 (from TV90)
    - Lower heart batch: 2 hearts instead of 3 (from TV91)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV92Policy(MettagridSemanticPolicy):
    """TournamentV92: aggressive 2-agent aligner + reduced batch."""
    short_names = ["alpha-tournament-v92"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV92AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV93: TV88 + no scramble in 2-agent + earlier aligner (step 100) ──────────

class AlphaTournamentV93AgentPolicy(AlphaTournamentV88AgentPolicy):
    """TournamentV93: TV88 + skip scramble in 2-agent + earlier aligner activation.

    In 2v6, scrambling wastes one of only 2 agents. Also lower the step
    threshold from 200 to 100 for 2-agent games — get aligning faster.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Earlier activation: step 100 instead of 200
            if step < 100 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Override to skip scrambling entirely in 2-agent mode."""
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8
        if num_agents <= 2:
            step = state.step or self._step_index
            team_id = _h.team_id(state)

            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            if step >= 100:
                if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
                    return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()
            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            # Expand toward (from TV89)
            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
            friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
            network_positions = {e.position for e in [*hubs, *friendly_junctions]}
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                best = min(targets, key=lambda j: (
                    _h.manhattan(current_pos, j.position)
                    - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
                ))
                dist = _h.manhattan(current_pos, best.position)
                if dist < hp - 20:
                    return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

            min_res = _h.team_min_resource(state)

            # NO SCRAMBLING in 2-agent mode — just mine or explore
            if self._stagnation_mode:
                return self._explore_action(state, role="aligner", summary="stag_find_junction")

            if step % 200 < 100:
                return self._explore_action(state, role="aligner", summary="find_neutral_junction")
            if min_res < 30:
                return self._miner_action(state, summary_prefix="idle_align_")
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        return super()._aligner_action(state)


class AlphaTournamentV93Policy(MettagridSemanticPolicy):
    """TournamentV93: earlier 2-agent aligner + no scramble."""
    short_names = ["alpha-tournament-v93"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV93AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV94: TV88 + zero scramble (all team sizes) ──────────────────────────────

class AlphaTournamentV94AgentPolicy(AlphaTournamentV88AgentPolicy):
    """TournamentV94: TV88 + no scrambling at all.

    Key insight: tournament scores are shared between both teams (game-level
    metric). Scrambling reduces total aligned junctions, hurting the game score.
    All current matches are self-play, so scrambling always hurts.
    Remove ALL scramble behavior: stagnation scramble, idle scramble, and
    scrambler budget.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        """Zero scrambler budget for all team sizes."""
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0  # Always 0 scramblers

        # 5+ agents — same as TV88 but never any scramblers
        if step < 30:
            return 2, 0
        elif step < 100:
            pressure_budget = 3
        elif step < 3000:
            pressure_budget = min(5, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)
            elif min_res < 7:
                pressure_budget = min(4, num_agents - 2)
        else:
            pressure_budget = min(6, num_agents - 2)
            if min_res < 3 and not can_hearts:
                pressure_budget = max(2, num_agents // 3)

        return pressure_budget, 0  # ALL aligners, ZERO scramblers

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Override to remove all scrambling from idle/stagnation behavior."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}

        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # NO SCRAMBLING — just explore or mine
        if self._stagnation_mode:
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV94Policy(MettagridSemanticPolicy):
    """TournamentV94: TV88 + zero scramble."""
    short_names = ["alpha-tournament-v94"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV94AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV95: TV94 + aggressive 2-agent (min_res >= 7) ───────────────────────────

class AlphaTournamentV95AgentPolicy(AlphaTournamentV94AgentPolicy):
    """TournamentV95: TV94 (zero scramble) + aggressive 2-agent aligner.

    Combines no-scramble with lower 2-agent aligner threshold (min_res >= 7).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV94AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV95Policy(MettagridSemanticPolicy):
    """TournamentV95: zero scramble + aggressive 2-agent."""
    short_names = ["alpha-tournament-v95"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV95AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV96: TV95 + more aligners in 4v4 ────────────────────────────────────────

class AlphaTournamentV96AgentPolicy(AlphaTournamentV95AgentPolicy):
    """TournamentV96: TV95 + 3 aligners in 4v4.

    In 4v4 with zero scramblers, we currently run 2 aligners + 2 miners.
    Try 3 aligners + 1 miner for faster junction capture.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 7:
                return 1, 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            # 3 aligners in 4v4 (leave 1 miner)
            return min(3, num_agents - 1), 0

        # 5+ agents
        return AlphaTournamentV94AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV96Policy(MettagridSemanticPolicy):
    """TournamentV96: zero scramble + aggressive 2-agent + 3 aligners in 4v4."""
    short_names = ["alpha-tournament-v96"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV96AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV97: TV92 (best 2-agent) + 3 aligners in 4v4 (with scramble) ────────────

class AlphaTournamentV97AgentPolicy(AlphaTournamentV92AgentPolicy):
    """TournamentV97: TV92 + 3 aligners in 4v4.

    TV92 has aggressive 2-agent (min_res >= 7) + reduced heart batch.
    TV97 adds 3 aligners instead of 2 in 4v4, while keeping scramble defense.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 7:
                return 1, 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            # 3 aligners in 4v4 (leave 1 miner), no scramblers
            return min(3, num_agents - 1), 0

        # 5+ agents: standard budgets with scramblers from TV82
        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV97Policy(MettagridSemanticPolicy):
    """TournamentV97: TV92 + 3 aligners in 4v4."""
    short_names = ["alpha-tournament-v97"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV97AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV98: TV92 + faster early game (step 100 aligner for all sizes) ───────────

class AlphaTournamentV98AgentPolicy(AlphaTournamentV92AgentPolicy):
    """TournamentV98: TV92 + faster early game.

    Lower the initial aligner delay from step 200 to step 100 for 2-agent,
    and from step 80 to step 50 for 4-agent. Get aligners moving faster.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            # Faster: step 100 instead of 200
            if step < 100 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            # Faster: step 50 instead of 80
            if step < 50:
                return 1, 0
            if min_res < 7:
                return 1, 0
            aligner_budget = min(2, num_agents - 1)
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: standard from TV82
        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV98Policy(MettagridSemanticPolicy):
    """TournamentV98: TV92 + faster early game."""
    short_names = ["alpha-tournament-v98"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV98AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV99: TV90 + 3 aligners in 4v4 (with scramble defense) ──────────────────

class AlphaTournamentV99AgentPolicy(AlphaTournamentV90AgentPolicy):
    """TournamentV99: TV90 (best overall) + 3 aligners in 4v4.

    TV90 is leading at 13.87 with just the min_res >= 7 2-agent change.
    TV99 adds 3 aligners (instead of 2) in 4v4 to capture more junctions.
    Keeps all scramble behavior (idle scramble, stagnation scramble) intact.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        if num_agents <= 4:
            if step < 80:
                return 1, 0
            if min_res < 7:
                return 1, 0
            if min_res < 1 and not can_hearts:
                return 1, 0
            if objective == "economy_bootstrap":
                return 1, 0
            # 3 aligners in 4v4 (leave 1 miner)
            return min(3, num_agents - 1), 0

        # 5+ agents: standard from TV82
        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV99Policy(MettagridSemanticPolicy):
    """TournamentV99: TV90 + 3 aligners in 4v4."""
    short_names = ["alpha-tournament-v99"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV99AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV100: TV90 + faster stagnation detection (200 steps instead of 300) ─────

class AlphaTournamentV100AgentPolicy(AlphaTournamentV90AgentPolicy):
    """TournamentV100: TV90 + faster stagnation detection.

    The current stagnation trigger is 300 steps without junction growth.
    TV100 lowers to 200 steps — detect stagnation earlier and start
    scrambling/exploring sooner to break out of it.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Override stagnation detection to use 200-step threshold."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # 200 instead of 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward (from TV89)
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV100Policy(MettagridSemanticPolicy):
    """TournamentV100: TV90 + faster stagnation."""
    short_names = ["alpha-tournament-v100"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV100AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV101: TV90 + conservative idle scramble (min_res >= 14) ─────────────────

class AlphaTournamentV101AgentPolicy(AlphaTournamentV90AgentPolicy):
    """TournamentV101: TV90 + higher idle scramble threshold.

    Current: idle scramble when hearts > 0 and min_res >= 7.
    TV101: idle scramble only when hearts > 0 and min_res >= 14.
    Conserves hearts for alignment, only scrambles when resources are abundant.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        # Chain-value expand-toward
        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_positions = {e.position for e in [*hubs, *friendly_junctions]}
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            best = min(targets, key=lambda j: (
                _h.manhattan(current_pos, j.position)
                - _chain_expansion_value(j.position, [u for u in unreachable if u is not j], network_positions, max_depth=3) * 5.0
            ))
            dist = _h.manhattan(current_pos, best.position)
            if dist < hp - 20:
                return self._move_to_known(state, best, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Stagnation scramble: same as base (min_res >= 7)
        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # CONSERVATIVE idle scramble: only when resources abundant (min_res >= 14)
        if hearts > 0 and min_res >= 14:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV101Policy(MettagridSemanticPolicy):
    """TournamentV101: TV90 + conservative idle scramble."""
    short_names = ["alpha-tournament-v101"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV101AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV102: TV90 + 2-agent dual aligner (both agents align when resources allow) ──

class AlphaTournamentV102AgentPolicy(AlphaTournamentV90AgentPolicy):
    """TournamentV102: TV90 + dual aligner in 2-agent mode.

    PROBLEM: In 2v6 games, TV90 assigns 1 aligner + 1 miner. The aligner
    aligns ~4 junctions then loops idle_align_scramble forever. The miner
    accumulates massive resources (300+ of each) that are never used.

    FIX: Once hub has enough resources (min_res >= 20), promote BOTH agents
    to aligners. This doubles alignment capacity and uses stockpiled resources.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            # Once resources are healthy, both agents become aligners
            if min_res >= 20:
                return 2, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV102Policy(MettagridSemanticPolicy):
    """TournamentV102: TV90 + dual aligner in 2-agent mode."""
    short_names = ["alpha-tournament-v102"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV102AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV103: TV102 + skip heart batching in 2-agent mode ───────────────────────

class AlphaTournamentV103AgentPolicy(AlphaTournamentV102AgentPolicy):
    """TournamentV103: TV102 (dual aligner) + skip heart batching for 2-agent.

    In 2-agent mode, waiting at hub for 3+ hearts wastes critical time.
    With both agents aligning, they should grab 1 heart and go immediately.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self.policy_env_info.num_agents
        if num_agents <= 2:
            step = state.step or self._step_index
            team_id = _h.team_id(state)

            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            # Skip batching in 2-agent mode — go align immediately with 1 heart
            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()

            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            # If no reachable junctions, explore aggressively instead of scrambling
            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
                dist = _h.manhattan(current_pos, nearest.position)
                if dist < hp - 20:
                    return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

            min_res = _h.team_min_resource(state)

            if self._stagnation_mode and hp > 50:
                if step % 200 < 160 and hearts > 0 and min_res >= 7:
                    scramble_target = self._preferred_scramble_target(state)
                    if scramble_target is not None:
                        return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
                return self._explore_action(state, role="aligner", summary="stag_find_junction")

            if hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        # For non-2-agent games, use parent behavior
        return super()._aligner_action(state)


class AlphaTournamentV103Policy(MettagridSemanticPolicy):
    """TournamentV103: TV102 + skip heart batching in 2-agent mode."""
    short_names = ["alpha-tournament-v103"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV103AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV104: TV90 + explore-first when idle (no scramble in 2-agent) ────────────

class AlphaTournamentV104AgentPolicy(AlphaTournamentV90AgentPolicy):
    """TournamentV104: TV90 + explore-first when aligner is idle in 2-agent.

    Instead of scrambling when no junctions are reachable, explore to find
    more neutral junctions. Scrambling reduces total aligned junctions and
    hurts the shared score.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self.policy_env_info.num_agents
        if num_agents <= 2:
            step = state.step or self._step_index
            team_id = _h.team_id(state)

            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
                self._clear_target_claim()
                self._clear_sticky_target()
                assert hub is not None
                return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()

            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
                dist = _h.manhattan(current_pos, nearest.position)
                if dist < hp - 20:
                    return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

            # In 2-agent mode: prefer explore over scramble
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        return super()._aligner_action(state)


class AlphaTournamentV104Policy(MettagridSemanticPolicy):
    """TournamentV104: TV90 + explore-first in 2-agent mode."""
    short_names = ["alpha-tournament-v104"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV104AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV105: TV102 + TV104 (dual aligner + explore-first, no scramble in 2-agent) ──

class AlphaTournamentV105AgentPolicy(AlphaTournamentV102AgentPolicy):
    """TournamentV105: dual aligner + explore-first in 2-agent mode.

    Combines TV102 (both agents become aligners once resources > 20) with
    TV104's explore-first approach (no idle scramble in 2-agent, always explore).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self.policy_env_info.num_agents
        if num_agents <= 2:
            step = state.step or self._step_index
            team_id = _h.team_id(state)

            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            # Skip batching — go align immediately
            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()

            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
                dist = _h.manhattan(current_pos, nearest.position)
                if dist < hp - 20:
                    return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

            # Always explore — never scramble in 2-agent
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        return super()._aligner_action(state)


class AlphaTournamentV105Policy(MettagridSemanticPolicy):
    """TournamentV105: dual aligner + explore-first in 2-agent."""
    short_names = ["alpha-tournament-v105"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV105AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV106: TV104 (explore-first) + dual aligner (TV102 budget) ────────────────

class AlphaTournamentV106AgentPolicy(AlphaTournamentV104AgentPolicy):
    """TournamentV106: TV104 (explore-first) + TV102 (dual aligner budget).

    TV104 (explore-first) is the best at 14.51. TV102 (dual aligner) helped
    2vX to 12.98. Combine: dual aligner with explore-first behavior.
    TV105 was similar but also removed heart batching — that hurt.
    TV106 keeps heart batching but adds explore-first + dual budget.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 20:
                return 2, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV106Policy(MettagridSemanticPolicy):
    """TournamentV106: TV104 explore-first + TV102 dual aligner."""
    short_names = ["alpha-tournament-v106"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV106AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV107: TV104 + wider explore for 2-agent aligner ─────────────────────────

class AlphaTournamentV107AgentPolicy(AlphaTournamentV104AgentPolicy):
    """TournamentV107: TV104 + wider explore pattern for 2-agent aligner.

    The default aligner explore radius is 22. In 2-agent games, the aligner
    may exhaust nearby junctions quickly. Use wider explore offsets (r=30)
    to cover more of the 88x88 map and find distant neutral junctions.
    """

    _WIDE_EXPLORE_OFFSETS = (
        (0, -30), (21, -21), (30, 0), (21, 21),
        (0, 30), (-21, 21), (-30, 0), (-21, -21),
        (0, -15), (11, -11), (15, 0), (11, 11),
        (0, 15), (-11, 11), (-15, 0), (-11, -11),
    )

    def _explore_action(self, state: MettagridState, *, role: str, summary: str) -> tuple[Action, str]:
        num_agents = self.policy_env_info.num_agents
        if num_agents <= 2 and role == "aligner":
            current_pos = _h.absolute_position(state)
            hub = self._nearest_hub(state)
            center = (hub.global_x, hub.global_y) if hub is not None else current_pos
            offsets = self._WIDE_EXPLORE_OFFSETS
            offset_index = (self._explore_index + self._agent_id) % len(offsets)
            target = offsets[offset_index]
            absolute_target = (center[0] + target[0], center[1] + target[1])
            if _h.manhattan(current_pos, absolute_target) <= 2:
                self._explore_index += 1
                offset_index = (self._explore_index + self._agent_id) % len(offsets)
                target = offsets[offset_index]
                absolute_target = (center[0] + target[0], center[1] + target[1])
            return self._move_to_position(state, absolute_target, summary=summary, vibe=_h.role_vibe(role))
        return super()._explore_action(state, role=role, summary=summary)


class AlphaTournamentV107Policy(MettagridSemanticPolicy):
    """TournamentV107: TV104 + wider explore for 2-agent."""
    short_names = ["alpha-tournament-v107"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV107AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV108: TV104 explore-first for ALL team sizes (not just 2-agent) ──────────

class AlphaTournamentV108AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV108: explore-first for ALL team sizes.

    TV104 showed explore-first dramatically helps 2v6 (14.40 vs 11.80).
    Apply the same approach to all configs: when the aligner has no reachable
    junctions, explore instead of scrambling. This should help in 4v4 too
    since scrambling hurts the shared game score.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        return AlphaTournamentV90AgentPolicy._pressure_budgets(self, state, objective=objective)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        num_agents = self.policy_env_info.num_agents
        if num_agents > 2 and _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()

        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Explore-first for ALL team sizes — no idle scramble
        if self._stagnation_mode and hp > 50:
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV108Policy(MettagridSemanticPolicy):
    """TournamentV108: explore-first for ALL team sizes."""
    short_names = ["alpha-tournament-v108"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV108AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV109: FIXED TV104 (explore-first) — uses shared_team_ids for team size ──

class AlphaTournamentV109AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV109: FIXED explore-first for 2-agent mode.

    CRITICAL BUG FIX: All TV90-TV108 used policy_env_info.num_agents which
    is ALWAYS 8 (total agents in game), not the team count. The `num_agents <= 2`
    check NEVER fired. This version uses len(shared_team_ids) to correctly
    detect 2-agent teams.

    Also fixes _pressure_budgets with the correct team size detection.
    Combines TV88's budget fix with explore-first behavior.
    """

    def _team_size(self) -> int:
        return len(self._shared_team_ids) if self._shared_team_ids else 8

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self._team_size()

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self._team_size()
        if num_agents <= 2:
            step = state.step or self._step_index
            team_id = _h.team_id(state)

            friendly_count = len(self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            ))
            if friendly_count > self._peak_junction_count:
                self._peak_junction_count = friendly_count
                self._last_junction_growth_step = step
                self._stagnation_mode = False
            elif (self._peak_junction_count >= 5
                  and step - self._last_junction_growth_step > 300
                  and step > 500):
                self._stagnation_mode = True

            hearts = int(state.self_state.inventory.get("heart", 0))
            hub = self._nearest_hub(state)

            if hearts <= 0:
                self._clear_target_claim()
                self._clear_sticky_target()
                if not _h.team_can_refill_hearts(state):
                    return self._miner_action(state, summary_prefix="rebuild_hearts_")
                if hub is not None:
                    return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
                return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

            if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
                self._clear_target_claim()
                self._clear_sticky_target()
                assert hub is not None
                return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

            target = self._preferred_alignable_neutral_junction(state)
            if target is not None:
                self._claim_target(target.position)
                self._set_sticky_target(target.position, target.entity_type)
                return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

            self._clear_target_claim()
            self._clear_sticky_target()

            if _h.resource_total(state) > 0:
                depot = self._nearest_friendly_depot(state)
                if depot is not None:
                    return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

            current_pos = _h.absolute_position(state)
            hp = int(state.self_state.inventory.get("hp", 0))
            unreachable = self._known_junctions(
                state, predicate=lambda j: j.owner in {None, "neutral"}
            )
            if unreachable:
                safe_unreachable = [
                    j for j in unreachable
                    if _h.manhattan(current_pos, j.position) < hp - 20
                ]
                targets = safe_unreachable if safe_unreachable else unreachable
                nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
                dist = _h.manhattan(current_pos, nearest.position)
                if dist < hp - 20:
                    return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

            # In 2-agent mode: always explore, never scramble
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        return super()._aligner_action(state)


class AlphaTournamentV109Policy(MettagridSemanticPolicy):
    """TournamentV109: FIXED explore-first for 2-agent mode."""
    short_names = ["alpha-tournament-v109"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV109AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV110: FIXED dual aligner (TV102) + explore-first ─────────────────────────

class AlphaTournamentV110AgentPolicy(AlphaTournamentV109AgentPolicy):
    """TournamentV110: FIXED dual aligner + explore-first.

    Like TV109 but promotes both agents to aligners when resources >= 20.
    Uses correct team size detection.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self._team_size()

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 20:
                return 2, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)


class AlphaTournamentV110Policy(MettagridSemanticPolicy):
    """TournamentV110: FIXED dual aligner + explore-first."""
    short_names = ["alpha-tournament-v110"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV110AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV111: FIXED explore-first for ALL team sizes ─────────────────────────────

class AlphaTournamentV111AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV111: explore-first for ALL team sizes (fixed team detection).

    Like TV108 but with correct team size detection AND applies explore-first
    universally: when the aligner has no reachable junctions, explore instead
    of scrambling. Also uses TV90's budget for 2-agent.
    """

    def _team_size(self) -> int:
        return len(self._shared_team_ids) if self._shared_team_ids else 8

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self._team_size()

        if objective == "resource_coverage":
            return 0, 0

        if num_agents <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 7:
                return 1, 0
            return 0, 0

        return AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = self._team_size()

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Skip batching in 2-agent mode
        if num_agents > 2 and _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()

        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Explore-first for ALL team sizes — no idle scramble
        if self._stagnation_mode and hp > 50:
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV111Policy(MettagridSemanticPolicy):
    """TournamentV111: explore-first for ALL team sizes (fixed)."""
    short_names = ["alpha-tournament-v111"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV111AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV112: TV82 + no idle scramble for 2-agent teams (minimal fix) ────────────

class AlphaTournamentV112AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV112: TV82 + skip idle scramble for small teams.

    INSIGHT: TV90-TV108 all had a bug (num_agents always 8). The actual
    behavior that worked well in tournament is TV82's standard behavior
    which accidentally gives 2-agent teams a large aligner budget.

    The ONLY change: when the aligner has no reachable junctions and would
    normally scramble, skip the scramble and explore instead — BUT ONLY
    for 2-agent teams. This avoids hurting the shared game score.

    Does NOT change _pressure_budgets — the default budget works well.
    """

    def _team_size(self) -> int:
        return len(self._shared_team_ids) if self._shared_team_ids else 8

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self._team_size()
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # For 2-agent teams: NEVER scramble (hurts shared score), always explore
        if num_agents <= 2:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        # For larger teams: standard scramble + explore behavior
        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV112Policy(MettagridSemanticPolicy):
    """TournamentV112: TV82 + no idle scramble for 2-agent teams."""
    short_names = ["alpha-tournament-v112"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV112AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV113: TV112 + no idle scramble for ALL team sizes ────────────────────────

class AlphaTournamentV113AgentPolicy(AlphaTournamentV112AgentPolicy):
    """TournamentV113: TV112 but apply no-scramble to ALL team sizes.

    If scrambling hurts the shared score in 2-agent, it probably hurts in
    4v4 and 6v2 too. Test universal explore-first.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Always explore, never scramble — for ALL team sizes
        if self._stagnation_mode and hp > 50:
            return self._explore_action(state, role="aligner", summary="stag_find_junction")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV113Policy(MettagridSemanticPolicy):
    """TournamentV113: no idle scramble for ALL team sizes."""
    short_names = ["alpha-tournament-v113"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV113AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV114: TV112 + defensive re-alignment when losing territory ─────────────

class AlphaTournamentV114AgentPolicy(AlphaTournamentV112AgentPolicy):
    """TournamentV114: TV112 + defensive re-alignment.

    When we've lost >20% of peak junctions (enemy scramble), suppress offensive
    scrambling and focus on exploring/re-aligning. This prevents wasting hearts
    on scramble when we should be defending our network.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self._team_size()
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # 2-agent: never scramble
        if num_agents <= 2:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        # Defensive: if lost >20% of peak junctions, stop scrambling
        lost_territory = (self._peak_junction_count > 0 and
                         friendly_count < self._peak_junction_count * 0.8)

        if self._stagnation_mode and hp > 50:
            if lost_territory:
                return self._explore_action(state, role="aligner", summary="defensive_explore")
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7 and not lost_territory:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV114Policy(MettagridSemanticPolicy):
    """TournamentV114: TV112 + defensive re-alignment."""
    short_names = ["alpha-tournament-v114"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV114AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV115: TV112 + delayed heart batching until step 500 ────────────────────

class AlphaTournamentV115AgentPolicy(AlphaTournamentV112AgentPolicy):
    """TournamentV115: TV112 + delayed heart batching.

    Heart batching sends aligners back to hub, costing many steps. Delaying
    until step 500 keeps aligners on the field during the crucial early game
    land-grab phase.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        num_agents = self._team_size()
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Delayed batching: only after step 500
        if step >= 500 and _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if num_agents <= 2:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")

        if self._stagnation_mode and hp > 50:
            if step % 200 < 160 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV115Policy(MettagridSemanticPolicy):
    """TournamentV115: TV112 + delayed heart batching."""
    short_names = ["alpha-tournament-v115"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV115AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV116: TV113 (no scramble) + delayed batching (TV115 combo) ─────────────

class AlphaTournamentV116AgentPolicy(AlphaTournamentV113AgentPolicy):
    """TournamentV116: no scramble for all team sizes + delayed batching.

    Combines the two most promising ideas:
    - TV113: never scramble (explore instead)
    - TV115: delayed heart batching until step 500
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # Delayed batching: only after step 500
        if step >= 500 and _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        # Always explore, never scramble
        if self._stagnation_mode and hp > 50:
            return self._explore_action(state, role="aligner", summary="stag_find_junction")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV116Policy(MettagridSemanticPolicy):
    """TournamentV116: no scramble + delayed batching combo."""
    short_names = ["alpha-tournament-v116"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV116AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV117: TV82 + dynamic stagnation with re-alignment priority ─────────────

class AlphaTournamentV117AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV117: TV82 + dynamic stagnation recovery.

    When in stagnation and we've lost >20% of peak junctions, temporarily
    exit stagnation mode to prioritize re-alignment. Still scramble, but
    reduce scramble ratio to 50% when losing territory (vs 80% default).
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Dynamic scramble ratio based on territory status
        losing_territory = (self._peak_junction_count > 0 and
                           friendly_count < self._peak_junction_count * 0.8)
        # When losing: 50% scramble (step%200 < 100). When stable: 80% (step%200 < 160)
        scramble_window = 100 if losing_territory else 160

        if self._stagnation_mode and hp > 50:
            if step % 200 < scramble_window and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV117Policy(MettagridSemanticPolicy):
    """TournamentV117: TV82 + dynamic stagnation recovery."""
    short_names = ["alpha-tournament-v117"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV117AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV118: TV82 + reduced retreat margin (more time on field) ───────────────

class AlphaTournamentV118AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV118: TV82 + less conservative retreat.

    Default retreat margin is 20 + modifiers (can reach 40-60+). This wastes
    many steps walking back to hub. Reduce base margin from 20 to 10, keeping
    agents on the field ~15% more.
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target) -> bool:
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)

        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 10  # Reduced from 20
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 10
        margin += int(state.self_state.inventory.get("heart", 0)) * 5
        margin += min(_h.resource_total(state), 12) // 2
        if not _h.has_role_gear(state, role):
            margin += 10
        if (state.step or 0) >= 2_500:
            margin += 5  # Reduced from 10 for aligner/scrambler
        return hp <= safe_steps + margin


class AlphaTournamentV118Policy(MettagridSemanticPolicy):
    """TournamentV118: TV82 + reduced retreat margin."""
    short_names = ["alpha-tournament-v118"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV118AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV119: TV82 + faster early ramp (start aligning at step 10) ────────────

class AlphaTournamentV119AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV119: TV82 + faster early ramp.

    Default starts with 2 aligners at step 30. TV119 starts 2 aligners at
    step 10 to grab junctions faster in the early land-rush.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # Cap by actual sub-team size
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        # Faster ramp: 2 aligners from step 10 instead of 30
        if step < 10:
            aligner, scrambler = 1, 0
        elif min_res < 10 and not can_hearts:
            aligner, scrambler = 1, 0
        elif min_res < 30:
            aligner, scrambler = 2, 0
        elif min_res < 80:
            aligner, scrambler = 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 500 else 0
            aligner = 4
        else:
            scrambler = 1 if step >= 500 else 0
            aligner = min(5, num_agents - 2 - scrambler)

        max_roles = max(team_size - 1, 1)
        total = aligner + scrambler
        if total > max_roles:
            scrambler = min(scrambler, max(0, max_roles - 1))
            aligner = min(aligner, max_roles - scrambler)

        return aligner, scrambler


class AlphaTournamentV119Policy(MettagridSemanticPolicy):
    """TournamentV119: TV82 + faster early ramp."""
    short_names = ["alpha-tournament-v119"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV119AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV120: TV118 (reduced retreat) + even more aggressive (margin=5) ────────

class AlphaTournamentV120AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV120: TV82 + very aggressive retreat (margin 5).

    TV118 uses margin=10 (vs default 20). TV120 pushes to margin=5.
    Agents stay on the field as long as physically possible.
    Risk: agents may die more often, losing gear.
    """

    def _should_retreat(self, state: MettagridState, role: str, safe_target) -> bool:
        hp = int(state.self_state.inventory.get("hp", 0))
        if safe_target is None:
            return hp <= _h.retreat_threshold(state, role)

        safe_steps = max(0, _h.manhattan(_h.absolute_position(state), safe_target.position) - _h._JUNCTION_AOE_RANGE)
        margin = 5  # Very aggressive (default 20, TV118 uses 10)
        if self._in_enemy_aoe(state, _h.absolute_position(state), team_id=_h.team_id(state)):
            margin += 8
        margin += int(state.self_state.inventory.get("heart", 0)) * 3
        margin += min(_h.resource_total(state), 12) // 3
        if not _h.has_role_gear(state, role):
            margin += 8
        if (state.step or 0) >= 2_500:
            margin += 3
        return hp <= safe_steps + margin


class AlphaTournamentV120Policy(MettagridSemanticPolicy):
    """TournamentV120: TV82 + very aggressive retreat (margin 5)."""
    short_names = ["alpha-tournament-v120"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV120AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV121: TV118 (reduced retreat) + TV117 (dynamic stagnation) combo ───────

class AlphaTournamentV121AgentPolicy(AlphaTournamentV118AgentPolicy):
    """TournamentV121: reduced retreat + dynamic stagnation recovery.

    Combines the two most promising ideas that don't remove scramble:
    - TV118's reduced retreat margin (10)
    - TV117's dynamic scramble ratio when losing territory
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Dynamic scramble from TV117
        losing_territory = (self._peak_junction_count > 0 and
                           friendly_count < self._peak_junction_count * 0.8)
        scramble_window = 100 if losing_territory else 160

        if self._stagnation_mode and hp > 50:
            if step % 200 < scramble_window and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV121Policy(MettagridSemanticPolicy):
    """TournamentV121: reduced retreat + dynamic stagnation combo."""
    short_names = ["alpha-tournament-v121"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV121AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV122: TV82 + dual aligner for 2-agent teams (no miner cap) ────────────

class AlphaTournamentV122AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV122: TV82 + both agents align in 2-agent teams.

    In a 2v6 game, the opponent's 6 agents provide enough mining/resources.
    Having our 2nd agent mine is wasted effort. Instead, let both align.
    This overrides the team cap to allow all 2 agents to be aligners.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner, scrambler = AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)
        # For 2-agent teams: override cap to allow 2 aligners (no miner)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if team_size <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            # Early game: 1 aligner (need resources)
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0
            # Once economy is stable: both align
            if min_res >= 14:
                return 2, 0
            return 1, 0
        return aligner, scrambler


class AlphaTournamentV122Policy(MettagridSemanticPolicy):
    """TournamentV122: TV82 + dual aligner for 2-agent teams."""
    short_names = ["alpha-tournament-v122"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV122AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV123: TV82 + aggressive aligner budgets + earlier 2-agent alignment ────

class AlphaTournamentV123AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV123: TV82 + more aligners when rich + earlier 2-agent play.

    Key insight: scoring is cooperative (both teams get same score). More
    aligners = more junctions = higher team score. When resources are abundant,
    mining extra is wasted effort.

    Changes from TV82/TV7 budgets:
    1. 2-agent: Start 1 aligner at step 200 (not 500) when min_res >= 14 (not 50)
    2. 5+ agents: When rich (min_res >= 200), use 6 aligners + 1 scrambler (1 miner)
       instead of 5 aligners + 1 scrambler (2 miners)
    3. 5+ agents: When very rich (min_res >= 400), ALL non-miner agents align/scramble
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        if team_size <= 2:
            # Earlier alignment: step 200 with min_res >= 14
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 0
            return 0, 0

        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            scrambler = 1 if step >= 200 and team_size >= 4 else 0
            if min_res < 30:
                return 1, scrambler
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1 - scrambler)
            return aligner_budget, scrambler

        # 5+ agents: aggressive scaling
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 500 else 0
            return 4, scrambler
        elif min_res < 400:
            # Rich: 6 aligners + 1 scrambler (only 1 miner)
            scrambler = 1 if step >= 500 else 0
            aligner = min(6, team_size - 1 - scrambler)
            return aligner, scrambler
        else:
            # Very rich: max aligners, keep just 1 miner
            scrambler = 1 if step >= 500 else 0
            aligner = min(team_size - 1 - scrambler, team_size - 1)
            return aligner, scrambler


class AlphaTournamentV123Policy(MettagridSemanticPolicy):
    """TournamentV123: TV82 + aggressive budgets + earlier 2-agent alignment."""
    short_names = ["alpha-tournament-v123"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV123AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV124: TV123 + smarter stagnation (prioritize bridge scramble over explore) ──

class AlphaTournamentV124AgentPolicy(AlphaTournamentV123AgentPolicy):
    """TournamentV124: TV123 + improved stagnation behavior.

    When stagnating, instead of alternating scramble/explore randomly:
    1. Prioritize scrambling enemy junctions within network range (bridges)
    2. If no bridge targets, explore outward from network edge (not random)
    3. When resources are very abundant (min_res > 200), convert stagnating
       aligners to scramblers (every scramble of an enemy junction near our
       network creates a new alignment opportunity)
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        # TV70 improvement: skip batching for 2-agent games until step 200
        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        if self._stagnation_mode and hp > 50:
            # Improved stagnation: always try bridge scramble first (100% of time)
            if hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_bridge_scramble", vibe="change_vibe_scrambler")
            # If no scramble target, explore outward from hub (not random)
            if hub is not None and hp > 80:
                # Explore far from hub to discover distant junctions
                return self._explore_action(state, role="aligner", summary="stag_far_explore")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV124Policy(MettagridSemanticPolicy):
    """TournamentV124: TV123 + smarter stagnation behavior."""
    short_names = ["alpha-tournament-v124"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV124AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV125: TV123 + dual aligner for 2-agent (like TV122 but with TV123 base) ─

class AlphaTournamentV125AgentPolicy(AlphaTournamentV123AgentPolicy):
    """TournamentV125: TV123 + both agents align in 2-agent teams.

    Combines TV123's aggressive budgets with TV122's dual-aligner for 2-agent.
    In 2v6, partner's 6 agents provide resources. Our 2 should both align.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner, scrambler = super()._pressure_budgets(state, objective=objective)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if team_size <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 14:
                return 2, 0
            return 1, 0
        return aligner, scrambler


class AlphaTournamentV125Policy(MettagridSemanticPolicy):
    """TournamentV125: TV123 + dual aligner for 2-agent teams."""
    short_names = ["alpha-tournament-v125"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV125AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV126: TV124 + TV125 (smarter stagnation + dual 2-agent + aggressive budgets) ─

class AlphaTournamentV126AgentPolicy(AlphaTournamentV124AgentPolicy):
    """TournamentV126: All improvements combined.

    - TV123's aggressive aligner budgets (more aligners when rich)
    - TV124's smarter stagnation (bridge scramble priority)
    - TV125's dual aligner for 2-agent teams
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner, scrambler = AlphaTournamentV124AgentPolicy._pressure_budgets(self, state, objective=objective)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if team_size <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 14:
                return 2, 0
            return 1, 0
        return aligner, scrambler


class AlphaTournamentV126Policy(MettagridSemanticPolicy):
    """TournamentV126: All improvements combined."""
    short_names = ["alpha-tournament-v126"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV126AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV127: TV82 + 2-agent scramble specialist ──────────────────────────────

class AlphaTournamentV127AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV127: TV82 + 1 scrambler for 2-agent teams.

    Hypothesis: in 2v6, partner's 6 agents handle alignment. Our 2 agents
    should focus on: 1 miner (sustain economy) + 1 scrambler (disrupt clips'
    network near team edge to create re-alignment opportunities for partner).
    This is the opposite of TV122's dual-aligner approach.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner, scrambler = AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if team_size <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            if step < 300 or (min_res < 7 and not can_hearts):
                return 0, 0  # Both mine early
            # 1 scrambler to disrupt clips, 0 aligners (partner handles alignment)
            if min_res >= 14:
                return 0, 1
            return 0, 0
        return aligner, scrambler


class AlphaTournamentV127Policy(MettagridSemanticPolicy):
    """TournamentV127: TV82 + 2-agent scramble specialist."""
    short_names = ["alpha-tournament-v127"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV127AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV128: TV82 + 2-agent hybrid (1 aligner + 1 scrambler) ─────────────────

class AlphaTournamentV128AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV128: TV82 + mixed roles for 2-agent teams.

    1 aligner + 1 scrambler for 2-agent teams when economy is healthy.
    Aligner expands from our side, scrambler disrupts clips near network edge.
    This tests whether the optimal 2-agent mix is align+scramble rather than
    2x align (TV122) or mine+scramble (TV127).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        aligner, scrambler = AlphaTournamentV82AgentPolicy._pressure_budgets(self, state, objective=objective)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents
        if team_size <= 2:
            step = state.step or self._step_index
            min_res = _h.team_min_resource(state)
            can_hearts = _h.team_can_refill_hearts(state)
            if step < 200 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 14:
                return 1, 1  # 1 aligner + 1 scrambler
            return 1, 0  # Just aligner
        return aligner, scrambler


class AlphaTournamentV128Policy(MettagridSemanticPolicy):
    """TournamentV128: TV82 + 2-agent hybrid (aligner + scrambler)."""
    short_names = ["alpha-tournament-v128"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV128AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV129: TV82 + faster early ramp (2 aligners from step 30 for 4a) ────────

class AlphaTournamentV129AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV129: TV82 + faster early game.

    Key insight: early aligned junctions have outsized impact on average score
    (they contribute for every tick). Get to 2 aligners at step 30 instead of
    step 100 for 4-agent teams. Also add scrambler earlier (step 100 vs 200).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        if team_size <= 2:
            # Keep TV7's conservative 2-agent budget
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        if team_size <= 4:
            # FASTER RAMP: 2 aligners from step 30 instead of step 100
            if step < 30:
                return 1, 0
            scrambler = 1 if step >= 100 and team_size >= 4 else 0  # Earlier scrambler (100 vs 200)
            if min_res < 7 and not can_hearts:
                return 1, scrambler
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1 - scrambler)
            return aligner_budget, scrambler

        # 5+ agents: faster ramp too
        if step < 10:
            return 2, 0  # 2 aligners from step 10 instead of 30

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 300 else 0  # Earlier scrambler
            return 4, scrambler
        else:
            scrambler = 1 if step >= 300 else 0
            aligner = min(5, team_size - 2 - scrambler)
            return aligner, scrambler


class AlphaTournamentV129Policy(MettagridSemanticPolicy):
    """TournamentV129: TV82 + faster early ramp."""
    short_names = ["alpha-tournament-v129"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV129AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV130: TV126 (all improvements) + TV129 (faster ramp) ───────────────────

class AlphaTournamentV130AgentPolicy(AlphaTournamentV126AgentPolicy):
    """TournamentV130: Everything + faster early ramp.

    Combines:
    - TV123's aggressive aligner budgets
    - TV124's smarter stagnation
    - TV125's dual 2-agent aligner
    - TV129's faster early game ramp
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: dual aligner (from TV125)
        if team_size <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 14:
                return 2, 0
            return 1, 0

        # 4-agent: faster ramp (from TV129)
        if team_size <= 4:
            if step < 30:
                return 1, 0
            scrambler = 1 if step >= 100 and team_size >= 4 else 0
            if min_res < 7 and not can_hearts:
                return 1, scrambler
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1 - scrambler)
            return aligner_budget, scrambler

        # 5+ agents: aggressive + fast (from TV123 + TV129)
        if step < 10:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            scrambler = 1 if step >= 300 else 0
            return 4, scrambler
        elif min_res < 400:
            scrambler = 1 if step >= 300 else 0
            aligner = min(6, team_size - 1 - scrambler)
            return aligner, scrambler
        else:
            scrambler = 1 if step >= 300 else 0
            aligner = min(team_size - 1 - scrambler, team_size - 1)
            return aligner, scrambler


class AlphaTournamentV130Policy(MettagridSemanticPolicy):
    """TournamentV130: Everything combined."""
    short_names = ["alpha-tournament-v130"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV130AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV131: TV82 + no dedicated scramblers (aligners scramble when idle) ──────

class AlphaTournamentV131AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV131: TV82 + zero dedicated scramblers.

    Key insight: dedicated scramblers join at step 200+ and ALWAYS scramble.
    But in early game (steps 0-1000), there are many alignable junctions.
    Having that agent be an aligner instead speeds up early expansion.
    Aligners already scramble when idle (idle_align_scramble, stag_scramble),
    so scramble coverage is maintained without a dedicated role.

    For 4-agent teams: 3 aligners + 0 scramblers + 1 miner (vs 2+1+1)
    For 6-agent teams: 5 aligners + 0 scramblers + 1 miner (vs 4+1+1)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: keep TV7's budget
        if team_size <= 2:
            if step < 500 or (min_res < 7 and not can_hearts):
                return 0, 0
            if min_res >= 50:
                return 1, 0
            return 0, 0

        # 4-agent: NO dedicated scrambler, more aligners
        if team_size <= 4:
            if step < 30:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 2, 0
            # 3 aligners + 0 scramblers + 1 miner (vs 2+1+1)
            return min(3, team_size - 1), 0

        # 5+ agents: NO dedicated scrambler, more aligners
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        else:
            # All non-miner agents are aligners (they scramble when idle)
            return min(team_size - 1, 6), 0


class AlphaTournamentV131Policy(MettagridSemanticPolicy):
    """TournamentV131: TV82 + zero dedicated scramblers."""
    short_names = ["alpha-tournament-v131"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV131AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV132: TV131 (no scramblers) + faster ramp + dual 2-agent aligner ────────

class AlphaTournamentV132AgentPolicy(AlphaTournamentV131AgentPolicy):
    """TournamentV132: TV131 + all competitive improvements.

    Combines:
    - TV131's no dedicated scramblers (aligners scramble when idle)
    - TV129's faster ramp (aligners from step 30)
    - TV122's dual aligner for 2-agent teams
    - TV124's smarter stagnation
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        team_size = len(self._shared_team_ids) if self._shared_team_ids else self.policy_env_info.num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: dual aligner (from TV122)
        if team_size <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0  # 1 aligner early
            if min_res >= 14:
                return 2, 0  # Both align
            return 1, 0

        # 4-agent: 3 aligners from step 30, no scrambler
        if team_size <= 4:
            if step < 30:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 2, 0
            return min(3, team_size - 1), 0

        # 5+ agents: max aligners, no scramblers
        if step < 10:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV132AgentStagnationPolicy(AlphaTournamentV132AgentPolicy):
    """TV132 with TV124's improved stagnation."""

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        # Delegate to TV124's aligner action (which has better stagnation)
        return AlphaTournamentV124AgentPolicy._aligner_action(self, state)


class AlphaTournamentV132Policy(MettagridSemanticPolicy):
    """TournamentV132: No scramblers + fast ramp + dual 2a + smart stagnation."""
    short_names = ["alpha-tournament-v132"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV132AgentStagnationPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV133: TV82 + MINIMAL changes (dual 2a + no scrambler for 6a) ────────────

class AlphaTournamentV133AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV133: Minimal improvement over TV82.

    ONLY two changes from TV82:
    1. 2-agent teams: dual aligner (from TV122, proven at v483=13.07 comp)
    2. 5+ agent teams: replace dedicated scrambler with extra aligner
       (aligners already scramble when idle via idle_align_scramble/stag_scramble)

    4-agent teams: EXACT same budget as TV82 (don't touch what works)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: dual aligner (TV122, proven)
        if team_size <= 2:
            if step < 200 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 14:
                return 2, 0
            return 1, 0

        # 4-agent: EXACT TV82 budget (AdaptiveV3 for num_agents <= 4)
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV82 base but replace scrambler with aligner
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            # TV82 would have: 4 aligners + 1 scrambler
            # TV133: 5 aligners + 0 scramblers (replace scrambler with aligner)
            return min(5, team_size - 1), 0
        else:
            # Rich: max aligners, no scramblers
            return min(team_size - 1, 6), 0


class AlphaTournamentV133Policy(MettagridSemanticPolicy):
    """TournamentV133: TV82 + dual 2a + no scrambler for 6a."""
    short_names = ["alpha-tournament-v133"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV133AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV134: TV133 + faster 2a start + lower dual-aligner threshold ────────────

class AlphaTournamentV134AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV134: TV133 improvements + faster 2-agent engagement.

    Changes from TV133:
    1. 2-agent: start aligning at step 100 (was 200) — partner's 6 agents carry economy
    2. 2-agent: dual aligner at min_res >= 7 (was 14) — align more aggressively
    3. 4-agent: EXACT TV82 (proven, don't touch)
    4. 5+ agent: same as TV133 (no scramblers)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: faster start + more aggressive dual aligner
        if team_size <= 2:
            if step < 100 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 7:
                return 2, 0  # Dual align earlier (was min_res >= 14)
            return 1, 0

        # 4-agent: EXACT TV82 budget (proven, don't touch)
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV133 (no scramblers)
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            return min(5, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV134Policy(MettagridSemanticPolicy):
    """TournamentV134: TV133 + faster 2a start + lower dual-aligner threshold."""
    short_names = ["alpha-tournament-v134"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV134AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV135: TV134 + faster 6a ramp when economy is strong ─────────────────────

class AlphaTournamentV135AgentPolicy(AlphaTournamentV134AgentPolicy):
    """TournamentV135: TV134 + more aggressive 6a expansion.

    When 6a team has strong economy, ramp to max aligners faster.
    Changes from TV134:
    - 5+ agents: 4 aligners at min_res >= 50 (was 80 in TV134/TV133)
    - 5+ agents: max aligners at min_res >= 100 (was 200)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: same as TV134 (fast start + dual aligner)
        if team_size <= 2:
            if step < 100 or (min_res < 7 and not can_hearts):
                return 1, 0
            if min_res >= 7:
                return 2, 0
            return 1, 0

        # 4-agent: EXACT TV82 (proven, don't touch)
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: faster ramp than TV134 (carry harder as 6a)
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV135Policy(MettagridSemanticPolicy):
    """TournamentV135: TV134 + faster 6a aligner ramp."""
    short_names = ["alpha-tournament-v135"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV135AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV136: Ultra-fast 2a start — both align from step 1 ─────────────────────

class AlphaTournamentV136AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV136: Ultra-fast 2a start. Both agents align immediately.

    Key insight: score = avg junctions/tick across 10K steps. Early junctions
    contribute to more ticks → disproportionate impact. In 2v6, partner's 6
    agents handle economy. Our 2 agents should align from step 1.

    Initial hub has 24 of each resource. 1 heart costs 7 of each.
    2 agents can each get 1 heart immediately → both align from step 1.

    Changes from TV134:
    - 2-agent: both align from step 1 (no mining wait)
    - 2-agent: always dual aligner (no min_res check — partner handles economy)
    - 4-agent: EXACT TV82
    - 5+ agents: same as TV133 (no scramblers)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: BOTH align from step 1 (partner's 6 agents handle economy)
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0  # Emergency: need hearts, 1 mine
            return 2, 0  # Always dual aligner

        # 4-agent: EXACT TV82 budget (proven, don't touch)
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV133 (no scramblers)
        if step < 30:
            return 2, 0

        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            return min(5, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV136Policy(MettagridSemanticPolicy):
    """TournamentV136: Ultra-fast 2a (both align from step 1)."""
    short_names = ["alpha-tournament-v136"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV136AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV137: Faster 4a start — align sooner, ramp faster ──────────────────────

class AlphaTournamentV137AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV137: Faster 4-agent engagement.

    4a=11.1 underperforms expected ~13.5. Hypothesis: TV82's 4a is too
    conservative early (waits until step 100, needs min_res 30 for 2 aligners).

    Changes from TV82 (4a only):
    - Start aligning at step 50 (was 100)
    - 2 aligners at min_res >= 20 (was 30)
    - 3 aligners at min_res >= 80, step >= 300 (was 100/500)
    2a/6a: same as TV136/TV133
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: FASTER than TV82
        if team_size <= 4:
            if step < 50:
                return 1, 0  # Was 100 in TV82
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0  # Was 30 in TV82
            aligner_budget = 2
            if min_res >= 80 and step >= 300:  # Was 100/500 in TV82
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV133 (no scramblers)
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            return min(5, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV137Policy(MettagridSemanticPolicy):
    """TournamentV137: Faster 4a start (step 50, min_res 20)."""
    short_names = ["alpha-tournament-v137"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV137AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV138: Combined best — TV136 2a + TV137 4a + TV135 6a ───────────────────

class AlphaTournamentV138AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV138: Best of all worlds.

    Combines proven improvements across all team sizes:
    - 2a: TV136 ultra-fast (both align from step 1)
    - 4a: TV137 faster (step 50, min_res 20, earlier 3-aligner)
    - 6a: TV135 faster ramp (50/100 thresholds)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: TV137 faster
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV138Policy(MettagridSemanticPolicy):
    """TournamentV138: Combined best (TV136 2a + TV137 4a + TV135 6a)."""
    short_names = ["alpha-tournament-v138"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV138AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV139: Blitz start — max aligners from step 1 for all team sizes ────────

class AlphaTournamentV139AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV139: Blitz start. Max aligners from step 1.

    Key insight from self-play analysis: score peaks at step 500 (14 junctions)
    then declines as Clips NPC catches up. Every junction-tick before NPC
    recovery matters. Maximize early aligner count.

    Strategy: Use initial hub resources (24 each, enough for 3 hearts) to
    get aligners running immediately. Minimal economy—just enough to sustain
    hearts. Score is won in first 1000 steps.

    All team sizes: 2 aligners from step 1 (use initial hub resources).
    Economy agents mine only when aligner can't get hearts.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: BLITZ — 2 aligners from step 1
        if team_size <= 4:
            if not can_hearts and min_res < 7:
                return 1, 0  # Emergency: need economy
            if step < 30:
                return 2, 0  # 2 aligners from step 1!
            if min_res < 15:
                return 2, 0  # Keep 2 aligners even with low res
            aligner_budget = 2
            if min_res >= 50 and step >= 200:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: blitz — 3 aligners from step 1
        if not can_hearts and min_res < 7:
            return 1, 0
        if step < 30:
            return 3, 0  # 3 aligners from step 1!
        if min_res < 20:
            return 2, 0
        elif min_res < 40:
            return 3, 0
        elif min_res < 80:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV139Policy(MettagridSemanticPolicy):
    """TournamentV139: Blitz start (max aligners from step 1)."""
    short_names = ["alpha-tournament-v139"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV139AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV140: Economy support for 2a — mine instead of dual-align ───────────────

class AlphaTournamentV140AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV140: Adaptive 2a strategy.

    Hypothesis: against aggressive 6-agent opponents, our 2 agents can't hold
    junctions. Instead of futile dual alignment, use 1 aligner + 1 miner.
    The miner feeds team economy so partner's 6 agents sustain pressure.

    - 2a: 1 aligner + 1 miner. Dual align only when economy is strong (min_res >= 20).
    - 4a/6a: same as TV137 (faster start, proven)
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: 1 aligner + 1 miner (economy support)
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 0, 0  # Both mine — emergency
            if min_res >= 20:
                return 2, 0  # Dual align when economy strong
            return 1, 0  # Default: 1 aligner + 1 miner

        # 4-agent: TV137 (faster start)
        if team_size <= 4:
            if not can_hearts and min_res < 7:
                return 1, 0
            if step < 50:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV133 (no scramblers)
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 80:
            return 3, 0
        elif min_res < 200:
            return min(5, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV140Policy(MettagridSemanticPolicy):
    """TournamentV140: Economy support for 2a (1 aligner + 1 miner)."""
    short_names = ["alpha-tournament-v140"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV140AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV141: TV138 + earlier 4a start (step 30 vs 50) ─────────────────────────

class AlphaTournamentV141AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV141: TV138 with even faster 4a.

    TV138 starts 4a at step 50. TV137 showed 4a=11.90 is better than TV82's
    4a=9.92. Let's try step 30 to gain more early junction-ticks.

    Also try: 2 aligners as default for 4a even with low min_res (skip the
    min_res < 20 → 1 aligner check). The hub starts with 24 resources,
    enough for 1-2 hearts. With 4 agents, 2 miners sustain the economy.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: even faster than TV137
        if team_size <= 4:
            if not can_hearts and min_res < 7:
                return 1, 0  # Emergency
            if step < 30:
                return 1, 0  # Brief mining start
            # Always 2 aligners after step 30 (skip min_res < 20 check)
            aligner_budget = 2
            if min_res >= 60 and step >= 200:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV141Policy(MettagridSemanticPolicy):
    """TournamentV141: TV138 + even faster 4a (step 30, no min_res gate)."""
    short_names = ["alpha-tournament-v141"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV141AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV142: TV138 + 50% stagnation scramble (vs 80%) ─────────────────────────

class AlphaTournamentV142AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV142: TV138 budgets + reduced stagnation scramble.

    TV138 is current best. This variant tests whether 50% scramble in
    stagnation (vs 80%) improves by giving more exploration time.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        # Exact TV138 budgets
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """Override aligner to use 50% stagnation scramble instead of 80%."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # KEY CHANGE: 50% scramble in stagnation (was 80%)
        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV142Policy(MettagridSemanticPolicy):
    """TournamentV142: TV138 + 50% stagnation scramble."""
    short_names = ["alpha-tournament-v142"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV142AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV143: Optimal combo — TV136 2a + TV82 4a + TV135 6a ────────────────────

class AlphaTournamentV143AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV143: Data-driven optimal combination.

    Based on tournament results vs v54 (strongest opponent):
    - 2a: TV136 ultra-fast (14.59) — best 2a strategy
    - 4a: TV82 original (12.21) — slightly better than TV137's 11.97
    - 6a: TV135 faster ramp (16.00) — best 6a strategy

    This differs from TV138 which used TV137's faster 4a start.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast (both align from step 1)
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: EXACT TV82 (proven best vs strong opponents)
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 30:
                return 1, 0
            aligner_budget = 2
            if min_res >= 100 and step >= 500:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV143Policy(MettagridSemanticPolicy):
    """TournamentV143: Optimal combo (TV136 2a + TV82 4a + TV135 6a)."""
    short_names = ["alpha-tournament-v143"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV143AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV144: TV143 + lower 4a 2-aligner threshold (min_res 15 vs 30) ──────────
# NOTE: TV144 tests min_res 15 for 4a 2nd aligner. Results pending.

class AlphaTournamentV144AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV144: TV143 with lower 4a threshold.

    Match log analysis shows 4a stuck at 1 aligner until step ~800 because
    carbon bottleneck keeps min_res below 30. Lowering to 15 should get
    2 aligners by step ~400, adding ~400 extra steps of 2-aligner pressure.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: TV82 with LOWER 2-aligner threshold
        if team_size <= 4:
            if step < 100:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 15:  # Was 30 in TV82/TV143
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 400:  # Also slightly faster 3-aligner
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV144Policy(MettagridSemanticPolicy):
    """TournamentV144: TV143 + lower 4a threshold (min_res 15)."""
    short_names = ["alpha-tournament-v144"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV144AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV145: Aggressive 4a — no economy gate for 2nd aligner ──────────────────

class AlphaTournamentV145AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV145: Aggressive 4a — trust partner economy.

    Carbon bottleneck analysis shows 4a stuck at 1 aligner until step ~800
    because min_res stays below 30 (carbon consumed at 3x rate).

    TV144 lowered threshold to 15. TV145 goes further: start 2 aligners
    at step 50 unconditionally. Rationale:
    - Both players share the cogs network (cooperative scoring)
    - Partner's miners contribute to hub economy
    - Each alignment-tick matters more than each resource
    - 2a (TV136) proved that ultra-fast alignment beats economy-first
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast (both align from step 1)
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: aggressive — 2 aligners from step 50 regardless of economy
        if team_size <= 4:
            if step < 50:
                return 1, 0  # Brief economy bootstrap
            if min_res < 1 and not can_hearts:
                return 1, 0  # Emergency only: zero resources, can't make hearts
            aligner_budget = 2  # Always 2 from step 50 — trust partner economy
            if min_res >= 80 and step >= 400:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV145Policy(MettagridSemanticPolicy):
    """TournamentV145: Aggressive 4a (no economy gate for 2nd aligner)."""
    short_names = ["alpha-tournament-v145"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV145AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV146: Carbon-priority mining + aggressive 4a ────────────────────────────

class AlphaTournamentV146AgentPolicy(AlphaTournamentV145AgentPolicy):
    """TournamentV146: TV145 + carbon-priority mining.

    Carbon is consumed at 3x rate (aligner costs carbon:3 vs 1 each for others).
    By pre-emptively biasing miners toward carbon, we keep the economy flowing
    and avoid the carbon bottleneck that stalls aligner ramp-up.
    """

    def _macro_directive(self, state: MettagridState) -> MacroDirective:
        """Carbon-priority: bias toward carbon when it's within 2x of min resource."""
        resources = _shared_resources(state)
        least = _least_resource(resources)
        carbon = resources.get("carbon", 0)
        min_amount = min(resources.values())

        # If carbon is within 2x of the minimum resource, prioritize it
        # because it depletes 3x faster due to aligner costs
        if carbon <= min_amount * 2 + 10:
            return MacroDirective(resource_bias="carbon")
        return MacroDirective(resource_bias=least)


class AlphaTournamentV146Policy(MettagridSemanticPolicy):
    """TournamentV146: Aggressive 4a + carbon-priority mining."""
    short_names = ["alpha-tournament-v146"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV146AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV147: Ultra-aggressive 4a — 2 aligners from step 1 ─────────────────────

class AlphaTournamentV147AgentPolicy(AlphaTournamentV82AgentPolicy):
    """TournamentV147: Ultra-aggressive 4a — align from step 1 like 2a.

    TV136 proved ultra-fast works for 2a (14.59 vs 12.06). TV145 showed
    step 50 start is better than step 100 for 4a. TV147 pushes further:
    start 2 aligners from step 1. With 4 agents, 2 align + 2 mine from
    the very start. Partner economy should compensate for lost mining time.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: ultra-aggressive — 2 aligners from step 1
        if team_size <= 4:
            if min_res < 1 and not can_hearts and step > 100:
                return 1, 0  # Emergency only after initial phase
            aligner_budget = 2  # Always 2 from step 1
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: TV135 faster ramp
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 30:
            return 2, 0
        elif min_res < 50:
            return 3, 0
        elif min_res < 100:
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV147Policy(MettagridSemanticPolicy):
    """TournamentV147: Ultra-aggressive 4a (2 aligners from step 1)."""
    short_names = ["alpha-tournament-v147"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV147AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV148: TV145 + hub-proximity bias for aligner targeting ──────────────────

class AlphaTournamentV148AgentPolicy(AlphaTournamentV145AgentPolicy):
    """TournamentV148: TV145 + hub-proximity targeting for compact network.

    Idea: prioritize junctions closer to hub for a compact, dense network
    that's harder to scramble and easier to re-align. The hub_penalty was
    part of v65's original scoring — let's add a stronger version.
    """

    def _nearest_alignable_neutral_junction(self, state: MettagridState) -> KnownEntity | None:
        """Override: bias toward junctions closer to hub for compact network."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else None
        hubs = self._world_model.entities(entity_type="hub", predicate=lambda entity: entity.team == team_id)
        friendly_junctions = self._known_junctions(state, predicate=lambda entity: entity.owner == team_id)
        network_sources = [*hubs, *friendly_junctions]
        candidates = []
        for entity in self._known_junctions(state, predicate=lambda junction: junction.owner in {None, "neutral"}):
            if not _h.within_alignment_network(entity.position, network_sources):
                continue
            candidates.append(entity)
        if not candidates:
            return None
        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate

        # Score candidates: distance from self + hub proximity bonus
        # Closer to hub = lower score = preferred
        def _target_score(entity: KnownEntity) -> float:
            dist_to_self = _h.manhattan(current_pos, entity.position)
            dist_to_hub = _h.manhattan(hub_pos, entity.position) if hub_pos else 0
            # Weight hub proximity: each step from hub adds 0.5 to score
            return dist_to_self + dist_to_hub * 0.5

        return min(candidates, key=lambda e: (_target_score(e), e.position))


class AlphaTournamentV148Policy(MettagridSemanticPolicy):
    """TournamentV148: TV145 + hub-proximity targeting."""
    short_names = ["alpha-tournament-v148"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV148AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV149: TV147 + patrol defense in stagnation mode ─────────────────────────

class AlphaTournamentV149AgentPolicy(AlphaTournamentV147AgentPolicy):
    """TournamentV149: TV147 + patrol defense during stagnation.

    Problem: in stagnation mode (step 1000+), aligners roam to scramble
    enemy junctions but our own junctions keep getting scrambled by Clips.
    Friendly junction count drops from ~12 to ~3 by step 2000.

    Fix: instead of scrambling, patrol near friendly junctions. When Clips
    scrambles one of ours, we're nearby to immediately re-align it. This
    should maintain junction count much better through the mid-late game.

    Strategy: pick the nearest friendly junction (or hub) and move toward
    it. Every few ticks check if there's a neutral junction in alignment
    range (recently scrambled). If so, re-align it.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        # Always check for alignable junctions first (including recently scrambled ones)
        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # PATROL DEFENSE: in stagnation, patrol near friendly junctions
        # instead of roaming to scramble. When Clips scrambles our junctions,
        # we'll be nearby to immediately re-align.
        if self._stagnation_mode and hp > 50:
            friendly_junctions = self._known_junctions(
                state, predicate=lambda j: j.owner == team_id
            )
            if friendly_junctions and hearts > 0:
                # 60% patrol, 40% scramble (mixed defense/offense)
                if step % 200 < 120:
                    # Patrol: move toward nearest friendly junction
                    nearest_friendly = min(
                        friendly_junctions,
                        key=lambda j: _h.manhattan(current_pos, j.position)
                    )
                    dist_to_friendly = _h.manhattan(current_pos, nearest_friendly.position)
                    if dist_to_friendly > 5:
                        return self._move_to_known(
                            state, nearest_friendly,
                            summary="patrol_defend", vibe="change_vibe_aligner"
                        )
                    # Already near a junction — scramble nearby enemies
                    scramble_target = self._preferred_scramble_target(state)
                    if scramble_target is not None:
                        scramble_dist = _h.manhattan(current_pos, scramble_target.position)
                        if scramble_dist < 20:  # Only scramble nearby
                            return self._move_to_known(
                                state, scramble_target,
                                summary="patrol_scramble", vibe="change_vibe_scrambler"
                            )
                    return self._explore_action(state, role="aligner", summary="patrol_scan")
                else:
                    # Scramble phase
                    if min_res >= 7:
                        scramble_target = self._preferred_scramble_target(state)
                        if scramble_target is not None:
                            return self._move_to_known(
                                state, scramble_target,
                                summary="stag_scramble", vibe="change_vibe_scrambler"
                            )
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV149Policy(MettagridSemanticPolicy):
    """TournamentV149: TV147 + patrol defense in stagnation."""
    short_names = ["alpha-tournament-v149"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV149AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV150: TV147 + ultra-aggressive 6a (5 aligners from step 30) ─────────────

class AlphaTournamentV150AgentPolicy(AlphaTournamentV147AgentPolicy):
    """TournamentV150: TV147 + ultra-aggressive 6a.

    TV147's 6a uses TV135's gradual ramp (2→3→4→5→6 based on min_res).
    But TV136 proved ultra-fast works for 2a, and TV147 proved it for 4a.
    Try the same for 6a: start 5 aligners from step 30 regardless of economy.
    With 8 total agents (6 ours + 2 partner), 5 aligners + 1 miner from us.
    Partner has 2 agents mining.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: TV147 ultra-aggressive
        if team_size <= 4:
            if min_res < 1 and not can_hearts and step > 100:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: ULTRA-AGGRESSIVE — 5 aligners from step 30
        if step < 30:
            return 2, 0  # Brief economy bootstrap
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0  # Emergency
        # 5 aligners, 1 miner (partner handles some mining)
        aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV150Policy(MettagridSemanticPolicy):
    """TournamentV150: TV147 + ultra-aggressive 6a."""
    short_names = ["alpha-tournament-v150"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV150AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV151: TV147 + balanced-aggressive 6a (4 aligners from step 30) ──────────

class AlphaTournamentV151AgentPolicy(AlphaTournamentV147AgentPolicy):
    """TournamentV151: TV147 + balanced-aggressive 6a.

    TV150 tried 5 aligners from step 30 but economy was too starved.
    TV147 uses gradual ramp (2→3→4→5) which is too slow.
    TV151: 4 aligners from step 30, leaving 2 miners for economy.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: TV147 ultra-aggressive
        if team_size <= 4:
            if min_res < 1 and not can_hearts and step > 100:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: BALANCED-AGGRESSIVE — 4 aligners from step 30
        if step < 30:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0  # Emergency
        # 4 aligners, 2 miners (better economy than TV150's 5a/1m)
        aligner_budget = min(team_size - 2, 4)
        if min_res >= 100 and step >= 500:
            # Economy surplus: push to 5 aligners
            aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV151Policy(MettagridSemanticPolicy):
    """TournamentV151: TV147 + balanced-aggressive 6a."""
    short_names = ["alpha-tournament-v151"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV151AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV152: TV151 + adaptive 2a (mine when partner is bad) ────────────────────

class AlphaTournamentV152AgentPolicy(AlphaTournamentV151AgentPolicy):
    """TournamentV152: TV151 + adaptive 2a.

    Problem: 2a scores 8.59 when partner is bad (v50 6a), vs 14+ with good
    partners. Ultra-fast 2a (both align from step 1) fails when partner
    can't maintain economy.

    Fix: in 2a mode, check economy health every 200 steps. If min_res < 5
    and step > 300, drop to 1 aligner so the other agent can mine and
    rebuild. Resume 2 aligners when economy recovers (min_res >= 10).
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: ADAPTIVE — detect bad partner economy
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0  # Economy crisis: 1 aligner, 1 miner
            # After step 300, if economy is struggling, scale back
            if step > 300 and min_res < 5:
                return 1, 0  # Partner not mining enough — help
            return 2, 0  # Ultra-fast when economy is healthy

        # 4-agent: TV147 ultra-aggressive
        if team_size <= 4:
            if min_res < 1 and not can_hearts and step > 100:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: TV151 balanced-aggressive
        if step < 30:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0
        aligner_budget = min(team_size - 2, 4)
        if min_res >= 100 and step >= 500:
            aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV152Policy(MettagridSemanticPolicy):
    """TournamentV152: TV151 + adaptive 2a."""
    short_names = ["alpha-tournament-v152"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV152AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV153: TV151 budgets + TV142 stagnation (50% scramble) ───────────────────

class AlphaTournamentV153AgentPolicy(AlphaTournamentV151AgentPolicy):
    """TournamentV153: TV151 aggressive budgets + TV142 stagnation behavior.

    v506 (TV142) is #1 at 14.23. Its key innovation: 50% stagnation scramble
    instead of 80%. TV151 has the best budget config (aggressive 4a + balanced 6a).
    Combining both should be the overall best.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        """TV151 aligner with TV142's 50% stagnation scramble."""
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # TV142's 50% stagnation scramble (key to #1 ranking)
        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV153Policy(MettagridSemanticPolicy):
    """TournamentV153: TV151 budgets + TV142 50% stagnation scramble."""
    short_names = ["alpha-tournament-v153"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV153AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV161: TV142 + carbon-weighted mining ─────────────────────────────────────

class AlphaTournamentV161AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV161: TV142 + carbon-weighted mining priority.

    Aligner costs carbon:3, oxygen:1, germanium:1, silicon:1.
    Carbon depletes 3x faster. Weighting carbon 3x in mining priority
    should keep hub better stocked for sustained alignment.
    (Was TV156 but renamed to avoid collision with pre-existing TV156 ultimate combo.)
    """

    def _preferred_miner_extractor(self, state: MettagridState) -> KnownEntity | None:
        """Override to weight carbon 3x in resource priority."""
        if self._should_force_miner_explore_reset(state):
            self._clear_sticky_target()
            return None

        current_pos = _h.absolute_position(state)
        shared_inv = {} if state.team_summary is None else state.team_summary.shared_inventory

        # Weight carbon by dividing its effective inventory by 3
        # (since aligners consume 3x carbon vs other elements)
        def _weighted_inv(resource: str) -> int:
            raw = int(shared_inv.get(resource, 0))
            if resource == "carbon":
                return raw // 3  # Effective value is 1/3 since consumed 3x faster
            return raw

        priority = sorted(
            _ELEMENTS,
            key=lambda r: (
                _weighted_inv(r),
                0 if r == self._resource_bias else 1,
                r,
            ),
        )

        candidates: list[KnownEntity] = []
        for resource_name in priority:
            matches = self._world_model.entities(
                entity_type=f"{resource_name}_extractor",
                predicate=lambda entity: _h.is_usable_recent_extractor(entity, step=state.step or self._step_index),
            )
            candidates.extend(
                sorted(
                    matches,
                    key=lambda entity: (_h.manhattan(current_pos, entity.position), entity.position),
                )
            )
        if not candidates:
            return None

        directed_candidate = self._directive_target_candidate(candidates)
        if directed_candidate is not None:
            return directed_candidate

        sticky = self._sticky_miner_target(state)
        if sticky is None:
            return candidates[0]

        candidate = candidates[0]
        sticky_distance = _h.manhattan(current_pos, sticky.position)
        candidate_distance = _h.manhattan(current_pos, candidate.position)
        if candidate.position != sticky.position and candidate_distance + 5 < sticky_distance:
            return candidate
        return sticky


class AlphaTournamentV161Policy(MettagridSemanticPolicy):
    """TournamentV161: TV142 + carbon-weighted mining."""
    short_names = ["alpha-tournament-v161"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV161AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV159: TV142 + 30% stagnation scramble (vs 50%) ──────────────────────────

class AlphaTournamentV159AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV159: TV142 but with 30% stagnation scramble (step%200<60).

    TV142's 50% stag scramble is key to #1 (14.30). But maybe even LESS
    scrambling in stagnation is better — more exploration to find missed
    junctions. Test: step%200 < 60 = 30% scramble, 70% explore.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # KEY CHANGE: 30% scramble in stagnation (vs TV142's 50%)
        if self._stagnation_mode and hp > 50:
            if step % 200 < 60 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV159Policy(MettagridSemanticPolicy):
    """TournamentV159: TV142 + 30% stagnation scramble."""
    short_names = ["alpha-tournament-v159"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV159AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV160: TV142 + faster stagnation entry (200 steps, step>300) ──────────────

class AlphaTournamentV160AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV160: TV142 but stagnation triggers earlier.

    TV142 enters stagnation after 300 steps no growth && step>500.
    TV160: 200 steps no growth && step>300. Faster detection should
    let us start scrambling the clips network sooner.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200  # Faster: 200 vs 300
              and step > 300):  # Earlier: step 300 vs 500
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Same 50% stag scramble as TV142
        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV160Policy(MettagridSemanticPolicy):
    """TournamentV160: TV142 + faster stagnation entry."""
    short_names = ["alpha-tournament-v160"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV160AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV157: TV142 + ultra-aggressive 6a (5 aligners from step 15) ─────────────

class AlphaTournamentV157AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV157: TV142 + ultra-aggressive 6a.

    TV142's 6a uses gradual ramp (1→2→3→4→6 based on min_res thresholds).
    This variant pushes to 5 aligners from step 15 with minimal economy gate.
    Theory: since early expansion is everything, max aligners ASAP.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: EXACT TV142/TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: EXACT TV142 (step 50, min_res 20 gate)
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: ULTRA-AGGRESSIVE — 5 aligners from step 15
        if step < 15:
            return 2, 0  # Brief bootstrap
        if min_res < 1 and not can_hearts and step > 100:
            return 2, 0  # Emergency only
        aligner_budget = min(team_size - 1, 5)  # Max aligners immediately
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV157Policy(MettagridSemanticPolicy):
    """TournamentV157: TV142 + ultra-aggressive 6a."""
    short_names = ["alpha-tournament-v157"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV157AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV158: TV156 + TV157 combined (carbon mining + aggressive 6a) ────────────

class AlphaTournamentV158AgentPolicy(AlphaTournamentV161AgentPolicy):
    """TournamentV158: Carbon-weighted mining + ultra-aggressive 6a.

    Combines TV156's carbon-priority mining with TV157's aggressive 6a budget.
    Theory: more aligners need more carbon → carbon mining helps even more.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        # Use TV157's aggressive 6a budgets
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # Ultra-aggressive 6a
        if step < 15:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 100:
            return 2, 0
        aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV158Policy(MettagridSemanticPolicy):
    """TournamentV158: Carbon mining + aggressive 6a."""
    short_names = ["alpha-tournament-v158"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV158AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV154: TV153 with TV145's 4a (step 50 instead of step 1) ────────────────

class AlphaTournamentV154AgentPolicy(AlphaTournamentV153AgentPolicy):
    """TournamentV154: TV153 with TV145's proven 4a (step 50).

    Leaderboard shows v509 (TV145, step 50) = 13.52 (#5) outperforms
    v510 (TV147, step 1) = 13.45 (#14). So step 50 is better than step 1
    for 4a in tournament. TV154 = TV153 but with step 50 for 4a.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: TV145 (step 50, proven better than step 1 in tournament)
        if team_size <= 4:
            if step < 50:
                return 1, 0  # Brief economy bootstrap
            if min_res < 1 and not can_hearts:
                return 1, 0  # Emergency
            aligner_budget = 2  # 2 from step 50
            if min_res >= 80 and step >= 400:
                aligner_budget = min(3, team_size - 1)
            if objective == "economy_bootstrap":
                return 1, 0
            return aligner_budget, 0

        # 5+ agents: TV151 balanced-aggressive
        if step < 30:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0
        aligner_budget = min(team_size - 2, 4)
        if min_res >= 100 and step >= 500:
            aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV154Policy(MettagridSemanticPolicy):
    """TournamentV154: TV153 with TV145's 4a (step 50)."""
    short_names = ["alpha-tournament-v154"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV154AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV155: Exact TV142 budgets + TV151 6a only ──────────────────────────────

class AlphaTournamentV155AgentPolicy(AlphaTournamentV153AgentPolicy):
    """TournamentV155: Exact TV142/v506 #1 formula + TV151's 6a improvement.

    v506 (TV142) is #1 at 14.30. The ONLY change from TV142 is the 6a
    budget: TV151's 4 aligners from step 30 instead of TV135's gradual ramp.
    All other behavior (2a, 4a, stagnation) stays EXACTLY as TV142.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: EXACT TV142/TV136 ultra-fast
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: EXACT TV142/TV137 (step 50, min_res 20 gate)
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0  # TV142's economy gate — proven #1
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV151 balanced (ONLY CHANGE from TV142)
        if step < 30:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0
        aligner_budget = min(team_size - 2, 4)
        if min_res >= 100 and step >= 500:
            aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV155Policy(MettagridSemanticPolicy):
    """TournamentV155: Exact TV142 + TV151 6a only."""
    short_names = ["alpha-tournament-v155"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV155AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV156: Ultimate combo — TV142 stag + TV142 4a + TV151 6a + TV152 2a ──────

class AlphaTournamentV156AgentPolicy(AlphaTournamentV155AgentPolicy):
    """TournamentV156: Ultimate combo of all proven-best components.

    Combines:
    - TV142's 50% stagnation scramble (key to #1 v506)
    - TV142's 4a budgets (step 50, min_res 20 — proven better than no gate)
    - TV151's 6a balanced (4 aligners from step 30 — self-play validated)
    - TV152's adaptive 2a (drop to 1 aligner when partner bad — #2 in tournament)

    Only change from TV155 (which is TV142 + TV151 6a): adds adaptive 2a.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: TV152 ADAPTIVE (detect bad partner economy)
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            if step > 300 and min_res < 5:
                return 1, 0  # Partner not mining enough — help
            return 2, 0

        # 4-agent: EXACT TV142/TV137 (step 50, min_res 20 gate)
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV151 balanced
        if step < 30:
            return 2, 0
        if min_res < 1 and not can_hearts and step > 200:
            return 2, 0
        aligner_budget = min(team_size - 2, 4)
        if min_res >= 100 and step >= 500:
            aligner_budget = min(team_size - 1, 5)
        if objective == "economy_bootstrap":
            return 2, 0
        return aligner_budget, 0


class AlphaTournamentV156Policy(MettagridSemanticPolicy):
    """TournamentV156: Ultimate combo (TV142 stag + TV142 4a + TV151 6a + TV152 2a)."""
    short_names = ["alpha-tournament-v156"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV156AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV162: TV142 + slightly lower 6a thresholds ──────────────────────────────

class AlphaTournamentV162AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV162: TV142 with slightly lower 6a resource thresholds.

    TV142's 6a ramp: 10→30→50→100 (1→2→3→4→6 aligners).
    TV162: 10→25→40→80 — slightly faster expansion, still conservative.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: EXACT TV142
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: EXACT TV142
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: slightly lower thresholds than TV142
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 25:  # Was 30
            return 2, 0
        elif min_res < 40:  # Was 50
            return 3, 0
        elif min_res < 80:  # Was 100
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV162Policy(MettagridSemanticPolicy):
    """TournamentV162: TV142 + slightly lower 6a thresholds."""
    short_names = ["alpha-tournament-v162"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV162AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV163: TV142 + adaptive stag scramble ─────────────────────────────────────

class AlphaTournamentV163AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV163: Adaptive stagnation scramble instead of fixed 50%.

    Instead of step%200 < 100 (fixed 50%), scramble only when enemy junctions
    are known nearby. Otherwise explore. This should be more efficient:
    - Don't waste steps scrambling when no targets visible
    - Don't waste steps exploring when scramble targets available
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 300
              and step > 500):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # ADAPTIVE stag scramble: scramble when targets available, explore otherwise
        if self._stagnation_mode and hp > 50:
            if hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            # No scramble target — always explore
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV163Policy(MettagridSemanticPolicy):
    """TournamentV163: TV142 + adaptive stag scramble."""
    short_names = ["alpha-tournament-v163"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV163AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV164: Even faster stagnation entry (150 steps, step>200) ─────────────────

class AlphaTournamentV164AgentPolicy(AlphaTournamentV142AgentPolicy):
    """TournamentV164: TV142 but stagnation triggers even faster than TV160.

    TV160 (v519=#1 at 14.46): 200 steps no growth && step>300.
    TV164: 150 steps no growth && step>200 — push it further.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 150  # Even faster: 150 vs 200
              and step > 200):  # Even earlier: 200 vs 300
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Same 50% stag scramble as TV142
        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        if hearts > 0 and min_res >= 7:
            scramble_target = self._preferred_scramble_target(state)
            if scramble_target is not None:
                return self._move_to_known(state, scramble_target, summary="idle_align_scramble", vibe="change_vibe_scrambler")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV164Policy(MettagridSemanticPolicy):
    """TournamentV164: Even faster stagnation (150 steps, step>200)."""
    short_names = ["alpha-tournament-v164"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV164AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV165: TV160 + TV162's lower 6a thresholds ───────────────────────────────

class AlphaTournamentV165AgentPolicy(AlphaTournamentV160AgentPolicy):
    """TournamentV165: Combines TV160 (faster stag entry, #1) + TV162 (lower 6a thresholds, #4).

    TV160 (v519) = 14.46, TV162 (v522) = 14.05. Combining the two best
    improvements from TV142.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: EXACT TV142
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: EXACT TV142
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: TV162's lower thresholds
        if step < 30:
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 25:  # Was 30
            return 2, 0
        elif min_res < 40:  # Was 50
            return 3, 0
        elif min_res < 80:  # Was 100
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV165Policy(MettagridSemanticPolicy):
    """TournamentV165: TV160 + TV162 combined."""
    short_names = ["alpha-tournament-v165"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV165AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV166: TV160 + NO idle scramble (cooperative scoring optimization) ────────

class AlphaTournamentV166AgentPolicy(AlphaTournamentV160AgentPolicy):
    """TournamentV166: TV160 but remove idle scramble.

    Since scoring is cooperative (both teams share the same score),
    scrambling the opponent's junctions hurts OUR score too.
    Keep stagnation scramble (needed to break plateaus) but remove
    the idle_align_scramble that fires when no neutral junctions visible.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200
              and step > 300):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # Stagnation scramble still needed to break plateaus
        if self._stagnation_mode and hp > 50:
            if step % 200 < 100 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # NO idle scramble — in cooperative scoring, scrambling hurts both teams
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV166Policy(MettagridSemanticPolicy):
    """TournamentV166: TV160 + no idle scramble (cooperative scoring)."""
    short_names = ["alpha-tournament-v166"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV166AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV167: TV160 + reduced stagnation scramble (25% instead of 50%) ───────────

class AlphaTournamentV167AgentPolicy(AlphaTournamentV160AgentPolicy):
    """TournamentV167: TV160 but only 25% stagnation scramble.

    TV160 uses 50% stag scramble (step%200<100). Since cooperative scoring
    means scramble hurts both teams, try reducing to 25% (step%200<50).
    Still breaks plateaus but less destructive.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200
              and step > 300):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # 25% stag scramble (was 50%): step%200<50 instead of step%200<100
        if self._stagnation_mode and hp > 50:
            if step % 200 < 50 and hearts > 0 and min_res >= 7:
                scramble_target = self._preferred_scramble_target(state)
                if scramble_target is not None:
                    return self._move_to_known(state, scramble_target, summary="stag_scramble", vibe="change_vibe_scrambler")
            return self._explore_action(state, role="aligner", summary="stag_find_junction")

        # No idle scramble
        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV167Policy(MettagridSemanticPolicy):
    """TournamentV167: TV160 + 25% stag scramble + no idle scramble."""
    short_names = ["alpha-tournament-v167"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV167AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV168: TV160 + more aggressive early expansion (faster 6a ramp) ───────────

class AlphaTournamentV168AgentPolicy(AlphaTournamentV160AgentPolicy):
    """TournamentV168: TV160 + faster early expansion for 6a.

    TV160's 6a ramp: step<30->2, then gates on min_res (30->2, 50->3, 100->4+).
    This variant: step 15->2 aligners, and lower first threshold (min_res 20->2).
    Theory: the first 100 steps of alignment are highest-value (most neutrals
    near hub), so getting 2 aligners out 15 steps earlier compounds.
    """

    def _pressure_budgets(self, state: MettagridState, *, objective: str | None = None) -> tuple[int, int]:
        step = state.step or self._step_index
        min_res = _h.team_min_resource(state)
        can_hearts = _h.team_can_refill_hearts(state)
        num_agents = self.policy_env_info.num_agents
        team_size = len(self._shared_team_ids) if self._shared_team_ids else num_agents

        if objective == "resource_coverage":
            return 0, 0

        # 2-agent: same as TV160/TV142
        if team_size <= 2:
            if not can_hearts and min_res < 7:
                return 1, 0
            return 2, 0

        # 4-agent: same as TV160/TV142
        if team_size <= 4:
            if step < 50:
                return 1, 0
            if min_res < 7 and not can_hearts:
                return 1, 0
            if min_res < 20:
                return 1, 0
            aligner_budget = 2
            if min_res >= 80 and step >= 300:
                aligner_budget = min(3, team_size - 1)
            return aligner_budget, 0

        # 5+ agents: FASTER early ramp
        if step < 15:  # Was 30
            return 2, 0
        if min_res < 10 and not can_hearts:
            return 1, 0
        elif min_res < 20:  # Was 30
            return 2, 0
        elif min_res < 40:  # Was 50
            return 3, 0
        elif min_res < 80:  # Was 100
            return min(4, team_size - 1), 0
        else:
            return min(team_size - 1, 6), 0


class AlphaTournamentV168Policy(MettagridSemanticPolicy):
    """TournamentV168: TV160 + faster 6a early expansion."""
    short_names = ["alpha-tournament-v168"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV168AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV169: TV160 + ZERO scramble (pure cooperative — never scramble) ──────────

class AlphaTournamentV169AgentPolicy(AlphaTournamentV160AgentPolicy):
    """TournamentV169: TV160 but never scramble at all.

    Cooperative scoring means BOTH teams share the same score.
    Scrambling opponent junctions directly reduces our score.
    Even stagnation scramble hurts — just explore and mine instead.
    """

    def _aligner_action(self, state: MettagridState) -> tuple[Action, str]:
        step = state.step or self._step_index
        team_id = _h.team_id(state)
        num_agents = len(self._shared_team_ids) if self._shared_team_ids else 8

        friendly_count = len(self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        ))
        if friendly_count > self._peak_junction_count:
            self._peak_junction_count = friendly_count
            self._last_junction_growth_step = step
            self._stagnation_mode = False
        elif (self._peak_junction_count >= 5
              and step - self._last_junction_growth_step > 200
              and step > 300):
            self._stagnation_mode = True

        hearts = int(state.self_state.inventory.get("heart", 0))
        hub = self._nearest_hub(state)

        if hearts <= 0:
            self._clear_target_claim()
            self._clear_sticky_target()
            if not _h.team_can_refill_hearts(state):
                return self._miner_action(state, summary_prefix="rebuild_hearts_")
            if hub is not None:
                return self._move_to_known(state, hub, summary="acquire_heart", vibe="change_vibe_heart")
            return self._explore_action(state, role="aligner", summary="find_hub_for_heart")

        if num_agents <= 2 and step < 200:
            pass
        elif step < 200:
            pass
        elif _h.should_batch_hearts(state, role="aligner", hub_position=hub.position if hub else None):
            self._clear_target_claim()
            self._clear_sticky_target()
            assert hub is not None
            return self._move_to_known(state, hub, summary="batch_hearts", vibe="change_vibe_heart")

        target = self._preferred_alignable_neutral_junction(state)
        if target is not None:
            self._claim_target(target.position)
            self._set_sticky_target(target.position, target.entity_type)
            return self._move_to_known(state, target, summary="align_junction", vibe="change_vibe_aligner")

        self._clear_target_claim()
        self._clear_sticky_target()
        if _h.resource_total(state) > 0:
            depot = self._nearest_friendly_depot(state)
            if depot is not None:
                return self._move_to_known(state, depot, summary="deposit_cargo", vibe="change_vibe_aligner")

        current_pos = _h.absolute_position(state)
        hp = int(state.self_state.inventory.get("hp", 0))
        unreachable = self._known_junctions(
            state, predicate=lambda j: j.owner in {None, "neutral"}
        )
        if unreachable:
            safe_unreachable = [
                j for j in unreachable
                if _h.manhattan(current_pos, j.position) < hp - 20
            ]
            targets = safe_unreachable if safe_unreachable else unreachable
            nearest = min(targets, key=lambda j: _h.manhattan(current_pos, j.position))
            dist = _h.manhattan(current_pos, nearest.position)
            if dist < hp - 20:
                return self._move_to_known(state, nearest, summary="expand_toward_junction", vibe="change_vibe_aligner")

        min_res = _h.team_min_resource(state)

        # ZERO scramble — even in stagnation, just explore/mine
        if self._stagnation_mode:
            if step % 200 < 100:
                return self._explore_action(state, role="aligner", summary="stag_find_junction")
            if min_res < 30:
                return self._miner_action(state, summary_prefix="stag_mine_")
            return self._explore_action(state, role="aligner", summary="stag_explore")

        if step % 200 < 100:
            return self._explore_action(state, role="aligner", summary="find_neutral_junction")
        if min_res < 30:
            return self._miner_action(state, summary_prefix="idle_align_")
        return self._explore_action(state, role="aligner", summary="find_neutral_junction")


class AlphaTournamentV169Policy(MettagridSemanticPolicy):
    """TournamentV169: TV160 + zero scramble (pure cooperative)."""
    short_names = ["alpha-tournament-v169"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV169AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]


# ── TV170: TV166 + network-boundary-aware stagnation scramble ─────────────────

class AlphaTournamentV170AgentPolicy(AlphaTournamentV166AgentPolicy):
    """TournamentV170: TV166 + smarter stagnation scramble targeting.

    TV166 uses the default scramble targeting (nearest enemy). This variant
    instead prioritizes enemy junctions that are within alignment range (15)
    of our friendly junctions or hub (25). When scrambled, these become
    neutral and are immediately re-alignable, creating efficient network growth.
    """

    def _preferred_scramble_target(self, state: MettagridState) -> KnownEntity | None:
        """Prioritize enemy junctions within alignment range of our network."""
        team_id = _h.team_id(state)
        current_pos = _h.absolute_position(state)
        hub = self._nearest_hub(state)
        hub_pos = hub.position if hub is not None else current_pos

        enemy_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner not in {None, "neutral", team_id}
        )
        if not enemy_junctions:
            return None

        friendly_junctions = self._known_junctions(
            state, predicate=lambda j: j.owner == team_id
        )

        def boundary_scramble_score(candidate: KnownEntity) -> float:
            distance = float(_h.manhattan(current_pos, candidate.position))

            # Strong bonus for junctions within alignment range of our network
            within_network = any(
                _h.manhattan(candidate.position, f.position) <= 15
                for f in friendly_junctions
            )
            within_hub = _h.manhattan(candidate.position, hub_pos) <= 25
            network_bonus = 50.0 if within_network else (30.0 if within_hub else 0.0)

            return distance - network_bonus

        best = min(enemy_junctions, key=boundary_scramble_score)

        # Use sticky target
        sticky = self._sticky_scramble_target(state)
        if sticky is None:
            return best

        sticky_score = boundary_scramble_score(sticky)
        best_score = boundary_scramble_score(best)

        if best.position != sticky.position and best_score + 5.0 < sticky_score:
            return best
        return sticky


class AlphaTournamentV170Policy(MettagridSemanticPolicy):
    """TournamentV170: TV166 + network-boundary scramble targeting."""
    short_names = ["alpha-tournament-v170"]

    def agent_policy(self, agent_id: int) -> AgentPolicy:
        self._shared_team_ids.add(agent_id)
        if agent_id not in self._agent_policies:
            self._agent_policies[agent_id] = AlphaTournamentV170AgentPolicy(
                self.policy_env_info,
                agent_id=agent_id,
                world_model=SharedWorldModel(),
                shared_claims=self._shared_claims,
                shared_junctions=self._shared_junctions,
                shared_hotspots=self._shared_hotspots,
                shared_team_ids=self._shared_team_ids,
            )
        return self._agent_policies[agent_id]
